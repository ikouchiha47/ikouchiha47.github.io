<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://ikouchiha47.github.io/feed.xml" rel="self" type="application/atom+xml" /><link href="https://ikouchiha47.github.io/" rel="alternate" type="text/html" /><updated>2026-08-26T21:37:05+00:00</updated><id>https://ikouchiha47.github.io/feed.xml</id><title type="html">iko’s logs</title><subtitle>Yet another platform engineer who&apos;d rather design systems, single malts, and write tools than glue together someone else&apos;s API.</subtitle><author><name>ikouchiha47</name></author><entry><title type="html">Making a Small Language Model Actually Useful in Movie Search</title><link href="https://ikouchiha47.github.io/2026/08/26/mofy-search-distilbert.html" rel="alternate" type="text/html" title="Making a Small Language Model Actually Useful in Movie Search" /><published>2026-08-26T00:00:00+00:00</published><updated>2026-08-26T00:00:00+00:00</updated><id>https://ikouchiha47.github.io/2026/08/26/mofy-search-distilbert</id><content type="html" xml:base="https://ikouchiha47.github.io/2026/08/26/mofy-search-distilbert.html"><![CDATA[<h2 id="what-mofy-is">What Mofy is</h2>

<p>Mofy is a personal movie and TV library app for Android. Point it at the folders where your media lives, or let it match torrents you already have, and it builds a local catalog with posters, overviews, genres, ratings, the works - pulled from TMDB once, cached forever after. It plays what you own, resumes where you left off, and lets you watch something with someone else in a different room, in sync, over a room code.</p>

<p>None of this touches a server you don’t control, and none of it ever will: it’s not going on an app store, so there was never a pressure to phone home for analytics, ads, or a subscription check. That constraint sounds like a limitation. It turned out to be the most useful constraint in the whole project, because it ruled out the easy answer to the one hard problem the app has: search.</p>

<p>Everyone who has actually managed a personal media library knows the boring version of this problem. You have three hundred movies. You do not remember which folder “that one with the guy who loses his memory” is in. Typing “memento” works, if you remember it’s called Memento. Typing “a guy loses his memory every day and writes notes to himself” should also work, and on Mofy, it does - that query correctly surfaces Memento as the top result. Getting there is the subject of this post.</p>

<h2 id="the-two-shapes-of-a-search-query-and-why-one-api-call-cant-cleanly-do-both">The two shapes of a search query, and why one API call can’t cleanly do both</h2>

<p>Sit with real queries for a minute and they split into two families that want completely different handling:</p>

<ul>
  <li><strong>Structured queries</strong>: “90s highly rated action”, “dark thriller from the 80s under 2 hours”, “download inception 2010 1080p”. These have facets - a date range, a genre, a runtime ceiling, a rating floor - that are either right or wrong. There’s no partial credit for guessing 1985 when the user meant “the 90s.”</li>
  <li><strong>Vibe queries</strong>: “feeling nostalgic”, “something to watch when I don’t want to think”, “a guy loses his memory every day and writes notes to himself”. These have no correct discrete answer. They want <em>similar</em>, not <em>equal</em>.</li>
</ul>

<p>The naive move is to throw the whole query at an LLM and ask it to return both - hand back a JSON blob with <code class="language-plaintext highlighter-rouge">genre</code>, <code class="language-plaintext highlighter-rouge">date_range</code>, <code class="language-plaintext highlighter-rouge">mood_embedding_hint</code>, done. That’s the shape every hosted-API tutorial reaches for, and it’s the first thing that was off the table here, for a reason that had nothing to do with model quality:</p>

<blockquote>
  <p>No API-based LLM calls, anywhere in the app, no exceptions.</p>
</blockquote>

<p>A personal media app that ships nothing to a store has no business making its search feature depend on someone else’s uptime, someone else’s rate limit, or someone else’s server logging every query a user types. If it can’t run on the phone with the network off, it doesn’t belong in the app.</p>

<p>So the real question was never “which API.” It was: how much of “understand what the user meant” can actually run inside a Compose app on a mid-range Android phone, with no server on the other end of it.</p>

<p>The answer turned out to be two separate small models doing two separate small jobs, not one model doing everything. This post is about how that split got found, including the part where the first several attempts at the structured half were quietly, confidently wrong in a way that a loss curve does not tell you about.</p>

<pre><code class="language-mermaid">flowchart LR
    Q["free-text query"] --&gt; S{"split"}
    S --&gt;|"structured slots"| F["facet classifier\n(this post)"]
    S --&gt;|"leftover text"| E["EmbeddingGemma\nvector search"]
    F --&gt; R["ranking"]
    E --&gt; R
    R --&gt; O["results"]
</code></pre>

<p>Next: why the vibe half was the easy half, and why the obvious move for the structured half - just generate JSON - turned out to be a dead end worth documenting in detail.</p>

<h2 id="the-vibe-half-embeddings-run-entirely-on-the-phone">The vibe half: embeddings, run entirely on the phone</h2>

<p>Start with the part that turned out to be simpler than expected, because it sets the baseline the rest of the post keeps referring back to.</p>

<p>An embedding model turns a sentence into a fixed-length vector - a few hundred numbers - such that sentences with similar meaning end up as vectors that point in similar directions. “A guy loses his memory every day” and Memento’s actual plot summary don’t share many words, but they point roughly the same way in that space. Cosine similarity between two vectors gives you a single number for “how alike,” and that’s the entire mechanism behind “search by vibe”: embed the query once, embed every movie’s overview once (offline, ahead of time), and rank movies by how close their vector lands to the query’s.</p>

<p>Two decisions made this work fully offline:</p>

<p><strong>The model</strong>: <a href="https://ai.google.dev/gemma/docs/embeddinggemma">EmbeddingGemma-300M</a>, a sub-<code class="language-plaintext highlighter-rouge">500M</code>-parameter embedding model from Google that leads its size class on MTEB (the standard embedding benchmark), shipped as a ready-made <code class="language-plaintext highlighter-rouge">.tflite</code> file that runs through LiteRT, Google’s on-device inference runtime. No conversion pipeline to build - Google already did the export.</p>

<p>The same checkpoint runs twice: once offline in Python to embed the entire movie catalog ahead of time, and once on-device to embed whatever the user types. Query and catalog vectors have to come from the exact same model and the exact same prompt template, or the comparison is meaningless - more on that below.</p>

<p><strong>The storage</strong>: plain SQLite BLOB columns, one float32 array per movie, about <code class="language-plaintext highlighter-rouge">3KB</code> per vector. That sounds almost too simple for a “vector database,” and that’s deliberate - Google’s own AI Edge RAG Library ships the identical pattern (<code class="language-plaintext highlighter-rouge">SqliteVectorStore</code>) for exactly this reason. The alternative, a native <code class="language-plaintext highlighter-rouge">vec0</code> virtual table via the <code class="language-plaintext highlighter-rouge">sqlite-vec</code> extension, needs a custom SQLite driver loaded underneath Room, Android’s usual database layer, and Room’s default driver doesn’t support that.</p>

<p>At a personal-library scale - hundreds to a few thousand titles - a brute-force scan comparing the query vector against every stored vector is fast enough that the native extension buys nothing but integration risk. Retrieval is a streaming top-K scan with a min-heap, so the whole catalog never has to sit in memory at once, just the heap and whichever row is currently being decoded.</p>

<p>One detail that looked cosmetic and wasn’t: EmbeddingGemma doesn’t take a raw sentence. It expects a fixed prefix depending on what you’re embedding - <code class="language-plaintext highlighter-rouge">task: search result | query: {text}</code> for a search query, <code class="language-plaintext highlighter-rouge">title: {title} | text: {text}</code> for a document.</p>

<p>These are literal strings baked into the model’s own <code class="language-plaintext highlighter-rouge">config_sentence_transformers.json</code>, the file <code class="language-plaintext highlighter-rouge">sentence-transformers</code> reads at load time. Skip the prefix, or reformat it as <code class="language-plaintext highlighter-rouge">{title}: {text}</code> because that reads more naturally, and the model gets an input shape it was never fine-tuned to see, which quietly degrades retrieval quality without throwing any error you’d notice.</p>

<p>That’s the trap with a lot of “the model card says X” claims: docs drift from what a release actually ships, so it’s worth pulling the config the model itself loads and reading the literal keys rather than trusting the paraphrase. With the correct templates, a plain title search like “the dark knight” correctly ranks the exact title first, and the Memento plot query ranks Memento first - the retrieval-tuned prompt pair working as documented, not just documented.</p>

<p>That’s the whole vibe path: embed, store as a blob, brute-force cosine similarity, rank. No training, no labeled data, no classifier - just a general-purpose sentence encoder pointed at overview text. It handles “feeling nostalgic” and “a guy loses his memory every day” equally well, because neither of those has a <em>correct</em> discrete answer, only a <em>closer</em> one.</p>

<p>It does not, however, know what “under 2 hours” means. Ask it and it will happily return movies with no relationship to runtime at all, because nothing in a 768-dimensional similarity space encodes an inequality. That’s a different kind of problem, and it needed a completely different kind of model.</p>

<h2 id="the-other-half-of-the-vibe-problem-getting-text-worth-embedding-at-all">The other half of the vibe problem: getting text worth embedding at all</h2>

<p>None of the retrieval mechanism above matters if the overview text it’s embedding is thin or missing. The catalog is <code class="language-plaintext highlighter-rouge">31,785</code> titles filtered from IMDb’s public datasets, and combining a static Kaggle movie dataset with scoped TMDB API calls got real overview text for <code class="language-plaintext highlighter-rouge">63.2%</code> of them. TMDB’s API is generous about rate limits, free tier included, so calling it wasn’t the bottleneck; TMDB simply doesn’t have overview text for every title in its database, particularly older, lower-profile, or less mainstream ones. That gap is a coverage limit of the source itself, not a throttling problem, and it left roughly <code class="language-plaintext highlighter-rouge">11,700</code> titles with no usable text: no overview at all, or a degenerate one (empty, the title copied verbatim into the overview field, or a “No overview found.” sentinel).</p>

<p>Direct bulk-scraping imdb.com is against IMDb’s own terms of service, and it was already ruled out for that reason during the catalog build. What’s fine, and what this used, is driving a real browser session through pages one at a time, the same way a person clicking through IMDb would, using <a href="https://browsermcp.io">Browser MCP</a>, a Chrome extension that exposes an MCP-shaped interface to a real Chrome tab.</p>

<p>The stock extension is built to be driven by an actual MCP server over a WebSocket handshake. That layer wasn’t needed here, so <code class="language-plaintext highlighter-rouge">ml/scripts/05_fetch_imdb_plots.py</code> skips it and speaks the extension’s plain JSON protocol directly: it opens a WebSocket listener on <code class="language-plaintext highlighter-rouge">localhost:9009</code>, the extension dials into it, and the script drives it title by title, navigating to each <code class="language-plaintext highlighter-rouge">imdb.com/title/{id}/plotsummary/</code> page and pulling the Plot Summaries and Synopsis text out of the page’s accessibility snapshot.</p>

<p>Two problems showed up running this at the actual scale needed, and both needed a small patch to the extension itself (vendored into <code class="language-plaintext highlighter-rouge">vendor/browser-mcp-extension/</code>, not just used as-is from the Chrome Web Store):</p>

<ul>
  <li><strong>Chrome kills the extension’s background service worker after about 30 seconds of idle, and an open WebSocket does not keep it alive.</strong> The connection just dies with the worker, silently, mid-run. The patch adds a <code class="language-plaintext highlighter-rouge">chrome.alarms</code> wake every 30 seconds that revives the worker and re-establishes the socket, so a dropped connection reconnects within roughly a second instead of stranding the run.</li>
  <li><strong>The stock extension can only read the page, not run arbitrary JavaScript in it</strong>, which matters for pages where the useful text isn’t in a clean accessibility-tree label. The patch adds a <code class="language-plaintext highlighter-rouge">browser_evaluate</code> tool that runs a JS expression in the connected tab via the already-attached debugger connection and returns the result directly.</li>
</ul>

<p>Even patched, this wasn’t fast: real page loads, a deliberate wait between titles to look like a human browsing rather than a script hammering the site, and the reconnect handling above every time Chrome decided to nap the worker mid-batch. The whole run to fetch <code class="language-plaintext highlighter-rouge">9,311</code> titles took on the order of <strong>30 hours</strong>, most of it just waiting on page loads and reconnects rather than doing any real work per title. It also checkpoints after every single title to a JSONL file (atomic rewrite, not appended-and-hope), so a run interrupted at hour 20 resumes at title 9,000-something rather than starting over, and a <code class="language-plaintext highlighter-rouge">--skip-existing</code> flag distinguishes titles that actually succeeded from ones tagged <code class="language-plaintext highlighter-rouge">failed</code> for retry on the next pass.</p>

<p>Thirty hours of a script babysitting a browser tab is not a glamorous part of the pipeline. But EmbeddingGemma’s retrieval quality is only as good as the text it’s given to embed, and a plot query like “a guy loses his memory every day and writes notes to himself” resolving correctly to Memento depends on that title actually having real synopsis text in the catalog in the first place, not a sentinel string. The unglamorous scraping stint is what the “vibe half” of this whole post is quietly standing on.</p>

<h2 id="the-structured-half-why-just-generate-json-doesnt-work">The structured half: why “just generate JSON” doesn’t work</h2>

<p>“Under 2 hours” is a number. “The 80s” is a range. “Highly rated” is a threshold. None of these are vibes to get <em>close to</em> - they’re predicates a movie either satisfies or doesn’t, and the only acceptable failure mode is “didn’t extract it,” never “extracted the wrong year and silently filtered out the right movie.” That distinction, approximate is fine for vibes, wrong is not fine for facets, is what ruled out reusing the embedding approach here.</p>

<p>It’s worth actually testing before assuming it, since “embeddings can do everything” is a seductive idea. The test: take six facet types (date, genre, rating, popularity, runtime, mood), write two or three example phrases for each, embed all of them with EmbeddingGemma’s classification prompt, and see whether a new query fragment lands closest to the right bucket by nearest neighbor.</p>

<p>Genre, date, rating, popularity, and runtime routed correctly with decent margins. But <code class="language-plaintext highlighter-rouge">nostalgic</code> landed closest to the <em>date</em> anchors, not mood, because nostalgia is semantically entangled with “the past,” and an embedding space that was never asked to separate those two things won’t separate them for you.</p>

<p>That’s a real, reproducible failure mode, and it’s exactly the kind of quietly-wrong-but-plausible-looking result a hard filter cannot tolerate: mood queries would get mistakenly hard-filtered by year and silently return nothing. Embeddings are the right tool for “how similar,” and the wrong tool for “which exact year range.”</p>

<p>So facets needed something that outputs a fixed, typed structure, correctly. The instinctive approach - and the one that got tried first, several different ways - was to lean on a small generative model and just ask it to produce JSON.</p>

<ul>
  <li>
    <p><strong>Attempt 1 - flan-t5-small, generate JSON spans directly.</strong> Feed it the query, ask it to emit something like <code class="language-plaintext highlighter-rouge">{"type": "genre", "span": "thriller"}</code>. This broke for a reason that had nothing to do with the model’s reasoning: T5’s tokenizer, built on SentencePiece, doesn’t have <code class="language-plaintext highlighter-rouge">{</code> or <code class="language-plaintext highlighter-rouge">}</code> as tokens it was trained to use fluently for structured output. The model emitted garbled fragments like <code class="language-plaintext highlighter-rouge">s"pan:…type:mod</code>, malformed text that no downstream parser could recover, rather than anything close to valid JSON.</p>
  </li>
  <li>
    <p><strong>Attempt 2 - switch formats, not architecture.</strong> The instinct at this point was “the format is the problem,” so the code switched to TANL-style spans (<code class="language-plaintext highlighter-rouge">[ span | type | norm ]</code>, the augmented natural language format from an AWS entity-tagging writeup, built for exactly this kind of information-extraction task) instead of JSON, on the theory that a simpler flat grammar would be easier for a small seq2seq model to reproduce reliably. It wasn’t - the underlying checkpoint had already been trained toward JSON-shaped outputs, so the model kept reverting to its old shape regardless of what the training data now looked like. Still free-form generation, still needing a brittle string parser to turn output back into a structure, just with a different string to fail at.</p>
  </li>
</ul>

<p>A handful of other output formats got weighed at the same time, on paper, before deciding none of them were worth a full training run:</p>

<ul>
  <li><strong>S-expressions</strong> (<code class="language-plaintext highlighter-rouge">(genre thriller) (date 1980 1989)</code>) - same bracket-tokenization risk as JSON, if the base tokenizer doesn’t treat <code class="language-plaintext highlighter-rouge">(</code> and <code class="language-plaintext highlighter-rouge">)</code> as first-class tokens.</li>
  <li><strong>BIO tagging</strong> (<code class="language-plaintext highlighter-rouge">B-GENRE I-GENRE O O</code>) - a solid format for sequence labeling, but it assumes the model is tagging each input token, not generating output text. Doesn’t fit a seq2seq decoder without a different architecture entirely.</li>
  <li><strong>Pipe-delimited fields</strong> (<code class="language-plaintext highlighter-rouge">genre=thriller|date=1980-1989</code>) - sidesteps the bracket problem, but it’s still free-form text generation underneath, with the same failure mode as TANL: nothing stops the model from emitting a value that isn’t in the schema, or dropping a field silently.</li>
</ul>

<p>Every one of these is a better string format than raw JSON. None of them changes the actual problem: a decoder generating output token by token has no built-in notion of “this field must be one of these 27 genres.” That constraint lives only in the training data’s shape, not in the model’s output space, so it can be violated as easily as followed.</p>

<ul>
  <li>
    <p><strong>Attempt 3 - few-shot prompting, smaller models.</strong> Gemma 270M and Qwen 0.5B, prompted with a handful of examples instead of fine-tuned, asked to emit either JSON or a simpler binary “which facets are present” answer. Open-ended generation from a model that size, with only a few examples in context, is unreliable - and even where it worked, binary presence detection only answers “is there a date facet in this query,” not “what is the actual year range,” which is the part that matters for filtering.</p>
  </li>
  <li>
    <p><strong>Attempt 4 - DSPy chain-of-thought span typing.</strong> Qwen variants, prompted to reason step by step about each span before typing it. Same failure family: parse errors on the output, and the model inventing span types that were never in the schema in the first place - the model isn’t being unreasonable, it’s doing exactly what generative models do when the output space isn’t constrained, which is produce something plausible-looking rather than something valid.</p>
  </li>
</ul>

<p>The pattern across all four is the same one, and it’s the real lesson to take from this whole detour:</p>

<blockquote>
  <p>A low training loss on any of these did not mean usable output.</p>
</blockquote>

<p><code class="language-plaintext highlighter-rouge">flan-t5-small</code>’s cross-entropy loss looked fine in every one of these runs. Loss measures “did the model get closer to predicting the next token the training data expects,” and every one of these checkpoints did that.</p>

<p>None of them measures “if I now run this output through a real parser, does it come out as a valid, correct structure.” That number, measured by actually decoding real outputs rather than reading a training curve, was bad in every attempt.</p>

<p>The lesson that survived all four attempts: small generative models are bad at freestyle structure, and they are fine at classification into a schema that’s fixed in advance. Those are different problems, and only one of them was actually being asked here.</p>

<h2 id="what-actually-worked-stop-generating-start-classifying">What actually worked: stop generating, start classifying</h2>

<p>The reframe that ended the JSON detour came from, of all places, a conversation with Grok - asked to sanity-check the whole “small model outputs structured facets” problem from scratch, it was the one that pushed back on generation entirely and suggested going the classification-head route instead of trying yet another output grammar. Worth crediting plainly, because the four failed attempts above all shared the same unexamined assumption - “the model needs to produce the structure as text” - and it took an outside prompt to actually question that assumption rather than iterate on top of it.</p>

<p>The reframe: don’t ask a model to write down a genre name character by character and hope it spells “Sci-Fi” correctly. Ask it “is Sci-Fi one of the genres in this query, yes or no” - for every genre the catalog has, all at once, in a single forward pass. That’s a classification problem, and classification problems don’t have a parsing step, because the output is a fixed vector of numbers, not text.</p>

<p>The architecture is one shared encoder - BERT-family, reads the query once, produces one <code class="language-plaintext highlighter-rouge">[CLS]</code> vector - with a small linear layer, a <strong>head</strong>, hanging off that same vector for each slot the schema needs.</p>

<p>One detail worth being precise about: this is a genuine fine-tune, not a frozen-encoder linear probe. The training loop hands DistilBERT’s own weights to the optimizer alongside the heads (<code class="language-plaintext highlighter-rouge">AdamW(model.parameters(), ...)</code>, no <code class="language-plaintext highlighter-rouge">requires_grad = False</code> anywhere), so gradients flow back through the whole 66M-parameter encoder on every step, not just through the small classification layers bolted on top of fixed embeddings. That’s a heavier training job than a linear probe, but it’s also why a much smaller encoder like BERT-tiny genuinely can’t be patched around later: the representations themselves are shaped by training, not just the heads reading them.</p>

<pre><code class="language-mermaid">flowchart TB
    Q["\"dark psychological thriller\nfrom the 80s under 2 hours\""] --&gt; ENC["shared encoder\n(DistilBERT, one CLS vector)"]
    ENC --&gt; G["genre head\n27-way multi-label"]
    ENC --&gt; D["date head\nhas_date + year range"]
    ENC --&gt; RT["runtime head\nhas_runtime + max minutes"]
    ENC --&gt; RA["rating head\nhas_rating + min rating"]
    ENC --&gt; P["popularity head\nnone / niche / mainstream"]
    ENC --&gt; M["title / mood / other\nbinary presence"]
</code></pre>

<p>Every head reads the same 768-dim CLS vector; nothing is generated token by token, so there’s no parser and no malformed-output failure mode - the output is always the same seven keys, always the same types, whether the query is empty, garbled, or perfectly formed. That validity guarantee comes directly from the architecture, not from any extra checking layered on top: a linear layer followed by a sigmoid or softmax cannot emit <code class="language-plaintext highlighter-rouge">{</code> mistranslated as <code class="language-plaintext highlighter-rouge">&lt;unk&gt;</code>, because it never emits characters at all.</p>

<p>The genre head is multi-label (<code class="language-plaintext highlighter-rouge">27</code> catalog genres, sigmoid per class, since a movie and a query can both carry more than one genre at once), with a <code class="language-plaintext highlighter-rouge">pos_weight</code> term capped at <code class="language-plaintext highlighter-rouge">20x</code> so that a plausible catch-all genre like Drama doesn’t dominate every prediction just because it’s overrepresented in the training data. With 27 unevenly-sized classes, an unweighted loss will happily learn “always predict Drama” and call that a local minimum.</p>

<p>Date, runtime, and rating are two-part heads: a “does this slot exist at all” binary flag, plus a regression value that’s masked out of the loss entirely for the majority of examples where the slot doesn’t apply, so the model isn’t penalized for a runtime guess on a query that never mentioned runtime.</p>

<p>Popularity is a plain three-way softmax (none / niche / mainstream). Title, mood, and “other”, meaning leftover tokens like “1080p” or “download” that belong to neither structured facets nor vibe search, are simple yes/no presence flags. The actual text for these is handled downstream by catalog full-text search and format-token matching, not by the encoder.</p>

<h2 id="a-second-dead-end-one-step-earlier-generating-the-training-data-itself">A second dead end, one step earlier: generating the training data itself</h2>

<p>Before any of the four generation attempts above got to a training run, there was a smaller, earlier problem: where do 4,200 labeled queries even come from. Nobody has a public dataset of “search box text mapped to genre/date/runtime facets” lying around, so the training data had to be generated, and the first approach to generating it hit its own version of the same wall the T5 experiments hit later - it looked fine up close and broke down at scale.</p>

<p><strong>First attempt: free-floating persona prompts.</strong> Describe a persona (“a Grazer looking for a mood-based comedy”) and ask an LLM (Gemma, then a larger model) to write plausible search queries for that persona, over and over, to build up volume. This works fine for the first few dozen examples. Past a few hundred, it stops working in a specific, boring way: the model runs out of genuinely different things to say about the same abstract persona and starts recycling phrasing - not exact copies, but near-duplicates close enough that they teach the classifier nothing it didn’t already learn from the first one. A hundred rephrasings of “something funny and lighthearted” is a hundred rows in the file and one effective training example.</p>

<p><strong>The fix, and it’s the same fix as the vibe-search half of this whole post: point an embedding layer at the problem of “how similar are these two things.”</strong> Every newly generated query gets embedded with the same EmbeddingGemma checkpoint already doing retrieval elsewhere in the pipeline, deliberately reusing it rather than standing up a second embedding setup just for data QA.</p>

<p>Each new query is then checked for near-duplicates against the existing pool: scoped first by persona and genre (a mood query and a structured-facet query are never meaningfully “similar” to each other regardless of embedding distance), and only then compared by cosine similarity within that narrowed set, rather than a brute-force compare against the entire accumulated pool as it grows into the thousands. Past a threshold, a candidate gets flagged as a near-duplicate and either dropped or used as a signal that the generator needs steering somewhere else.</p>

<p>That “somewhere else” needed its own answer, because “reject duplicates” alone just produces fewer examples, not more diverse ones. The approach borrowed from <a href="https://resources.nvidia.com/en-us-financial-services-industry/synthetic-data-generation-for-financial-ai-research-with-nvidia-nemo">NVIDIA’s writeup on synthetic data generation with NeMo Curator</a>, a case study on generating half a million unique financial news headlines that hit the identical wall for the identical reason: their naive first batch of <code class="language-plaintext highlighter-rouge">50,000</code> headlines came back <code class="language-plaintext highlighter-rouge">65%</code> near-duplicates, because a single large generation run against a fixed set of few-shot examples just repeats the same handful of phrasings at scale, no matter how big the batch is.</p>

<p>Their fix is an iterative loop rather than one large run:</p>

<ol>
  <li>Generate a batch.</li>
  <li>Deduplicate it against the <em>entire accumulated corpus so far</em> (not just the new batch), using embeddings and k-means clustering.</li>
  <li>Pull few-shot examples for the next round from whichever generated headline sits farthest from its own cluster’s centroid, the most atypical phrasing in that cluster, not the generic center-of-mass one.</li>
  <li>Reject any of those candidate examples that are too similar to one already used in a previous round, so the steering set itself doesn’t go stale.</li>
</ol>

<p>Their own numbers make the shape of the problem concrete: yield dropped from about <code class="language-plaintext highlighter-rouge">35%</code> net-new headlines per batch early on to about <code class="language-plaintext highlighter-rouge">15%</code> by the time the corpus passed <code class="language-plaintext highlighter-rouge">100,000</code>, because every new headline has to be distinct from an ever-larger accumulated pool, not just from the current batch. That’s the same mechanism applied here, just clustering within a persona’s query pool instead of within a financial-headline corpus.</p>

<p><strong>A more predictable step past that: stop asking for queries about an abstract persona, and ground each one in real catalog data instead.</strong> Pick two or three real movies that share a genre, show the model their actual titles, overviews, and ratings, and ask it to write a query a person might type looking for something like those. The title itself is deliberately never shown, so the generated query reflects real plot and tone signal rather than word-association off a title the model already recognizes. A handful of real, specific movies is inherently more varied input than one abstract sentence describing a persona, giving the model something concrete to react to instead of an empty room to free-associate in.</p>

<p>For the facets that don’t need any of this, genre, date, rating, popularity, title, all things a real catalog row already has ground truth for, a separate, fully deterministic script builds queries straight from string templates filled with real catalog values. No model call, no dedup step, because the label is correct by construction rather than something that has to be checked.</p>

<p>The shape of the lesson repeats from the facet-classification story, just one layer earlier in the pipeline: the expensive-looking, more “AI-native” approach (open-ended generation from an abstract prompt) quietly degraded in a way that only showed up at scale, and a cheap, boring measurement step, embedding similarity, is what fixed it, applied on top of the thing that was already unreliable on its own.</p>

<p>Training ran on roughly <code class="language-plaintext highlighter-rouge">4,200</code> labeled queries (hand-seeded examples plus scaled-up synthetic templates) on <a href="https://modal.com">Modal</a>, a serverless GPU platform: an <code class="language-plaintext highlighter-rouge">L4</code> instance for a few dollars an hour, spun up on demand and torn down when the run finishes, rather than provisioning and babysitting a persistent box for something that trains in minutes.</p>

<p>Three model sizes went through the same training script on that L4 before settling on one: <code class="language-plaintext highlighter-rouge">BERT-tiny</code> (<code class="language-plaintext highlighter-rouge">4.4M</code> parameters, 2 layers, 128 hidden units) first, as the cheapest possible thing to test the pipeline against; a mid-size BERT variant next, to see whether something between “tiny” and “full DistilBERT” would close the gap; and <code class="language-plaintext highlighter-rouge">DistilBERT</code> (<code class="language-plaintext highlighter-rouge">66M</code> parameters, the standard distillation of BERT-base) last, 8 epochs, which is what actually closed it.</p>

<p>The training script and the quantization step both live in <code class="language-plaintext highlighter-rouge">ml/</code>, so the same command that trains a checkpoint on the L4 also runs it back through <code class="language-plaintext highlighter-rouge">torchao</code>’s <code class="language-plaintext highlighter-rouge">Int8WeightOnlyConfig</code> afterward. The entire round trip, from labeled queries to an int8 checkpoint ready to export, is one pipeline rather than a training step and a separate manual compression step bolted on later.</p>

<table>
  <thead>
    <tr>
      <th>Metric</th>
      <th>DistilBERT</th>
      <th>BERT-tiny</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>genre F1</td>
      <td><strong>0.85</strong></td>
      <td>0.38</td>
    </tr>
    <tr>
      <td>popularity accuracy</td>
      <td>0.93</td>
      <td>0.91</td>
    </tr>
    <tr>
      <td>has_date F1</td>
      <td>0.99</td>
      <td>0.98</td>
    </tr>
    <tr>
      <td>has_runtime F1</td>
      <td>0.95</td>
      <td>0.92</td>
    </tr>
    <tr>
      <td>has_rating F1</td>
      <td>0.91</td>
      <td>0.86</td>
    </tr>
    <tr>
      <td>has_mood F1</td>
      <td>0.94</td>
      <td>0.89</td>
    </tr>
    <tr>
      <td>date error (years)</td>
      <td>~5.5</td>
      <td>~7</td>
    </tr>
    <tr>
      <td>runtime error (minutes)</td>
      <td>~14</td>
      <td>~16</td>
    </tr>
  </tbody>
</table>

<p>The table only shows the two ends of the size range, but the mid-size BERT variant tried in between is what made the comparison worth trusting rather than assuming: it landed closer to BERT-tiny’s genre collapse than to DistilBERT’s 0.85, confirming this wasn’t a smooth “bigger is linearly better” curve where any mid-point model would do - genre F1 held basically flat until DistilBERT’s extra depth and hidden size, then jumped. That’s the actual reason three sizes went through the same training run on Modal instead of stopping at two: a two-point comparison can’t tell you whether the gap is a cliff or a slope, and here it was a cliff.</p>

<p>The binary presence flags - “is there a date in this query at all” - are close between the two extremes shown, both comfortably above 0.9 F1. The gap is entirely in genre: 0.85 versus 0.38. A BERT-tiny genre head effectively collapses under that gap, either over-firing on common genres or missing rarer ones, because 27-way multi-label classification with meaningful class imbalance needs more representational capacity than 4.4M parameters and two transformer layers give you. DistilBERT shipped as the quality model on the strength of that one number.</p>

<p>A few honest weak spots came out of this, worth stating plainly rather than rounding off:</p>

<ul>
  <li><strong>Decades land approximately, not exactly</strong> - “80s” resolves to something centered near the 80s but with roughly 5 years of slop, because the regression head is learning a continuous year value from a discrete decade phrase. Explicit numeric years (“2012”) and regex-extractable decade patterns are handled by rules ahead of the model, with the head as a fallback rather than the primary path for date.</li>
  <li><strong>Genre bleed</strong> - the model occasionally adds an extra genre, usually Drama, and rarer genres are still weaker than common ones, which tracks with training-data frequency more than anything architectural.</li>
  <li><strong>Title extraction is the weakest head</strong> (F1 ~0.75) - titles make up only about 7% of the training data, and title-in-query is exactly the kind of thing that catalog full-text search already does well, so the model doesn’t need to carry that weight alone.</li>
  <li><strong>Mood is presence-only</strong> - the head answers “does this query have mood language,” not “which mood.” The actual mood text still flows to EmbeddingGemma as residual, which is the right split anyway: mood is a similarity problem, not a classification problem, per the earlier section.</li>
</ul>

<p>A follow-up test suite (38 tests spanning every head, including excluded-genre phrasing like “not a horror movie” and negation handling) locked in the decode behavior once the training data itself started covering those cases, so a future retrain can’t silently regress a head that used to work.</p>

<h2 id="winning-the-benchmark-doesnt-mean-it-fits-on-the-phone">Winning the benchmark doesn’t mean it fits on the phone</h2>

<p>DistilBERT’s checkpoint, saved as a plain PyTorch state dict straight off the Modal L4, is about <code class="language-plaintext highlighter-rouge">253MB</code> in fp32. That number matters a lot more here than it would for a server-side model, because it has to sit on a phone next to EmbeddingGemma, which is already budgeted at under <code class="language-plaintext highlighter-rouge">500MB</code> for the whole on-device model story.</p>

<p>This is the actual constraint that shaped every decision in this section, stated plainly:</p>

<blockquote>
  <p>Two models, one phone, one storage and RAM budget shared with everything else the app does.</p>
</blockquote>

<p>There’s no server to offload either model to, so whatever ships has to fit on a mid-range Android device alongside posters, a local catalog database, and the rest of the app, not just fit inside some abstract “model size” line item.</p>

<p>fp32 at 253MB doesn’t fit that budget comfortably next to EmbeddingGemma, so quantization decides whether this ships at all, not just how tidy the build is. The pipeline runs it as the last step of the same Modal job that does training: <code class="language-plaintext highlighter-rouge">torchao</code>’s <code class="language-plaintext highlighter-rouge">Int8WeightOnlyConfig</code> applied to the trained checkpoint, weights collapsed from float32 to int8, before the result gets pulled back down from the L4 instance.</p>

<p>int8 weight-only quantization typically lands a model like this in the <code class="language-plaintext highlighter-rouge">60-70MB</code> range, the same ballpark as an earlier int8 plan that had been considered for the T5 experiments, before those got dropped for the tokenizer and generation problems described above. A second ~60-70MB encoder alongside EmbeddingGemma is a reasonable footprint on a phone; a 250MB unquantized PyTorch checkpoint is not, and the fp32 checkpoint never had a real path to shipping on its own.</p>

<p>BERT-tiny, for reference, quantizes down to <code class="language-plaintext highlighter-rouge">5-8MB</code>, genuinely tiny, but as the size comparison above shows, it isn’t shipping for genre until either more training data or distillation from the DistilBERT checkpoint closes that <code class="language-plaintext highlighter-rouge">0.85</code>-vs-<code class="language-plaintext highlighter-rouge">0.38</code> gap.</p>

<h2 id="putting-the-pieces-back-together">Putting the pieces back together</h2>

<p>There are actually three retrieval mechanisms running here, not two, and none of them runs alone at query time - a query gets split, and the pieces feed three different signals that only get reconciled at the very end:</p>

<pre><code class="language-mermaid">flowchart TB
    Q["free-text query"] --&gt; RULES["deterministic rules first\n(explicit years, decade regex)"]
    RULES --&gt; FACETS["DistilBERT facet heads\ngenre / date / runtime / rating / popularity"]
    FACETS --&gt; HC{"high\nconfidence?"}
    HC --&gt;|"yes"| HARD["hard filter\n(exclude non-matches)"]
    HC --&gt;|"no / mood-only"| SOFT["soft boost, never exclude"]
    FACETS --&gt; RESID["residual text\n(mood + leftover words)"]
    RESID --&gt; EMBED["EmbeddingGemma\nvector similarity"]
    Q --&gt; FTS["FTS4 keyword match\n+ spellfix1 typo fallback"]
    HARD --&gt; RANK["combine\n(reciprocal rank fusion)"]
    SOFT --&gt; RANK
    EMBED --&gt; RANK
    FTS --&gt; RANK
    RANK --&gt; RESULTS["ranked results"]
</code></pre>

<p>The third path, running independently of both models, is a plain SQLite <code class="language-plaintext highlighter-rouge">FTS4</code> virtual table over title, overview, and original-title fields, doing ordinary keyword and prefix matching, the same kind of search that’s existed in relational databases for decades, no model involved. It’s there because embeddings and a facet classifier both answer “what does this query <em>mean</em>,” and neither one is the right tool for “the user typed a title with one letter wrong.”</p>

<p>FTS4’s prefix match handles the common case. When that misses entirely, a <code class="language-plaintext highlighter-rouge">spellfix1</code>-backed fallback tries typo-tolerant suggestions per token before giving up, so “the dark knigt” still finds The Dark Knight without needing either model to have ever seen that misspelling. Titles saved into the library before this indexing existed get backfilled once, in the background, in batches, rather than needing a full re-scan on every app launch.</p>

<p>All three signals, the facet-filtered/boosted set, the embedding-similarity set, and the FTS4 keyword set, get combined with <strong>reciprocal rank fusion</strong> (<code class="language-plaintext highlighter-rouge">RRF</code>): each item’s final score is the sum of <code class="language-plaintext highlighter-rouge">1 / (k + rank)</code> across whichever of the three lists it appears in, so an item that shows up near the top of two different signals outranks an item that’s merely the single best match on one.</p>

<p>That’s a deliberately simple combination rule, chosen over trying to hand-tune relative weights between three signals with very different score distributions. A BM25-ish FTS score, a cosine similarity, and a facet-match boolean don’t live on comparable scales, but rank position always does.</p>

<p>The rule that governs the facet side of this diagram is one sentence:</p>

<blockquote>
  <p>A high-confidence structured facet becomes a hard filter. Everything else is a soft signal that can boost a result but never silently drops one.</p>
</blockquote>

<p>That asymmetry is deliberate, not an oversight, and it’s the direct fix for the “nostalgic gets classified as a date” failure mode from earlier.</p>

<p>A genuinely ambiguous signal degrading to “rank slightly higher” is recoverable if it’s wrong. A genuinely ambiguous signal degrading to “excluded from results entirely” is not, and the user has no way to tell the difference between “nothing matched” and “the wrong thing got filtered out before it had a chance to match.”</p>

<p>That’s also why explicit numeric years and decade phrases go through regex first, ahead of the DistilBERT date head - a rule either matches “2012” exactly or it doesn’t, with zero ambiguity, while the head’s ~5-year slop is a reasonable fallback for phrasing a regex can’t anticipate, not a reasonable primary path for a case regex handles perfectly.</p>

<h2 id="what-mofys-search-can-actually-answer-today">What Mofy’s search can actually answer today</h2>

<p>Concretely, this is the range of query a single search box handles now, resolved through the split above rather than through separate search modes the user has to pick between:</p>

<ul>
  <li><strong>Structured, single facet</strong>: “action movies”, “movies from the 90s”, “highly rated”, “under 90 minutes” - a single head fires, the rest of the pipeline is a no-op.</li>
  <li><strong>Structured, compound</strong>: “dark psychological thriller from the 80s under 2 hours” - genre, date, and runtime heads all fire in the same forward pass, combined as hard filters.</li>
  <li><strong>Pure vibe</strong>: “feeling nostalgic”, “something to watch when I don’t want to think” - no facet head fires with confidence, the whole query flows to EmbeddingGemma as residual text.</li>
  <li><strong>Plot description with no title given</strong>: “a guy loses his memory every day and writes notes to himself” - resolves via embedding similarity to Memento’s stored overview vector, with no genre or date filtering involved at all.</li>
  <li><strong>Mixed structured and vibe in one sentence</strong>: “something dark from the 2000s” - date resolves as a hard range filter, “dark” survives as residual text into the semantic search, and the two signals combine at ranking time rather than one silently overriding the other.</li>
  <li><strong>Format/logistics queries</strong>: “download inception 2010 1080p” - title and date heads fire, “1080p” and “download” are caught by the “other” presence flag and handled by catalog matching rather than the encoder, since that’s a lookup problem, not a language-understanding one.</li>
</ul>

<p>Two things it deliberately still skips:</p>

<ul>
  <li>It folds all mood language into one similarity search, rather than resolving an exact mood vocabulary (“cozy” vs. “melancholic” as distinct, structured categories).</li>
  <li>It never runs a generative model on-device to paraphrase or expand a query.</li>
</ul>

<p>Both are staged as later, optional work: an on-device small language model for richer multi-aspect query decomposition, and a ColBERT-style multi-vector upgrade if single-vector retrieval ever turns out to be an actual bottleneck rather than a hypothetical one. Deferred specifically because the three-signal split above already covers the real query traffic without needing either.</p>

<h2 id="the-actual-lesson">The actual lesson</h2>

<p>None of this needed a frontier model. It needed the query correctly split into the things it actually is - a small number of exact facts, a fuzzy remainder, and plain keyword lookup for when the user already knows the title - and then a genuinely boring approach applied to each: a 66M-parameter encoder doing seven small classification jobs at once, a sub-300M embedding model doing nearest-neighbor lookup, and a SQLite feature that’s existed since before either model was trained.</p>

<p>The expensive, interesting-looking approach, generate structured JSON with a small LLM, failed four different ways before a classification reframe fixed it, and every one of those failures looked, from the training loss alone, like it was working. What actually mattered was what came out the other end of a real parser, on a real device, with the network off, and that only shows up if you go check.</p>]]></content><author><name>ikouchiha47</name></author><summary type="html"><![CDATA[Mofy is a personal, offline-first movie app with no cloud API calls. Making search understand human-like queries meant ruling out generative models after four failed attempts, and fine-tuning a 66M-parameter DistilBERT encoder instead, small enough to ship on-device.]]></summary></entry><entry><title type="html">A Quick rundown on concurrency and gc</title><link href="https://ikouchiha47.github.io/2026/08/12/concurrency-and-gc.html" rel="alternate" type="text/html" title="A Quick rundown on concurrency and gc" /><published>2026-08-12T00:00:00+00:00</published><updated>2026-08-12T00:00:00+00:00</updated><id>https://ikouchiha47.github.io/2026/08/12/concurrency-and-gc</id><content type="html" xml:base="https://ikouchiha47.github.io/2026/08/12/concurrency-and-gc.html"><![CDATA[<p>Related: 001_usage_of_signals_in_language_runtime.md, 006_other_reading_materials.md, 007_concurrency_comparison.md</p>

<h2 id="abstract">Abstract</h2>

<p>Three languages — Go, Kotlin, and Erlang/Elixir (running on BEAM) — solve the
same problem (run many logical tasks on few OS threads) with three different
answers to one question: <strong>who controls the switch between tasks, and what
does that controller need to know to do it safely?</strong></p>

<p>The answer to that question determines everything downstream: whether the
model is cooperative or preemptive, whether GC pauses one thread or the whole
process, and whether a crash is contained or catastrophic.</p>

<p>This document derives each model from its constraints rather than describing
it as a list of features. Each section ends with a checkpoint question you
should be able to answer before moving to the next section.</p>

<hr />

<h2 id="background-the-problem-all-three-are-solving">Background: the problem all three are solving</h2>

<p>A CPU core runs one instruction stream at a time. OS threads are the
kernel’s abstraction for time-slicing a core across many instruction
streams, but they are expensive:</p>

<ul>
  <li>~8MB stack per thread (Linux default)</li>
  <li>A context switch saves/restores the full register file and disturbs the
cache and TLB</li>
  <li>10,000 OS threads means gigabytes of stack space before any work is done</li>
</ul>

<p>So every runtime that wants cheap concurrency builds an <strong>M:N scheduler</strong>:
M logical tasks multiplexed onto N OS threads (typically N ≈ number of
cores). The three systems below are three different M:N schedulers, and
they differ because they made different decisions about who owns the
switching logic.</p>

<p><strong>Checkpoint:</strong> before continuing, state in one sentence why an OS thread is
too expensive to use one-per-logical-task at scale.</p>

<hr />

<h2 id="part-1--go">Part 1 — Go</h2>

<p>Sources read directly for this section (not paraphrased from memory):
<code class="language-plaintext highlighter-rouge">src/runtime/preempt.go</code>, <code class="language-plaintext highlighter-rouge">src/runtime/signal_unix.go</code>, <code class="language-plaintext highlighter-rouge">src/runtime/proc.go</code>,
<code class="language-plaintext highlighter-rouge">src/runtime/mgc.go</code> — golang/go, master branch, fetched from
raw.githubusercontent.com.</p>

<h3 id="11-key-concept-this-is-csp-not-fork-join">1.1 Key concept: this is CSP, not fork-join</h3>

<p>Go’s concurrency model is explicitly an implementation of Hoare’s
<strong>Communicating Sequential Processes (CSP, 1978)</strong> — independent sequential
processes that interact <em>only</em> through message passing over channels, not
shared mutable state accessed via locks. This is a design lineage, stated
directly in Go’s own materials: “Don’t communicate by sharing memory;
share memory by communicating.”</p>

<p><strong>Where CSP itself came from.</strong> Tony Hoare published “Communicating
Sequential Processes” in <em>Communications of the ACM</em>, 1978. The problem he
was working on wasn’t concurrency in the modern web-service sense — it was
correctness of concurrent programs at a time when shared-variable
concurrency (semaphores, monitors) was the dominant model and was proving
extremely hard to reason about formally: with shared mutable state, the
number of possible interleavings of two processes explodes, and proving a
program correct meant proving it correct under <em>all</em> of them.</p>

<p>Hoare’s move was to make the <em>only</em> interaction between processes an
explicit, synchronous, named event — a process names who it’s sending
to/receiving from, and the send/receive pair is the entire synchronization
primitive, with no separate lock needed. This has a real mathematical
payoff: because processes share nothing, you can reason about each process
in isolation and about the communication events between them as a
separate, much smaller problem — closer to algebra than to exhaustive
case analysis of shared-memory interleavings. CSP was formalized further
into a full process algebra in Hoare’s own later work and independently
alongside Robin Milner’s CCS (Calculus of Communicating Systems, also late
1970s) — the two are usually cited together as the origin of process
algebras generally.</p>

<p>Go’s designers (Rob Pike, in particular, who had earlier worked on
Newsqueak and Alef — direct experimental predecessors that already used
CSP-style channels) took the <em>communication primitive</em> from CSP —
synchronous, named-channel message passing — without adopting Hoare’s full
formal process algebra or his original synchronous-only restriction (Go’s
buffered channels allow asynchronous sends up to the buffer size, which
Hoare’s original calculus didn’t have). So “Go implements CSP” is accurate
at the level of the core idea — channels as the unit of synchronization,
not locks — and imprecise if taken to mean Go implements the full formal
calculus.</p>

<p>This matters because it’s easy to conflate with two other models that are
<em>not</em> what Go does:</p>

<ul>
  <li><strong>Fork-join</strong> (Java’s <code class="language-plaintext highlighter-rouge">ForkJoinPool</code>, Cilk, OpenMP): a task explicitly
splits into subtasks, waits for all of them, then joins. The parallelism
is structured around a single computation’s divide-and-conquer shape.
Go has nothing built into the language for this — you’d hand-roll it
with a <code class="language-plaintext highlighter-rouge">sync.WaitGroup</code>. Goroutines are not spawned with an implicit
join; <code class="language-plaintext highlighter-rouge">go f()</code> returns immediately and nothing waits for it unless you
add that synchronization yourself.</li>
  <li><strong>Shared-memory threading with locks</strong> (raw pthreads, Java <code class="language-plaintext highlighter-rouge">synchronized</code>):
the default coordination primitive is a shared address space guarded by
mutual exclusion. Go supports this too (<code class="language-plaintext highlighter-rouge">sync.Mutex</code> exists and is used
heavily inside the runtime itself), but it’s not the idiomatic surface
the language pushes you toward.</li>
</ul>

<p>Channels (<code class="language-plaintext highlighter-rouge">chan</code>) are the CSP primitive: a goroutine sends a value into a
channel, another receives it, and the transfer itself is the
synchronization point — no separate lock is needed for that handoff.</p>

<p><strong>Checkpoint:</strong> if goroutines don’t implicitly join, what actually
guarantees a <code class="language-plaintext highlighter-rouge">go f()</code> call’s side effects are visible before <code class="language-plaintext highlighter-rouge">main()</code>
returns? (Answer: nothing, by default — this is why programs that don’t
explicitly wait via a channel or <code class="language-plaintext highlighter-rouge">WaitGroup</code> can exit before spawned
goroutines finish; it’s a common bug source, not a language guarantee.)</p>

<h3 id="12-the-gmp-scheduler">1.2 The GMP scheduler</h3>

<p>From <code class="language-plaintext highlighter-rouge">proc.go</code>’s own top-of-file doc comment (line ~24 onward):</p>

<blockquote>
  <p>The scheduler’s job is to distribute ready-to-run goroutines over worker
threads.
G - goroutine.
M - worker thread, or machine.
P - processor, a resource that is required to execute Go code. M must
have an associated P to execute Go code, however it can be blocked or in
a syscall w/o an associated P.
Design doc at https://golang.org/s/go11sched.</p>
</blockquote>

<p>This is the go11sched design (Dmitry Vyukov, 2012) — the M:N:P model exists
specifically to solve the problem of per-P local run queues: without P as a
separate scheduling resource, every M contending for work would need to hit
a global queue, which doesn’t scale past a few cores. Each P owns a local
run queue; an M must acquire a P to run Go code at all, which is why a
goroutine blocked in a syscall releases its P for another M to pick up
(<code class="language-plaintext highlighter-rouge">handoffp</code> in <code class="language-plaintext highlighter-rouge">proc.go</code>) rather than leaving a core idle.</p>

<h3 id="13-preemption-how-it-evolved-and-why-gc-is-the-reason-it-changed-at-all">1.3 Preemption: how it evolved, and why GC is the reason it changed at all</h3>

<p><strong>Go ≤1.10 — cooperative, at function prologues.</strong> Every function call
checked a “please yield” flag (implemented by poisoning the stack-bound
check, per <code class="language-plaintext highlighter-rouge">preempt.go</code>’s header comment: “Synchronous safe-points are
implemented by overloading the stack bound check in function prologues”).
A goroutine with no function calls in its loop body (<code class="language-plaintext highlighter-rouge">for {}</code>) never
triggered that check and could never be preempted this way.</p>

<p><strong>Why this is a GC problem, specifically, and not just a scheduling
fairness problem:</strong> <code class="language-plaintext highlighter-rouge">mgc.go</code> calls <code class="language-plaintext highlighter-rouge">stopTheWorldWithSema</code> at both mark
termination and sweep termination (<code class="language-plaintext highlighter-rouge">gcMarkTermination</code>, <code class="language-plaintext highlighter-rouge">gcStart</code> —
<code class="language-plaintext highlighter-rouge">mgc.go</code> lines ~835, ~1066 in the current source). STW literally means
every goroutine must reach a state the GC considers safe before the GC
phase can proceed — the mark/sweep transition cannot begin with even one
goroutine still running arbitrary code, because the GC needs a globally
consistent view of what’s reachable. A goroutine stuck in a tight loop
with no function calls blocked this indefinitely. This was the actual,
measured production problem that motivated the 2019 proposal
(golang/proposal <code class="language-plaintext highlighter-rouge">24543-non-cooperative-preemption.md</code>) — not a general
desire for “fair” scheduling.</p>

<p><strong>Go 1.14 — non-cooperative, signal-based.</strong> <code class="language-plaintext highlighter-rouge">preempt.go</code>’s own comment
distinguishes three safe-point categories precisely:</p>

<ol>
  <li><strong>Blocked safe-points</strong> — a goroutine descheduled, blocked on sync, or
in a syscall. Cheap: the runtime already has full knowledge of its stack.</li>
  <li><strong>Synchronous safe-points</strong> — a running goroutine voluntarily checks for
a pending preemption request (the stack-bound-check trick above).</li>
  <li><strong>Asynchronous safe-points</strong> — <em>any</em> instruction in user code where a
conservative stack/register scan can still find all roots. The runtime
can stop a goroutine here using a signal, without the goroutine’s
cooperation.</li>
</ol>

<p><code class="language-plaintext highlighter-rouge">signal_unix.go</code> (lines 44–74) states the SIGURG rationale directly, as
four numbered criteria the signal had to satisfy:</p>

<ol>
  <li>Must be a signal debuggers pass through by default (on Linux: <code class="language-plaintext highlighter-rouge">SIGALRM</code>,
<code class="language-plaintext highlighter-rouge">SIGURG</code>, <code class="language-plaintext highlighter-rouge">SIGCHLD</code>, <code class="language-plaintext highlighter-rouge">SIGIO</code>, <code class="language-plaintext highlighter-rouge">SIGVTALRM</code>, <code class="language-plaintext highlighter-rouge">SIGPROF</code>, <code class="language-plaintext highlighter-rouge">SIGWINCH</code>).</li>
  <li>Must not be claimed internally by libc in mixed Go/C binaries (rules out
<code class="language-plaintext highlighter-rouge">SIGCANCEL</code>, <code class="language-plaintext highlighter-rouge">SIGSETXID</code>).</li>
  <li>Must be safe to receive spuriously — ruling out <code class="language-plaintext highlighter-rouge">SIGALRM</code> (ambiguous
cause) and <code class="language-plaintext highlighter-rouge">SIGUSR1</code>/<code class="language-plaintext highlighter-rouge">SIGUSR2</code> (commonly used by applications for real
things).</li>
  <li>Must exist on platforms without real-time signals (rules out macOS’s
missing RT signal range).</li>
</ol>

<p><code class="language-plaintext highlighter-rouge">SIGURG</code> won because out-of-band TCP data is essentially unused in
practice, the signal doesn’t even report which socket triggered it (making
it nearly useless for its literal purpose), and any correctly-written
application already has to tolerate a spurious <code class="language-plaintext highlighter-rouge">SIGURG</code>.</p>

<p>The mechanism, traced through the actual call path:
<code class="language-plaintext highlighter-rouge">suspendG</code> (<code class="language-plaintext highlighter-rouge">preempt.go</code>) drives a goroutine toward suspension — if it’s
<code class="language-plaintext highlighter-rouge">_Grunning</code>, it sets <code class="language-plaintext highlighter-rouge">gp.preemptStop</code>/<code class="language-plaintext highlighter-rouge">gp.preempt</code> and calls <code class="language-plaintext highlighter-rouge">preemptM</code>,
which calls <code class="language-plaintext highlighter-rouge">signalM(mp, sigPreempt)</code> (<code class="language-plaintext highlighter-rouge">signal_unix.go</code> line ~386) to
deliver <code class="language-plaintext highlighter-rouge">SIGURG</code> to that specific OS thread. The signal handler
(<code class="language-plaintext highlighter-rouge">doSigPreempt</code>, <code class="language-plaintext highlighter-rouge">signal_unix.go</code> line ~342) inspects the interrupted PC
and only acts if it lands on a recognized async safe-point; if not, it
leaves the goroutine to continue and retries. Once accepted, the handler
rewrites the signal context so execution resumes at <code class="language-plaintext highlighter-rouge">asyncPreempt</code>, which
spills all potentially-pointer-holding registers to the stack before
handing off to the scheduler — this is what makes the stack scannable by
the GC afterward.</p>

<p>An intermediate fix — inserting checks at loop back-edges so tight loops
without function calls would still hit synchronous safe-points — was
measured and <strong>rejected</strong>: 7.8% throughput regression, considered too
expensive to pay unconditionally across all Go programs just to close this
gap. Signal-based async preemption was strictly cheaper because it costs
nothing until actually invoked.</p>

<p><strong>Tracing it through a concrete example:</strong></p>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">func</span> <span class="n">main</span><span class="p">()</span> <span class="p">{</span>
    <span class="k">go</span> <span class="k">func</span><span class="p">()</span> <span class="p">{</span>
        <span class="k">for</span> <span class="p">{}</span> <span class="c">// spin forever, no function calls</span>
    <span class="p">}()</span>
    <span class="n">time</span><span class="o">.</span><span class="n">Sleep</span><span class="p">(</span><span class="n">time</span><span class="o">.</span><span class="n">Second</span><span class="p">)</span>
    <span class="n">runtime</span><span class="o">.</span><span class="n">GC</span><span class="p">()</span>
<span class="p">}</span>
</code></pre></div></div>

<p>On Go ≤1.10, <code class="language-plaintext highlighter-rouge">runtime.GC()</code> here would never return. <code class="language-plaintext highlighter-rouge">runtime.GC()</code> forces
a stop-the-world GC cycle, which calls <code class="language-plaintext highlighter-rouge">stopTheWorldWithSema</code> — every
goroutine, including the spinning one, must reach a safe-point first. The
spinning goroutine calls no functions, so it never hits the stack-bound
check the old synchronous mechanism relied on. It spins forever; the GC
waits forever; the program hangs.</p>

<p>On Go ≥1.14, the same program’s GC call succeeds. <code class="language-plaintext highlighter-rouge">stopTheWorldWithSema</code>
calls <code class="language-plaintext highlighter-rouge">suspendG</code> on the spinning goroutine; since it’s <code class="language-plaintext highlighter-rouge">_Grunning</code> with no
pending suspend, <code class="language-plaintext highlighter-rouge">suspendG</code> calls <code class="language-plaintext highlighter-rouge">preemptM</code>, which sends <code class="language-plaintext highlighter-rouge">SIGURG</code> to the
OS thread running it. The signal interrupts the <code class="language-plaintext highlighter-rouge">for {}</code> loop mid-flight —
at literally any instruction, since the compiler’s stack maps cover every
async safe-point in the loop body, even one with zero function calls. The
signal handler rewrites execution to resume at <code class="language-plaintext highlighter-rouge">asyncPreempt</code>, which
spills registers to the stack, and the goroutine parks. GC proceeds. This
exact scenario — an infinite loop with no calls — is the textbook case the
2019 proposal was written to fix.</p>

<h3 id="13a-the-actual-gc-algorithm-tricolor-mark-sweep-with-a-hybrid-write-barrier">1.3a The actual GC algorithm: tricolor mark-sweep with a hybrid write barrier</h3>

<p>Everything above (1.3) covers <em>when</em> goroutines get stopped for GC. This
subsection covers what the GC is actually doing — read directly from
<a href="https://github.com/golang/go/blob/master/src/runtime/mgc.go"><code class="language-plaintext highlighter-rouge">src/runtime/mgc.go</code></a>
(top-of-file doc comment) and
<a href="https://github.com/golang/go/blob/master/src/runtime/mbarrier.go"><code class="language-plaintext highlighter-rouge">src/runtime/mbarrier.go</code></a>
(top-of-file doc comment), not summarized from memory.</p>

<p><strong>What <code class="language-plaintext highlighter-rouge">mgc.go</code> states directly, line for line:</strong></p>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">// The GC runs concurrently with mutator threads, is type accurate (aka precise), allows multiple</span>
<span class="c">// GC threads to run in parallel. It is a concurrent mark and sweep that uses a write barrier. It is</span>
<span class="c">// non-generational and non-compacting.</span>
</code></pre></div></div>

<p><strong>Takeaway:</strong></p>
<ul>
  <li>“concurrent mark and sweep” — the mark phase (finding live objects) and
sweep phase (reclaiming dead ones) both run alongside your program’s own
goroutines, not just alongside each other.</li>
  <li>“non-generational” — Go’s GC does <em>not</em> separate young/old objects into
different heaps swept at different rates, unlike HotSpot’s G1 or the
young/old generation split most JVM/V8 GCs use. Every GC cycle scans the
whole live set. This is a stated design tradeoff, not an oversight; the
source doesn’t give the rationale for the choice.</li>
  <li>“non-compacting” — dead objects are freed in place; Go doesn’t move
live objects around to defragment the heap the way a compacting
collector does. This is also why Go’s stack-map / pointer-tracking
machinery (Part 1.3) doesn’t need to handle objects moving mid-flight —
one less category of complexity than a moving collector like ZGC.</li>
</ul>

<p><strong>The three-color abstraction, sourced from the mark-phase walkthrough
(<code class="language-plaintext highlighter-rouge">mgc.go</code>, steps 2b–2d) plus the explicit invariant comment further down
the same file:</strong></p>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">// At this point all Ps have enabled the write</span>
<span class="c">// barrier, thus maintaining the no white to</span>
<span class="c">// black invariant.</span>
</code></pre></div></div>

<p>Objects are conceptually white (not yet visited — presumed garbage),
grey (visited, but its own pointers not yet scanned), or black (visited
<em>and</em> all its pointers scanned). The mark phase’s job is to walk grey
objects until none remain — at that point everything reachable is black,
everything still white is garbage. The <strong>invariant being protected</strong> —
“no white to black” — is: a black object (already fully scanned) must
never gain a pointer directly to a white object without that white object
getting shaded first. If that invariant were violated, the GC could finish
marking, still see the object as white, and free something the mutator
(your running program) still holds a live reference to. That’s the
correctness property the write barrier mechanism below exists to uphold.</p>

<p><strong>The write barrier itself, from <code class="language-plaintext highlighter-rouge">mbarrier.go</code>’s pseudocode:</strong></p>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">// Go uses a hybrid barrier that combines a Yuasa-style deletion</span>
<span class="c">// barrier—which shades the object whose reference is being</span>
<span class="c">// overwritten—with Dijkstra insertion barrier—which shades the object</span>
<span class="c">// whose reference is being written.</span>
<span class="c">//</span>
<span class="c">//     writePointer(slot, ptr):</span>
<span class="c">//         shade(*slot)</span>
<span class="c">//         if current stack is grey:</span>
<span class="c">//             shade(ptr)</span>
<span class="c">//         *slot = ptr</span>
</code></pre></div></div>

<p>And the reasoning for why <em>both</em> halves are needed, stated as three
numbered cases in the source:</p>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">// 1. shade(*slot) prevents a mutator from hiding an object by moving</span>
<span class="c">// the sole pointer to it from the heap to its stack.</span>
<span class="c">// 2. shade(ptr) prevents a mutator from hiding an object by moving</span>
<span class="c">// the sole pointer to it from its stack into a black object in the heap.</span>
<span class="c">// 3. Once a goroutine's stack is black, the shade(ptr) becomes</span>
<span class="c">// unnecessary.</span>
</code></pre></div></div>

<p>That’s the pseudocode from the file’s doc comment. The actual code that
runs it — <code class="language-plaintext highlighter-rouge">typedmemmove</code>, the function every pointer-containing struct
copy in Go goes through — shows the same idea gated by a real runtime
check, not just documented intent:</p>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">//go:linkname typedmemmove</span>
<span class="c">//go:nosplit</span>
<span class="k">func</span> <span class="n">typedmemmove</span><span class="p">(</span><span class="n">typ</span> <span class="o">*</span><span class="n">abi</span><span class="o">.</span><span class="n">Type</span><span class="p">,</span> <span class="n">dst</span><span class="p">,</span> <span class="n">src</span> <span class="n">unsafe</span><span class="o">.</span><span class="n">Pointer</span><span class="p">)</span> <span class="p">{</span>
	<span class="k">if</span> <span class="n">dst</span> <span class="o">==</span> <span class="n">src</span> <span class="p">{</span>
		<span class="k">return</span>
	<span class="p">}</span>
	<span class="k">if</span> <span class="n">writeBarrier</span><span class="o">.</span><span class="n">enabled</span> <span class="o">&amp;&amp;</span> <span class="n">typ</span><span class="o">.</span><span class="n">Pointers</span><span class="p">()</span> <span class="p">{</span>
		<span class="c">// This always copies a full value of type typ so it's safe</span>
		<span class="c">// to pass typ along as an optimization. See the comment on</span>
		<span class="c">// bulkBarrierPreWrite.</span>
		<span class="n">bulkBarrierPreWrite</span><span class="p">(</span><span class="kt">uintptr</span><span class="p">(</span><span class="n">dst</span><span class="p">),</span> <span class="kt">uintptr</span><span class="p">(</span><span class="n">src</span><span class="p">),</span> <span class="n">typ</span><span class="o">.</span><span class="n">PtrBytes</span><span class="p">,</span> <span class="n">typ</span><span class="p">)</span>
	<span class="p">}</span>
	<span class="c">// There's a race here: if some other goroutine can write to</span>
	<span class="c">// src, it may change some pointer in src after we've</span>
	<span class="c">// performed the write barrier but before we perform the</span>
	<span class="c">// memory copy. This safe because the write performed by that</span>
	<span class="c">// other goroutine must also be accompanied by a write</span>
	<span class="c">// barrier, so at worst we've unnecessarily greyed the old</span>
	<span class="c">// pointer that was in src.</span>
	<span class="n">memmove</span><span class="p">(</span><span class="n">dst</span><span class="p">,</span> <span class="n">src</span><span class="p">,</span> <span class="n">typ</span><span class="o">.</span><span class="n">Size_</span><span class="p">)</span>
<span class="p">}</span>
</code></pre></div></div>

<p>Two things this shows that the pseudocode alone doesn’t: the check is
<code class="language-plaintext highlighter-rouge">writeBarrier.enabled &amp;&amp; typ.Pointers()</code> — the barrier only runs at all if
the type being copied actually contains pointers, so copying a struct of
plain integers pays nothing. And the barrier call (<code class="language-plaintext highlighter-rouge">bulkBarrierPreWrite</code>,
which does the <code class="language-plaintext highlighter-rouge">shade(*slot)</code>/<code class="language-plaintext highlighter-rouge">shade(ptr)</code> work from the pseudocode above)
happens strictly <em>before</em> the <code class="language-plaintext highlighter-rouge">memmove</code> that performs the real copy — the
comment directly under it explains why a race on <code class="language-plaintext highlighter-rouge">src</code> between the
barrier and the copy is still safe: the other goroutine’s own write to
<code class="language-plaintext highlighter-rouge">src</code> would have gone through its own barrier already, so at worst an
object gets greyed unnecessarily, never missed.</p>

<p><strong>Takeaway:</strong> a write barrier is code the compiler inserts around
<em>every</em> pointer write during the concurrent mark phase — not a hardware
mechanism like the JVM’s poll page (Part 3.5.2), but a software
instrumentation cost paid only while <code class="language-plaintext highlighter-rouge">gcphase == _GCmark</code> (confirmed by
<code class="language-plaintext highlighter-rouge">mgc.go</code>’s phase-transition code, which flips <code class="language-plaintext highlighter-rouge">gcBlackenEnabled</code>/enables
the barrier at mark start and disables it at sweep start). The two halves
guard the two directions an object could otherwise get “lost”: deleting
the only pointer to it from an already-scanned (black) part of the heap
(case 1, Yuasa-style), or the mutator itself moving a pointer from its own
unscanned stack into a black object before the GC ever sees it (case 2,
Dijkstra-style). Case 3 is the source explaining its own optimization —
once a goroutine’s <em>whole stack</em> has been scanned (turned black), the
insertion half becomes redundant, because nothing on that stack can be
hiding an unshaded pointer anymore.</p>

<p><strong>Open question:</strong> the mark-work-stealing/distributed-termination
algorithm in <code class="language-plaintext highlighter-rouge">gcMarkDone</code> (mentioned but not shown above), and the
concurrent sweep’s per-span locking scheme — not covered by these two
files.</p>

<h3 id="14-checkpoint">1.4 Checkpoint</h3>

<p>Trace the actual failure this fixed: before 1.14, <code class="language-plaintext highlighter-rouge">for {}</code> with no
function calls inside it. What specifically blocked, and for how long?
(Answer: any <code class="language-plaintext highlighter-rouge">stopTheWorldWithSema</code> call in <code class="language-plaintext highlighter-rouge">mgc.go</code> — i.e. every GC mark
or sweep termination phase — blocked indefinitely, because the goroutine
never reached the stack-bound check that synchronous preemption relies on.
This wasn’t a latency inconvenience; it was an unbounded stall.)</p>

<hr />

<h2 id="part-2--kotlin">Part 2 — Kotlin</h2>

<p>Sources read directly for this section: <code class="language-plaintext highlighter-rouge">kotlin.coroutines.Continuation</code>
and <code class="language-plaintext highlighter-rouge">kotlin.coroutines.intrinsics</code> (JetBrains/kotlin, stdlib — this part is
<strong>language-level</strong>), and <code class="language-plaintext highlighter-rouge">kotlinx.coroutines.CoroutineDispatcher</code> /
<code class="language-plaintext highlighter-rouge">kotlinx.coroutines.scheduling.CoroutineScheduler</code> (Kotlin/kotlinx.coroutines
— this part is a <strong>library</strong>, not the compiler or the language spec).</p>

<p>This split is the correction from the previous draft: <code class="language-plaintext highlighter-rouge">suspend</code> and the
CPS transform are compiler + stdlib. <code class="language-plaintext highlighter-rouge">Dispatchers</code>, <code class="language-plaintext highlighter-rouge">Job</code>,
<code class="language-plaintext highlighter-rouge">CoroutineScope</code>, structured concurrency, and the actual thread-pool
scheduler are <strong>kotlinx.coroutines</strong>, a library published by JetBrains but
not part of Kotlin itself — you can write <code class="language-plaintext highlighter-rouge">suspend</code> functions with zero
dependency on kotlinx.coroutines; it only becomes necessary once you want
dispatchers, structured cancellation, <code class="language-plaintext highlighter-rouge">async</code>/<code class="language-plaintext highlighter-rouge">await</code>-style builders, or
<code class="language-plaintext highlighter-rouge">Flow</code>.</p>

<h3 id="21-compiler--stdlib-layer-what-suspend-actually-is">2.1 Compiler + stdlib layer: what <code class="language-plaintext highlighter-rouge">suspend</code> actually is</h3>

<p>Kotlin runs primarily on the JVM. It cannot change the JVM’s thread model —
JVM threads are OS threads. The lever available is using fewer of them,
which means: don’t let a suspended computation hold a thread’s stack.</p>

<p>The mechanism is CPS (Continuation-Passing Style), and the actual
interface it compiles against is <code class="language-plaintext highlighter-rouge">kotlin.coroutines.Continuation</code>, defined
in stdlib:</p>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">public</span> <span class="kd">interface</span> <span class="nc">Continuation</span><span class="p">&lt;</span><span class="k">in</span> <span class="nc">T</span><span class="p">&gt;</span> <span class="p">{</span>
    <span class="k">public</span> <span class="kd">val</span> <span class="py">context</span><span class="p">:</span> <span class="nc">CoroutineContext</span>
    <span class="k">public</span> <span class="k">fun</span> <span class="nf">resumeWith</span><span class="p">(</span><span class="n">result</span><span class="p">:</span> <span class="nc">Result</span><span class="p">&lt;</span><span class="nc">T</span><span class="p">&gt;)</span>
<span class="p">}</span>
</code></pre></div></div>

<p>That’s the entire primitive. A <code class="language-plaintext highlighter-rouge">suspend</code> function, at the ABI level, is a
regular JVM method that takes an extra trailing <code class="language-plaintext highlighter-rouge">Continuation</code> parameter
and returns <code class="language-plaintext highlighter-rouge">Any?</code> — either the real result, or the sentinel
<code class="language-plaintext highlighter-rouge">COROUTINE_SUSPENDED</code> (defined in <code class="language-plaintext highlighter-rouge">kotlin.coroutines.intrinsics</code>,
stdlib) if it suspended. <code class="language-plaintext highlighter-rouge">suspendCoroutineUninterceptedOrReturn</code> is the
stdlib intrinsic that exposes the raw continuation to library authors
building suspension primitives on top.</p>

<p>The compiler transforms a <code class="language-plaintext highlighter-rouge">suspend</code> function’s body into a state machine
object implementing <code class="language-plaintext highlighter-rouge">Continuation</code>, with a <code class="language-plaintext highlighter-rouge">label</code> field tracking progress
and local variables lifted into fields, roughly:</p>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">suspend</span> <span class="k">fun</span> <span class="nf">fetchAndProcess</span><span class="p">()</span> <span class="p">{</span>
    <span class="kd">val</span> <span class="py">data</span> <span class="p">=</span> <span class="nf">fetch</span><span class="p">()</span>
    <span class="kd">val</span> <span class="py">result</span> <span class="p">=</span> <span class="nf">process</span><span class="p">(</span><span class="n">data</span><span class="p">)</span>
    <span class="nf">save</span><span class="p">(</span><span class="n">result</span><span class="p">)</span>
<span class="p">}</span>
</code></pre></div></div>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">class</span> <span class="nc">FetchAndProcessContinuation</span><span class="p">(</span><span class="kd">val</span> <span class="py">completion</span><span class="p">:</span> <span class="nc">Continuation</span><span class="p">&lt;</span><span class="nc">Unit</span><span class="p">&gt;)</span> <span class="p">:</span> <span class="nc">Continuation</span><span class="p">&lt;</span><span class="nc">Any</span><span class="p">?&gt;</span> <span class="p">{</span>
    <span class="kd">var</span> <span class="py">label</span> <span class="p">=</span> <span class="mi">0</span>
    <span class="kd">var</span> <span class="py">data</span><span class="p">:</span> <span class="nc">Data</span><span class="p">?</span> <span class="p">=</span> <span class="k">null</span>

    <span class="k">override</span> <span class="k">fun</span> <span class="nf">resumeWith</span><span class="p">(</span><span class="n">result</span><span class="p">:</span> <span class="nc">Result</span><span class="p">&lt;</span><span class="nc">Any</span><span class="p">?&gt;)</span> <span class="p">{</span>
        <span class="k">when</span> <span class="p">(</span><span class="n">label</span><span class="p">)</span> <span class="p">{</span>
            <span class="mi">0</span> <span class="p">-&gt;</span> <span class="p">{</span> <span class="n">label</span> <span class="p">=</span> <span class="mi">1</span><span class="p">;</span> <span class="nf">fetch</span><span class="p">(</span><span class="k">this</span><span class="p">)</span> <span class="p">}</span>
            <span class="mi">1</span> <span class="p">-&gt;</span> <span class="p">{</span> <span class="n">data</span> <span class="p">=</span> <span class="n">result</span><span class="p">.</span><span class="nf">getOrThrow</span><span class="p">()</span> <span class="k">as</span> <span class="nc">Data</span><span class="p">;</span> <span class="n">label</span> <span class="p">=</span> <span class="mi">2</span><span class="p">;</span> <span class="nf">process</span><span class="p">(</span><span class="n">data</span><span class="p">,</span> <span class="k">this</span><span class="p">)</span> <span class="p">}</span>
            <span class="mi">2</span> <span class="p">-&gt;</span> <span class="p">{</span> <span class="nf">save</span><span class="p">(</span><span class="n">result</span><span class="p">.</span><span class="nf">getOrThrow</span><span class="p">()</span> <span class="k">as</span> <span class="nc">Result</span><span class="p">);</span> <span class="n">completion</span><span class="p">.</span><span class="nf">resumeWith</span><span class="p">(</span><span class="nc">Result</span><span class="p">.</span><span class="nf">success</span><span class="p">(</span><span class="nc">Unit</span><span class="p">))</span> <span class="p">}</span>
        <span class="p">}</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<p>The function returns (freeing the thread) instead of blocking; the
continuation object on the heap carries the state needed to resume. All of
this — <code class="language-plaintext highlighter-rouge">Continuation</code>, the CPS transform, <code class="language-plaintext highlighter-rouge">suspend</code> as a keyword — is
<strong>part of the Kotlin language and compiler</strong>, independent of any
concurrency library. This is why <code class="language-plaintext highlighter-rouge">suspend</code> functions work fine in, say, a
minimal environment with no kotlinx.coroutines dependency at all, as long
as something implements <code class="language-plaintext highlighter-rouge">Continuation</code> to drive them.</p>

<p><strong>Tracing it through <code class="language-plaintext highlighter-rouge">launch</code>, which resolves the open question from
2.4a about where <code class="language-plaintext highlighter-rouge">DispatchedContinuation</code> gets constructed:</strong></p>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">fun</span> <span class="nf">main</span><span class="p">()</span> <span class="p">=</span> <span class="nf">runBlocking</span> <span class="p">{</span>
    <span class="nf">launch</span> <span class="p">{</span>
        <span class="nf">println</span><span class="p">(</span><span class="s">"A"</span><span class="p">)</span>
        <span class="nf">delay</span><span class="p">(</span><span class="mi">1000</span><span class="p">)</span>   <span class="c1">// suspend point</span>
        <span class="nf">println</span><span class="p">(</span><span class="s">"B"</span><span class="p">)</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">launch</code> (a kotlinx.coroutines builder, library-layer) takes the lambda,
which the compiler has already turned into a <code class="language-plaintext highlighter-rouge">Continuation</code>-implementing
state machine roughly like:</p>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">class</span> <span class="nc">LambdaContinuation</span> <span class="p">:</span> <span class="nc">Continuation</span><span class="p">&lt;</span><span class="nc">Unit</span><span class="p">&gt;</span> <span class="p">{</span>
    <span class="kd">var</span> <span class="py">label</span> <span class="p">=</span> <span class="mi">0</span>
    <span class="k">override</span> <span class="k">fun</span> <span class="nf">resumeWith</span><span class="p">(</span><span class="n">result</span><span class="p">:</span> <span class="nc">Result</span><span class="p">&lt;</span><span class="nc">Unit</span><span class="p">&gt;)</span> <span class="p">{</span>
        <span class="k">when</span> <span class="p">(</span><span class="n">label</span><span class="p">)</span> <span class="p">{</span>
            <span class="mi">0</span> <span class="p">-&gt;</span> <span class="p">{</span> <span class="nf">println</span><span class="p">(</span><span class="s">"A"</span><span class="p">);</span> <span class="n">label</span> <span class="p">=</span> <span class="mi">1</span><span class="p">;</span> <span class="nf">delay</span><span class="p">(</span><span class="mi">1000</span><span class="p">,</span> <span class="k">this</span><span class="p">)</span> <span class="p">}</span> <span class="c1">// returns here</span>
            <span class="mi">1</span> <span class="p">-&gt;</span> <span class="p">{</span> <span class="nf">println</span><span class="p">(</span><span class="s">"B"</span><span class="p">)</span> <span class="p">}</span>
        <span class="p">}</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">launch</code> wraps that state machine in a <code class="language-plaintext highlighter-rouge">DispatchedContinuation</code> (Part 2.4a)
before ever calling it. When execution reaches <code class="language-plaintext highlighter-rouge">delay(1000)</code>, the function
returns immediately — the thread is free, not blocked — and a timer is
armed. When the timer fires 1000ms later, something calls <code class="language-plaintext highlighter-rouge">resumeWith</code> on
the <em>same</em> <code class="language-plaintext highlighter-rouge">DispatchedContinuation</code> object; its <code class="language-plaintext highlighter-rouge">resumeWith</code> override (Part
2.4a) checks <code class="language-plaintext highlighter-rouge">dispatcher.safeIsDispatchNeeded</code> and routes the resume
through the dispatcher’s thread pool if needed, which then calls the
wrapped continuation’s <code class="language-plaintext highlighter-rouge">resumeWith</code>, landing back in <code class="language-plaintext highlighter-rouge">label = 1</code> and
printing “B” — possibly on a completely different thread than the one
that printed “A”.</p>

<p>The cooperative part: the compiler only inserted a resumable point at
<code class="language-plaintext highlighter-rouge">delay</code>, because <code class="language-plaintext highlighter-rouge">delay</code> is a <code class="language-plaintext highlighter-rouge">suspend</code> call. A <code class="language-plaintext highlighter-rouge">launch { while (true) {} }</code>
with no suspend calls inside the loop has no state machine transition
anywhere in the loop body — nothing for any dispatcher to interrupt into,
unlike Go’s <code class="language-plaintext highlighter-rouge">for {}</code> example above, which the runtime can still signal
into from outside regardless of what the loop body does.</p>

<h3 id="22-why-this-makes-suspension-cooperative">2.2 Why this makes suspension cooperative</h3>

<p>The compiler must know, at compile time, every point where execution might
suspend — that’s the entire reason <code class="language-plaintext highlighter-rouge">suspend</code> is a keyword rather than a
runtime annotation. If a coroutine body never calls a suspend function
inside a loop, the compiler never inserts a state-machine transition
there, so there is no return/yield point to force. There’s no external
mechanism analogous to Go’s <code class="language-plaintext highlighter-rouge">SIGURG</code> that could interrupt it, because
nothing external to the continuation object was ever created to interrupt
<em>into</em> — the compiler-generated state machine, not raw thread register
state, is the unit of suspension, and it only exists at points the source
code actually marked as suspendable.</p>

<h3 id="23-library-layer-kotlinxcoroutines-and-what-it-adds">2.3 Library layer: kotlinx.coroutines and what it adds</h3>

<p>Continuation alone gives you suspension; it gives you nothing about <em>where</em>
code resumes, cancellation, or structured lifetimes. That’s what
kotlinx.coroutines is for:</p>

<ul>
  <li><strong><code class="language-plaintext highlighter-rouge">CoroutineDispatcher</code></strong> implements <code class="language-plaintext highlighter-rouge">ContinuationInterceptor</code> — it’s
what decides <em>which thread</em> a continuation resumes on.
<code class="language-plaintext highlighter-rouge">Dispatchers.Default</code> and <code class="language-plaintext highlighter-rouge">Dispatchers.IO</code> are the common ones.</li>
  <li><strong><code class="language-plaintext highlighter-rouge">CoroutineScheduler</code></strong> (<code class="language-plaintext highlighter-rouge">kotlinx-coroutines-core/jvm/.../scheduling/</code>)
is the actual thread pool behind <code class="language-plaintext highlighter-rouge">Dispatchers.Default</code>/<code class="language-plaintext highlighter-rouge">IO</code>. Its own doc
comment is explicit about the design lineage:
    <blockquote>
      <p>“The original idea with a single-slot LIFO buffer comes from Golang
runtime scheduler by D. Vyukov. It was proven to be ‘fair enough’,
performant and generally well accepted and initially was a significant
inspiration source for the coroutine scheduler.”</p>
    </blockquote>

    <p>Concretely: per-worker local run queues, work-stealing when a worker’s
queue empties, and a global queue for tasks submitted from outside the
pool — the same shape as Go’s per-P local run queues plus work-stealing
(<code class="language-plaintext highlighter-rouge">findRunnable</code> in <code class="language-plaintext highlighter-rouge">proc.go</code>). This is convergent evolution <em>by direct
admission</em>, not independent design — Kotlin’s authors read Go’s
scheduler and reused the shape for a completely different substrate (JVM
threads instead of goroutines).</p>

    <p>The actual <code class="language-plaintext highlighter-rouge">dispatch</code> function, not just the doc comment describing it:</p>

    <div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">fun</span> <span class="nf">dispatch</span><span class="p">(</span><span class="n">block</span><span class="p">:</span> <span class="nc">Runnable</span><span class="p">,</span> <span class="n">taskContext</span><span class="p">:</span> <span class="nc">TaskContext</span> <span class="p">=</span> <span class="nc">NonBlockingContext</span><span class="p">,</span> <span class="n">fair</span><span class="p">:</span> <span class="nc">Boolean</span> <span class="p">=</span> <span class="k">false</span><span class="p">)</span> <span class="p">{</span>
    <span class="nf">trackTask</span><span class="p">()</span> <span class="c1">// this is needed for virtual time support</span>
    <span class="kd">val</span> <span class="py">task</span> <span class="p">=</span> <span class="nf">createTask</span><span class="p">(</span><span class="n">block</span><span class="p">,</span> <span class="n">taskContext</span><span class="p">)</span>
    <span class="kd">val</span> <span class="py">isBlockingTask</span> <span class="p">=</span> <span class="n">task</span><span class="p">.</span><span class="n">isBlocking</span>
    <span class="kd">val</span> <span class="py">stateSnapshot</span> <span class="p">=</span> <span class="k">if</span> <span class="p">(</span><span class="n">isBlockingTask</span><span class="p">)</span> <span class="nf">incrementBlockingTasks</span><span class="p">()</span> <span class="k">else</span> <span class="mi">0</span>
    <span class="c1">// try to submit the task to the local queue and act depending on the result</span>
    <span class="kd">val</span> <span class="py">currentWorker</span> <span class="p">=</span> <span class="nf">currentWorker</span><span class="p">()</span>
    <span class="kd">val</span> <span class="py">notAdded</span> <span class="p">=</span> <span class="n">currentWorker</span><span class="p">.</span><span class="nf">submitToLocalQueue</span><span class="p">(</span><span class="n">task</span><span class="p">,</span> <span class="n">fair</span><span class="p">)</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">notAdded</span> <span class="p">!=</span> <span class="k">null</span><span class="p">)</span> <span class="p">{</span>
        <span class="k">if</span> <span class="p">(!</span><span class="nf">addToGlobalQueue</span><span class="p">(</span><span class="n">notAdded</span><span class="p">))</span> <span class="p">{</span>
            <span class="k">throw</span> <span class="nc">RejectedExecutionException</span><span class="p">(</span><span class="s">"$schedulerName was terminated"</span><span class="p">)</span>
        <span class="p">}</span>
    <span class="p">}</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">isBlockingTask</span><span class="p">)</span> <span class="p">{</span>
        <span class="nf">signalBlockingWork</span><span class="p">(</span><span class="n">stateSnapshot</span><span class="p">)</span>
    <span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
        <span class="nf">signalCpuWork</span><span class="p">()</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div>    </div>

    <p>This is the local-queue-first, global-queue-as-overflow policy the doc
comment describes, made concrete: <code class="language-plaintext highlighter-rouge">submitToLocalQueue</code> tries the calling
worker’s own queue first (the cheap, no-contention path); only if that
fails (<code class="language-plaintext highlighter-rouge">notAdded != null</code> — the local queue is full) does the task fall
back to <code class="language-plaintext highlighter-rouge">addToGlobalQueue</code>, the shared, contended queue every idle
worker also checks. The blocking-vs-CPU distinction
(<code class="language-plaintext highlighter-rouge">signalBlockingWork</code>/<code class="language-plaintext highlighter-rouge">signalCpuWork</code>) is Kotlin-specific — Go’s GMP
model has no equivalent split, since a goroutine that blocks in a
syscall just releases its P (Part 1.2) rather than being classified
ahead of time by the scheduler.</p>
  </li>
  <li><strong><code class="language-plaintext highlighter-rouge">Job</code>, <code class="language-plaintext highlighter-rouge">CoroutineScope</code>, structured concurrency</strong> — the parent/child
cancellation tree that ensures a scope can’t complete while children are
still running, and that cancelling a parent cancels all children. This
is the closest thing Kotlin has to fork-join semantics, but it’s opt-in
library behavior layered on top of continuations, not a language feature.</li>
</ul>

<h3 id="24-gc-consequence">2.4 GC consequence</h3>

<p>Kotlin/JVM’s GC is whatever the underlying JVM GC is (G1, ZGC, etc.).
Coroutines don’t change the GC algorithm — they change what’s running on a
given OS thread at a given moment, and they add continuation objects to
the heap (one allocation per suspend point per invocation, though the JVM
JIT can sometimes eliminate short-lived ones). Suspension does not create
a GC safe-point problem analogous to Go’s, because JVM safe-points are a
separate, JIT-inserted polling mechanism entirely orthogonal to coroutine
suspension — see Part 4 for how HotSpot’s safepoint polling actually works,
which is the real GC-relevant mechanism on this stack, not the coroutine
machinery itself.</p>

<h3 id="24a-continuation-and-dispatchedcontinuation-from-source">2.4a Continuation and DispatchedContinuation, from source</h3>

<p>Read directly:
<a href="https://github.com/JetBrains/kotlin/blob/master/libraries/stdlib/src/kotlin/coroutines/Continuation.kt"><code class="language-plaintext highlighter-rouge">kotlin/libraries/stdlib/src/kotlin/coroutines/Continuation.kt</code></a>,
<a href="https://github.com/JetBrains/kotlin/blob/master/libraries/stdlib/src/kotlin/coroutines/intrinsics/Intrinsics.kt"><code class="language-plaintext highlighter-rouge">kotlin/libraries/stdlib/src/kotlin/coroutines/intrinsics/Intrinsics.kt</code></a>,
<a href="https://github.com/Kotlin/kotlinx.coroutines/blob/master/kotlinx-coroutines-core/common/src/internal/DispatchedContinuation.kt"><code class="language-plaintext highlighter-rouge">kotlinx.coroutines/kotlinx-coroutines-core/common/src/internal/DispatchedContinuation.kt</code></a>.</p>

<p><strong><code class="language-plaintext highlighter-rouge">Continuation.kt</code>, the entire interface, quoted in full because it’s short
enough that summarizing it would lose information:</strong></p>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">public</span> <span class="kd">interface</span> <span class="nc">Continuation</span><span class="p">&lt;</span><span class="k">in</span> <span class="nc">T</span><span class="p">&gt;</span> <span class="p">{</span>
    <span class="k">public</span> <span class="kd">val</span> <span class="py">context</span><span class="p">:</span> <span class="nc">CoroutineContext</span>
    <span class="k">public</span> <span class="k">fun</span> <span class="nf">resumeWith</span><span class="p">(</span><span class="n">result</span><span class="p">:</span> <span class="nc">Result</span><span class="p">&lt;</span><span class="nc">T</span><span class="p">&gt;)</span>
<span class="p">}</span>
</code></pre></div></div>

<p><strong>Takeaway:</strong> the stdlib’s contract for “a suspended computation” is
exactly two things — a context bag, and one method to call when a result
is ready. Nothing here mentions threads, dispatchers, or scheduling:
thread placement is not a compiler-level concern, since the interface the
compiler generates against has no slot for it.</p>

<p><strong><code class="language-plaintext highlighter-rouge">Intrinsics.kt</code>, the actual suspension primitive:</strong></p>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">public</span> <span class="k">suspend</span> <span class="k">inline</span> <span class="k">fun</span> <span class="p">&lt;</span><span class="nc">T</span><span class="p">&gt;</span> <span class="nf">suspendCoroutineUninterceptedOrReturn</span><span class="p">(</span>
    <span class="k">crossinline</span> <span class="n">block</span><span class="p">:</span> <span class="p">(</span><span class="nc">Continuation</span><span class="p">&lt;</span><span class="nc">T</span><span class="p">&gt;)</span> <span class="p">-&gt;</span> <span class="nc">Any</span><span class="p">?</span>
<span class="p">):</span> <span class="nc">T</span> <span class="p">{</span>
    <span class="nf">contract</span> <span class="p">{</span> <span class="nf">callsInPlace</span><span class="p">(</span><span class="n">block</span><span class="p">,</span> <span class="nc">InvocationKind</span><span class="p">.</span><span class="nc">EXACTLY_ONCE</span><span class="p">)</span> <span class="p">}</span>
    <span class="k">throw</span> <span class="nc">NotImplementedError</span><span class="p">(</span><span class="s">"Implementation of suspendCoroutineUninterceptedOrReturn is intrinsic"</span><span class="p">)</span>
<span class="p">}</span>
</code></pre></div></div>

<p><strong>Takeaway:</strong> the body is a <code class="language-plaintext highlighter-rouge">throw</code> — this function has no real
implementation in Kotlin source at all. The comment above it in the file
(not reproduced above) states the implementation is intrinsic: the
compiler recognizes this specific function by name and replaces the call
with generated bytecode directly. The CPS transform is not a library
trick built on top of ordinary language features — the compiler has
special-cased knowledge of this function.</p>

<p><strong>Open question:</strong> the compiler’s codegen for the intrinsic itself is not
visible from this file — only the signature and doc-comment stating that
special-casing happens.</p>

<p><strong><code class="language-plaintext highlighter-rouge">DispatchedContinuation.kt</code>, confirming the wrapping claim from
checkpoint 2.5:</strong></p>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">internal</span> <span class="kd">class</span> <span class="nc">DispatchedContinuation</span><span class="p">&lt;</span><span class="k">in</span> <span class="nc">T</span><span class="p">&gt;(</span>
    <span class="nd">@JvmField</span> <span class="k">internal</span> <span class="kd">val</span> <span class="py">dispatcher</span><span class="p">:</span> <span class="nc">CoroutineDispatcher</span><span class="p">,</span>
    <span class="nd">@JvmField</span> <span class="kd">val</span> <span class="py">continuation</span><span class="p">:</span> <span class="nc">Continuation</span><span class="p">&lt;</span><span class="nc">T</span><span class="p">&gt;</span>
<span class="p">)</span> <span class="p">:</span> <span class="nc">DispatchedTask</span><span class="p">&lt;</span><span class="nc">T</span><span class="p">&gt;(</span><span class="nc">MODE_UNINITIALIZED</span><span class="p">),</span> <span class="nc">CoroutineStackFrame</span><span class="p">,</span> <span class="nc">Continuation</span><span class="p">&lt;</span><span class="nc">T</span><span class="p">&gt;</span> <span class="k">by</span> <span class="nf">continuation</span> <span class="p">{</span>
</code></pre></div></div>

<p><strong>Takeaway:</strong> <code class="language-plaintext highlighter-rouge">DispatchedContinuation</code> holds both a <code class="language-plaintext highlighter-rouge">dispatcher</code> field and
the original compiler-generated <code class="language-plaintext highlighter-rouge">continuation</code> as a delegate
(<code class="language-plaintext highlighter-rouge">Continuation&lt;T&gt; by continuation</code>). It wraps the compiler’s continuation
rather than replacing it — the compiler produces the plain <code class="language-plaintext highlighter-rouge">Continuation</code>;
kotlinx.coroutines wraps it in a dispatcher-aware class before anything
calls <code class="language-plaintext highlighter-rouge">resumeWith</code>.</p>

<p>The actual dispatch decision — where the thread-hop the field declarations
above only imply actually happens — is <code class="language-plaintext highlighter-rouge">DispatchedContinuation</code>’s own
<code class="language-plaintext highlighter-rouge">resumeWith</code> override:</p>

<div class="language-kotlin highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">override</span> <span class="k">fun</span> <span class="nf">resumeWith</span><span class="p">(</span><span class="n">result</span><span class="p">:</span> <span class="nc">Result</span><span class="p">&lt;</span><span class="nc">T</span><span class="p">&gt;)</span> <span class="p">{</span>
    <span class="kd">val</span> <span class="py">state</span> <span class="p">=</span> <span class="n">result</span><span class="p">.</span><span class="nf">toState</span><span class="p">()</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">dispatcher</span><span class="p">.</span><span class="nf">safeIsDispatchNeeded</span><span class="p">(</span><span class="n">context</span><span class="p">))</span> <span class="p">{</span>
        <span class="n">_state</span> <span class="p">=</span> <span class="n">state</span>
        <span class="n">resumeMode</span> <span class="p">=</span> <span class="nc">MODE_ATOMIC</span>
        <span class="n">dispatcher</span><span class="p">.</span><span class="nf">safeDispatch</span><span class="p">(</span><span class="n">context</span><span class="p">,</span> <span class="k">this</span><span class="p">)</span>
    <span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
        <span class="nf">executeUnconfined</span><span class="p">(</span><span class="n">state</span><span class="p">,</span> <span class="nc">MODE_ATOMIC</span><span class="p">)</span> <span class="p">{</span>
            <span class="nf">withCoroutineContext</span><span class="p">(</span><span class="n">context</span><span class="p">,</span> <span class="n">countOrElement</span><span class="p">)</span> <span class="p">{</span>
                <span class="n">continuation</span><span class="p">.</span><span class="nf">resumeWith</span><span class="p">(</span><span class="n">result</span><span class="p">)</span>
            <span class="p">}</span>
        <span class="p">}</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div>

<p>Every time the wrapped operation completes, this is what actually runs
(not the plain <code class="language-plaintext highlighter-rouge">Continuation.resumeWith</code> interface method from stdlib,
Part 2.4a). It asks the dispatcher <code class="language-plaintext highlighter-rouge">safeIsDispatchNeeded(context)</code> — if
the current thread is already the right one (e.g. an unconfined
dispatcher, or resuming on the same thread that’s already correct), it
calls straight through to the wrapped <code class="language-plaintext highlighter-rouge">continuation.resumeWith(result)</code>
in place, no thread hop. Otherwise it stashes the result in <code class="language-plaintext highlighter-rouge">_state</code> and
calls <code class="language-plaintext highlighter-rouge">dispatcher.safeDispatch(context, this)</code>, which schedules this same
object onto the dispatcher’s thread pool (Part 2.3’s <code class="language-plaintext highlighter-rouge">CoroutineScheduler</code>)
to be resumed there. This is the concrete mechanism behind “the dispatcher
decides what thread to resume on” (checkpoint 2.5’s answer) — a runtime
branch inside <code class="language-plaintext highlighter-rouge">resumeWith</code>, not something the compiler’s state machine
knows anything about.</p>

<p><strong>Open question:</strong> the literal source line inside kotlinx.coroutines’
<code class="language-plaintext highlighter-rouge">launch</code>/<code class="language-plaintext highlighter-rouge">async</code> implementation where a <code class="language-plaintext highlighter-rouge">DispatchedContinuation</code> gets
constructed — Part 2.1’s <code class="language-plaintext highlighter-rouge">launch</code>/<code class="language-plaintext highlighter-rouge">delay</code> walkthrough traces the mechanism
conceptually (what gets wrapped, when it resumes, which thread), but the
actual builder source file itself hasn’t been read to confirm the
construction call site line-for-line.</p>

<h3 id="25-checkpoint">2.5 Checkpoint</h3>

<p>Two continuations exist for a suspending coroutine call: the compiler
generates one automatically. Where does the <em>dispatcher</em> decide what
thread to resume on — inside <code class="language-plaintext highlighter-rouge">Continuation.resumeWith</code>, or somewhere
kotlinx.coroutines wraps around it? (Answer: kotlinx.coroutines wraps the
compiler-generated continuation with <code class="language-plaintext highlighter-rouge">ContinuationInterceptor.interceptContinuation</code>,
producing a <code class="language-plaintext highlighter-rouge">DispatchedContinuation</code> that routes the actual <code class="language-plaintext highlighter-rouge">resumeWith</code>
call through the dispatcher’s thread pool. The compiler’s state machine
has no thread-affinity logic of its own — that’s entirely library-layer.)</p>

<hr />

<h2 id="part-3--beam">Part 3 — BEAM</h2>

<p>Sources read directly for this section:
<a href="https://github.com/erlang/otp/blob/master/erts/emulator/beam/erl_process.c"><code class="language-plaintext highlighter-rouge">erts/emulator/beam/erl_process.c</code></a>,
<a href="https://github.com/erlang/otp/blob/master/erts/emulator/beam/erl_vm.h"><code class="language-plaintext highlighter-rouge">erts/emulator/beam/erl_vm.h</code></a>,
<a href="https://github.com/erlang/otp/blob/master/erts/emulator/beam/emu/beam_emu.c"><code class="language-plaintext highlighter-rouge">erts/emulator/beam/emu/beam_emu.c</code></a>,
<a href="https://github.com/erlang/otp/blob/master/erts/emulator/beam/erl_gc.c"><code class="language-plaintext highlighter-rouge">erts/emulator/beam/erl_gc.c</code></a>
— erlang/otp, master branch.</p>

<h3 id="31-constraint">3.1 Constraint</h3>

<p>Erlang’s original design question (Joe Armstrong) was not about
performance — it was: how do you build a system that can run essentially
forever without going down? The answer: faults must be contained, which
requires isolation, which requires sharing nothing.</p>

<h3 id="32-design-per-process-heap-no-shared-memory">3.2 Design: per-process heap, no shared memory</h3>

<p>BEAM processes (VM-managed, not OS processes) share no memory. Each has
its own heap and its own GC. <code class="language-plaintext highlighter-rouge">beam_emu.c</code>’s <code class="language-plaintext highlighter-rouge">process_main</code> makes this
concrete — the interpreter loop’s registers are literally per-process
state, not per-OS-thread:</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cm">/* Pointer to X registers: x(0)..x(N). */</span>
<span class="k">register</span> <span class="n">Eterm</span><span class="o">*</span> <span class="n">reg</span> <span class="n">REG_xregs</span> <span class="o">=</span> <span class="nb">NULL</span><span class="p">;</span>

<span class="cm">/* Top of heap (next free location); grows upwards. */</span>
<span class="k">register</span> <span class="n">Eterm</span><span class="o">*</span> <span class="n">HTOP</span> <span class="n">REG_htop</span> <span class="o">=</span> <span class="nb">NULL</span><span class="p">;</span>

<span class="cm">/* Stack pointer. Grows downwards; points
 * to last item pushed (normally a saved
 * continuation pointer). */</span>
<span class="k">register</span> <span class="n">Eterm</span><span class="o">*</span> <span class="n">E</span> <span class="n">REG_stop</span> <span class="o">=</span> <span class="nb">NULL</span><span class="p">;</span>
</code></pre></div></div>

<p>Each process being swapped onto a scheduler thread brings its own heap
pointer (<code class="language-plaintext highlighter-rouge">HTOP</code>), its own stack pointer (<code class="language-plaintext highlighter-rouge">E</code>), and its own register file
(<code class="language-plaintext highlighter-rouge">reg</code>) — nothing here is shared across processes the way a JVM heap or Go
heap is shared across threads/goroutines. Sending a message to another
process copies the data into the target’s heap (large binaries are the
exception — reference-counted in a shared binary heap to avoid the copy).</p>

<p>Because no process can see another’s stack or heap, there’s no shared
safe-point problem: the scheduler never needs to reason about pointer
validity in <em>another</em> process’s memory to preempt one process, which is
exactly the constraint Go’s stack maps (Part 1.3) and HotSpot’s poll pages
(Part 3.5) both exist to satisfy and BEAM structurally doesn’t need to.</p>

<h3 id="33-key-concept-what-a-reduction-actually-is">3.3 Key concept: what a reduction actually is</h3>

<p>Before the mechanism, the term itself. “Reduction” is not BEAM-specific
jargon invented for scheduling — it comes from <strong>term rewriting /
reduction semantics</strong> in programming language theory: evaluating an
expression is a sequence of “reduction steps,” each rewriting the term
closer to a final value (the same root sense as “beta reduction” in lambda
calculus — Erlang’s interpreter is, structurally, a term-rewriting
machine, and BEAM is literally short for “Bogdan/Björn’s Erlang Abstract
Machine,” an abstract machine executing that rewriting). A BEAM reduction
is the <em>scheduling</em> projection of that idea: one accounting unit charged
per unit of interpreter work — approximately one function call, one BIF
(built-in function) call, or one comparable primitive operation — deducted
from a process’s budget every time the interpreter dispatches one.</p>

<p>It is deliberately <strong>not</strong> a fixed wall-clock unit. It’s an abstraction
that lets the scheduler compare “how much work has this process done”
across processes without needing a hardware timer interrupt (contrast Part
0’s OS timer-interrupt preemption) — the interpreter itself is the thing
counting, inline, as part of doing the work. That’s what makes it cheap:
there’s no separate polling instruction (contrast HotSpot’s poll page,
Part 3.5.2) or external interrupt (contrast Go’s SIGURG, Part 1.3) —
the decrement is folded into instruction dispatch that was happening
anyway.</p>

<p>Confirming this isn’t purely a scheduling-only concept, <code class="language-plaintext highlighter-rouge">erl_process.h</code>
also uses reductions as a genuine unit of measurement, converting elapsed
wall-clock time into an equivalent reduction count (used, among other
things, to charge a process for time spent blocked so the scheduler’s
accounting stays consistent):</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">ErtsMonotonicTime</span> <span class="n">time</span> <span class="o">=</span> <span class="n">end</span> <span class="o">-</span> <span class="n">start</span><span class="p">;</span>
<span class="p">...</span>
<span class="n">time</span> <span class="o">=</span> <span class="n">ERTS_MONOTONIC_TO_USEC</span><span class="p">(</span><span class="n">time</span><span class="p">);</span>
<span class="k">if</span> <span class="p">(</span><span class="n">time</span> <span class="o">==</span> <span class="mi">0</span><span class="p">)</span>
    <span class="k">return</span> <span class="p">(</span><span class="n">Sint64</span><span class="p">)</span> <span class="mi">1</span><span class="p">;</span> <span class="cm">/* At least one reduction */</span>
<span class="cm">/* Currently two reductions per micro second */</span>
<span class="n">time</span> <span class="o">*=</span> <span class="p">(</span><span class="n">CONTEXT_REDS</span><span class="o">-</span><span class="mi">1</span><span class="p">)</span><span class="o">/</span><span class="mi">1000</span> <span class="o">+</span> <span class="mi">1</span><span class="p">;</span>
<span class="k">return</span> <span class="p">(</span><span class="n">Sint64</span><span class="p">)</span> <span class="n">time</span><span class="p">;</span>
</code></pre></div></div>

<p>And the process struct itself (<code class="language-plaintext highlighter-rouge">erl_process.h</code>) documents <code class="language-plaintext highlighter-rouge">fcalls</code>
precisely as the live budget:</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">Sint32</span> <span class="n">fcalls</span><span class="p">;</span>   <span class="cm">/* Number of reductions left to execute.
                  * Only valid for the current process while it
                  * is executing. */</span>
</code></pre></div></div>

<p>So: a reduction is BEAM’s unit of “work done,” inherited in name from
term-rewriting theory, made concrete as roughly one function/BIF call in
the interpreter, and used both to decide <em>when to preempt</em> (3.3 below) and
to convert real elapsed time into comparable scheduling accounting.</p>

<h3 id="33a-reduction-vs-safepoint">3.3a Reduction vs. safepoint</h3>

<p>They answer different questions, and that difference is exactly why the
comparison is useful:</p>

<ul>
  <li>A <strong>safepoint</strong> (Go, Part 1.3; JVM, Part 3.5) answers <em>“where in the
code is it valid to inspect/pause this thread’s state?”</em> — a spatial
question. Most instructions are <em>not</em> safe-points; the compiler/runtime
has to specially mark or detect the few that are, because in a
shared-heap, register-based execution model most points mid-instruction
don’t have a fully-known, GC-inspectable machine state.</li>
  <li>A <strong>reduction</strong> answers <em>“has this process done enough work that it
should yield?”</em> — a budget question, not a location question.</li>
</ul>

<p>The reason reductions don’t need a <em>separate</em> safepoint concept is that in
BEAM’s interpreter, <strong>every reduction boundary already is a valid
safepoint</strong> — process state (heap pointer, stack pointer, registers) is
always fully known and consistent between BEAM instructions, because the
interpreter loop (<code class="language-plaintext highlighter-rouge">process_main</code>, 3.2) never leaves execution in a
partially-updated state across an instruction dispatch. There’s no
“mid-instruction, a pointer is briefly stored as a raw integer” hazard
(Go’s <code class="language-plaintext highlighter-rouge">unsafe.Pointer</code>→<code class="language-plaintext highlighter-rouge">uintptr</code> case, Part 1.3) to guard against, because
there’s no equivalent unsafe low-level escape hatch in normal BEAM
bytecode execution the way there is in compiled machine code.</p>

<p>So the precise relationship is: <strong>in Go and the JVM, safe-points are a
sparse subset of execution points, specially constructed at compile time
because most points aren’t safe. In BEAM, every point between instructions
is already safe, so the only remaining question is scheduling policy — how
often to actually act on that safety — and that’s exactly what the
reduction counter answers.</strong> Reduction counting is the “when to act”
policy layered on top of an execution model where “where is it safe to
act” was never a hard problem to begin with. This is a direct consequence
of Part 3.2’s isolation constraint: shared, GC-managed memory is what
makes “where is it safe” hard in the first place, and BEAM opted out of
that at the memory-model level, not at the scheduling level.</p>

<h3 id="331-preemption-reduction-counting-with-the-actual-constant">3.3.1 Preemption: reduction counting, with the actual constant</h3>

<p><code class="language-plaintext highlighter-rouge">erl_vm.h</code> defines the budget directly:</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">#define CONTEXT_REDS 4000            </span><span class="cm">/* Swap process out after this number */</span><span class="cp">
</span></code></pre></div></div>

<p>(Note: this is the current source value. Older Erlang documentation and
blog posts commonly cite ~2000 reductions — that number is stale; always
check the constant in the version you’re targeting rather than trusting a
remembered figure, including this document a year from now.)</p>

<p>The decrement mechanism is the interpreter’s own reduction counter,
<code class="language-plaintext highlighter-rouge">FCALLS</code>, documented directly at its declaration in <code class="language-plaintext highlighter-rouge">process_main</code>:</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cm">/* Number of reductions left.  This function
 * returns to the scheduler when FCALLS reaches zero. */</span>
<span class="k">register</span> <span class="n">Sint</span> <span class="n">FCALLS</span> <span class="n">REG_fcalls</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
</code></pre></div></div>

<p>Every BEAM instruction (roughly: one function call, one comparable unit of
work) decrements <code class="language-plaintext highlighter-rouge">FCALLS</code>. When it hits zero, <code class="language-plaintext highlighter-rouge">process_main</code> returns
control to the scheduler, which calls <code class="language-plaintext highlighter-rouge">erts_schedule</code> with the reduction
count actually consumed:</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">reds_used</span> <span class="o">=</span> <span class="n">REDS_IN</span><span class="p">(</span><span class="n">c_p</span><span class="p">)</span> <span class="o">-</span> <span class="n">FCALLS</span><span class="p">;</span>
<span class="p">...</span>
<span class="n">c_p</span> <span class="o">=</span> <span class="n">erts_schedule</span><span class="p">(</span><span class="nb">NULL</span><span class="p">,</span> <span class="n">c_p</span><span class="p">,</span> <span class="n">reds_used</span><span class="p">);</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">erl_process.c</code> also derives several scheduler tuning constants directly
from <code class="language-plaintext highlighter-rouge">CONTEXT_REDS</code> — e.g. <code class="language-plaintext highlighter-rouge">ERTS_RUNQ_CHECK_BALANCE_REDS_PER_SCHED
(2000*CONTEXT_REDS)</code> for load-balancing cadence between run queues, and
<code class="language-plaintext highlighter-rouge">ERTS_PROC_MIN_CONTEXT_SWITCH_REDS_COST (CONTEXT_REDS/10)</code> — confirming
reductions are the scheduler’s universal unit of “how much work happened,”
not just a preemption trigger.</p>

<p>There is no signal, no stack map, no poll page: preemption here is a
plain integer counter checked as part of every instruction dispatch, cheap
because it’s already inline in the interpreter’s hot loop, not a bolted-on
external mechanism.</p>

<p><strong>Tracing it through a concrete example:</strong></p>

<div class="language-erlang highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">loop</span><span class="p">()</span> <span class="o">-&gt;</span> <span class="nf">loop</span><span class="p">().</span>

<span class="nf">main</span><span class="p">()</span> <span class="o">-&gt;</span>
    <span class="nb">spawn</span><span class="p">(</span><span class="k">fun</span> <span class="n">loop</span><span class="o">/</span><span class="mi">0</span><span class="p">),</span>
    <span class="nb">spawn</span><span class="p">(</span><span class="k">fun</span><span class="p">()</span> <span class="o">-&gt;</span> <span class="nn">io</span><span class="p">:</span><span class="nf">format</span><span class="p">(</span><span class="s">"still runs fine</span><span class="si">~n</span><span class="s">"</span><span class="p">)</span> <span class="k">end</span><span class="p">).</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">spawn(fun loop/0)</code> starts a process that never returns — the BEAM
equivalent of Go’s <code class="language-plaintext highlighter-rouge">for {}</code>. Unlike Go pre-1.14, this doesn’t hang the
system: <code class="language-plaintext highlighter-rouge">process_main</code>’s interpreter loop dispatches <code class="language-plaintext highlighter-rouge">loop/0</code>’s call to
itself as one reduction, decrementing <code class="language-plaintext highlighter-rouge">FCALLS</code> (Part 3.3.1) each time.
When <code class="language-plaintext highlighter-rouge">FCALLS</code> hits zero — after <code class="language-plaintext highlighter-rouge">CONTEXT_REDS</code> (4000, by default)
dispatched calls — <code class="language-plaintext highlighter-rouge">process_main</code> returns control to <code class="language-plaintext highlighter-rouge">erts_schedule</code>
(Part 3.5a), which puts the looping process back on the run queue and
picks the next ready process. The second <code class="language-plaintext highlighter-rouge">spawn</code>, printing “still runs
fine,” gets scheduled and runs to completion on the same core without
ever waiting on the infinite loop. No signal was sent to interrupt
<code class="language-plaintext highlighter-rouge">loop/0</code>; it was never running long enough at a stretch to need one — the
counter reaching zero <em>is</em> the interruption, already built into every
single call the loop makes.</p>

<p>Because each process owns its heap exclusively, <code class="language-plaintext highlighter-rouge">erl_gc.c</code> runs GC
per-process — a GC pause for one process is invisible to every other
process running concurrently on other scheduler threads. There is no
whole-VM stop-the-world equivalent to Go’s <code class="language-plaintext highlighter-rouge">stopTheWorldWithSema</code> (Part
1.3) or HotSpot’s global safepoint synchronization (Part 3.5.1): BEAM
never needs “every process frozen at once,” because nothing GC-relevant is
ever shared across processes to make that necessary.</p>

<h3 id="35-fault-model-as-a-consequence-not-a-philosophy">3.5 Fault model as a consequence, not a philosophy</h3>

<p>Because processes are isolated, a crash is just a process exiting — it
cannot corrupt another process’s state by construction. This is why “let
it crash” is viable: supervisors restart failed processes and the
remaining system is provably unaffected, because isolation was the
starting constraint everything else (per-process GC, reduction-counted
preemption, copy-on-send messaging) follows from, not an afterthought
layered on top.</p>

<h3 id="35a-erts_schedule-the-deschedulingreschedule-handoff">3.5a <code class="language-plaintext highlighter-rouge">erts_schedule</code>: the descheduling/reschedule handoff</h3>

<p>Read directly:
<a href="https://github.com/erlang/otp/blob/master/erts/emulator/beam/erl_process.c"><code class="language-plaintext highlighter-rouge">erts/emulator/beam/erl_process.c</code></a>,
<code class="language-plaintext highlighter-rouge">Process *erts_schedule(...)</code>.</p>

<div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">Process</span> <span class="o">*</span><span class="nf">erts_schedule</span><span class="p">(</span><span class="n">ErtsSchedulerData</span> <span class="o">*</span><span class="n">esdp</span><span class="p">,</span> <span class="n">Process</span> <span class="o">*</span><span class="n">p</span><span class="p">,</span> <span class="kt">int</span> <span class="n">calls</span><span class="p">)</span>
<span class="p">{</span>
    <span class="p">...</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">ERTS_USE_MODIFIED_TIMING</span><span class="p">())</span> <span class="p">{</span>
        <span class="n">context_reds</span> <span class="o">=</span> <span class="n">ERTS_MODIFIED_TIMING_CONTEXT_REDS</span><span class="p">;</span>
    <span class="p">}</span>
    <span class="k">else</span> <span class="p">{</span>
        <span class="n">context_reds</span> <span class="o">=</span> <span class="n">CONTEXT_REDS</span><span class="p">;</span>
    <span class="p">}</span>
    <span class="p">...</span>
    <span class="cm">/*
     * Clean up after the process being scheduled out.
     */</span>
    <span class="k">if</span> <span class="p">(</span><span class="o">!</span><span class="n">p</span><span class="p">)</span> <span class="p">{</span>  <span class="cm">/* NULL in the very first schedule() call */</span>
        <span class="n">is_normal_sched</span> <span class="o">=</span> <span class="o">!</span><span class="n">esdp</span><span class="p">;</span>
        <span class="p">...</span>
        <span class="n">rq</span> <span class="o">=</span> <span class="n">erts_get_runq_current</span><span class="p">(</span><span class="n">esdp</span><span class="p">);</span>
        <span class="n">actual_reds</span> <span class="o">=</span> <span class="n">reds</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
        <span class="n">erts_runq_lock</span><span class="p">(</span><span class="n">rq</span><span class="p">);</span>
    <span class="p">}</span>
    <span class="k">else</span> <span class="p">{</span>
</code></pre></div></div>

<p><strong>Takeaway:</strong> this is the function <code class="language-plaintext highlighter-rouge">process_main</code> (3.3.1) calls once
<code class="language-plaintext highlighter-rouge">FCALLS</code> hits zero — <code class="language-plaintext highlighter-rouge">erts_schedule</code> both cleans up the process being
descheduled and picks and returns the next one, in a single function (the
<code class="language-plaintext highlighter-rouge">if (!p)</code> branch is the bootstrap case with no previous process). The
<code class="language-plaintext highlighter-rouge">ERTS_USE_MODIFIED_TIMING()</code> branch means <code class="language-plaintext highlighter-rouge">CONTEXT_REDS</code> (4000) is not
always the literal number used — there’s a “modified timing” mode with a
different constant. The 4000 figure in 3.3.1 is the default, not an
unconditional constant.</p>

<p><strong>Open question:</strong> what <code class="language-plaintext highlighter-rouge">ERTS_USE_MODIFIED_TIMING()</code> gates, and the
next-process-selection logic inside <code class="language-plaintext highlighter-rouge">erts_schedule</code> past the cleanup
section shown above (priority levels, run-queue migration) — only the
function’s opening is covered here.</p>

<h3 id="36-checkpoint">3.6 Checkpoint</h3>

<p>Given <code class="language-plaintext highlighter-rouge">CONTEXT_REDS = 4000</code> and one reduction ≈ one function call: can a
single BEAM process still starve the scheduler the way a Go goroutine
could pre-1.14? Look at what triggers the check. (Answer: no in the same
way — the check is baked into every instruction dispatch inside
<code class="language-plaintext highlighter-rouge">process_main</code>’s interpreter loop, not gated behind reaching a function
call boundary the way Go’s old stack-bound check was; there is no BEAM
equivalent of <code class="language-plaintext highlighter-rouge">for {}</code> with literally zero dispatched instructions, since
even an infinite loop’s body is instructions being dispatched and
decrementing <code class="language-plaintext highlighter-rouge">FCALLS</code>. The one thing that <em>can</em> still block a scheduler
thread is a genuinely blocking NIF/native call that doesn’t yield control
back to the VM — the moral equivalent of a goroutine in a blocking syscall
without releasing its P.)</p>

<hr />

<h2 id="part-35--jvm-safepoints-the-mechanism-kotlin-sits-on-top-of-and-gos-real-point-of-contrast">Part 3.5 — JVM Safepoints (the mechanism Kotlin sits on top of, and Go’s real point of contrast)</h2>

<p>Sources read directly for this section:
<a href="https://github.com/openjdk/jdk/blob/master/src/hotspot/share/runtime/safepoint.hpp"><code class="language-plaintext highlighter-rouge">src/hotspot/share/runtime/safepoint.hpp</code></a>,
<a href="https://github.com/openjdk/jdk/blob/master/src/hotspot/share/runtime/safepointMechanism.hpp"><code class="language-plaintext highlighter-rouge">src/hotspot/share/runtime/safepointMechanism.hpp</code></a>,
openjdk/jdk, master branch. Also relevant as an applied-engineering account
(not a primary source, but a good “what this looks like in production”
companion): Sunny Srinidhi, <a href="https://medium.com/software-under-the-hood/under-the-hood-java-peak-safepoints-dd45af07d766">“Under the Hood: Java Peak Safepoints”</a>.</p>

<p>This section was missing entirely from the previous draft even though it’s
the actual mechanism Part 2.4 waved at, and it’s the correct point of
contrast for Go’s approach — not an optional aside.</p>

<h3 id="351-what-a-safepoint-is-precisely-in-hotspots-own-words">3.5.1 What a safepoint is, precisely, in HotSpot’s own words</h3>

<p><code class="language-plaintext highlighter-rouge">safepoint.hpp</code>’s header comment:</p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// The VMThread uses the SafepointSynchronize::begin/end</span>
<span class="c1">// methods to enter/exit a safepoint region. The begin method will roll</span>
<span class="c1">// all JavaThreads forward to a safepoint.</span>
<span class="c1">//</span>
<span class="c1">// JavaThreads must use the ThreadSafepointState abstraction (defined in</span>
<span class="c1">// thread.hpp) to indicate that that they are at a safepoint.</span>
<span class="c1">//</span>
<span class="c1">// The Mutex/Condition variable and ObjectLocker classes calls the enter/</span>
<span class="c1">// exit safepoint methods, when a thread is blocked/restarted. Hence, all</span>
<span class="c1">// mutex enter/exit points *must* be at a safepoint.</span>
</code></pre></div></div>

<p>And the three-state enum that drives the whole mechanism:</p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">enum</span> <span class="n">SynchronizeState</span> <span class="p">{</span>
    <span class="n">_not_synchronized</span> <span class="o">=</span> <span class="mi">0</span><span class="p">,</span>   <span class="c1">// Threads not synchronized at a safepoint. Keep this value 0.</span>
    <span class="n">_synchronizing</span>    <span class="o">=</span> <span class="mi">1</span><span class="p">,</span>   <span class="c1">// Synchronizing in progress</span>
    <span class="n">_synchronized</span>     <span class="o">=</span> <span class="mi">2</span>    <span class="c1">// All Java threads are running in native, blocked in OS</span>
                              <span class="c1">// or stopped at safepoint. VM thread and any NonJavaThread</span>
                              <span class="c1">// may be running.</span>
<span class="p">};</span>
</code></pre></div></div>

<p>This confirms the GC-adjacent framing directly: a safepoint isn’t
GC-specific in HotSpot — it’s the general mechanism the VM uses any time it
needs every Java thread frozen in a known state (GC, deoptimization,
biased-lock revocation, thread dumps, class redefinition). GC is the most
frequent consumer, not the only one.</p>

<h3 id="352-the-poll-page-mechanism--how-threads-notice-they-should-stop">3.5.2 The poll-page mechanism — how threads notice they should stop</h3>

<p>Unlike Go’s synchronous safe-points (stack-bound-check poisoning) or async
safe-points (signal delivery), HotSpot’s classic mechanism is <strong>memory
polling</strong>. From <code class="language-plaintext highlighter-rouge">safepointMechanism.hpp</code>:</p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">SafepointMechanism</span> <span class="o">:</span> <span class="k">public</span> <span class="n">AllStatic</span> <span class="p">{</span>
  <span class="p">...</span>
  <span class="k">static</span> <span class="kt">uintptr_t</span> <span class="n">_poll_page_armed_value</span><span class="p">;</span>
  <span class="k">static</span> <span class="kt">uintptr_t</span> <span class="n">_poll_page_disarmed_value</span><span class="p">;</span>
  <span class="k">static</span> <span class="n">address</span>   <span class="n">_polling_page</span><span class="p">;</span>
  <span class="p">...</span>
  <span class="k">struct</span> <span class="nc">ThreadData</span> <span class="p">{</span>
    <span class="k">volatile</span> <span class="kt">uintptr_t</span> <span class="n">_polling_word</span><span class="p">;</span>
    <span class="k">volatile</span> <span class="kt">uintptr_t</span> <span class="n">_polling_page</span><span class="p">;</span>
    <span class="p">...</span>
  <span class="p">};</span>

  <span class="c1">// Call this method to see if this thread should block for a safepoint</span>
  <span class="c1">// or process handshake.</span>
  <span class="k">static</span> <span class="kr">inline</span> <span class="kt">bool</span> <span class="nf">should_process</span><span class="p">(</span><span class="n">JavaThread</span><span class="o">*</span> <span class="kr">thread</span><span class="p">,</span> <span class="kt">bool</span> <span class="n">allow_suspend</span> <span class="o">=</span> <span class="nb">true</span><span class="p">);</span>
</code></pre></div></div>

<p>The JIT compiler emits, at method entry and loop back-edges, a memory read
from <code class="language-plaintext highlighter-rouge">_polling_page</code>. Normally this page is mapped read-only-but-armable
and the read is nearly free (a single load the CPU can pipeline away). To
request a safepoint, the VM thread <strong>unmaps or protects the polling page</strong>
(the “armed” state). The next thread that executes its poll instruction
triggers a trap — a real page fault the OS delivers as a signal/exception
— which HotSpot’s fault handler intercepts and redirects into safepoint
processing.</p>

<p>This is the crucial contrast with Go: HotSpot’s mechanism is cooperative
in mechanism (a thread must execute its own poll instruction to notice) but
made cheap and <em>effectively involuntary</em> by tying the poll to a hardware
trap instead of a branch — a thread genuinely cannot skip past an armed
poll once it executes one, but a thread that never reaches a poll point at
all (analogous to Go’s pre-1.14 tight-loop problem) still can’t be stopped.
This is exactly the “time to safe-point” (TTSP) latency the linked Medium
piece measures in production — the gap between the VM thread arming the
page and the last straggler thread actually hitting its poll and blocking.
JIT-compiled code has poll points at loop back-edges and method returns/calls
(the compiler decides placement per-method based on loop structure);
interpreter frames check a flag on every bytecode dispatch, which is why
interpreted code reaches safepoints faster but runs slower generally.</p>

<h3 id="352a-exact-poll-placement-and-the-safepointblocked-distinction">3.5.2a Exact poll placement, and the safepoint/blocked distinction</h3>

<p>Additional sources for this subsection (secondary, but precise and
attributed): Gil Tene’s account on the mechanical-sympathy mailing list, as
reproduced at <a href="http://psy-lob-saw.blogspot.com/2015/12/safepoints.html">Nitsan Wakart, “Safepoints: Meaning, Side Effects and Overheads”</a>;
also <a href="http://chriskirk.blogspot.com/2013/09/what-is-java-safepoint.html">chriskirk.blogspot.com, “What is a Java Safepoint?”</a>
and <a href="http://jpbempel.blogspot.com/2013/03/safety-first-safepoints.html">jpbempel.blogspot.com, “Safety First: Safepoints”</a>.</p>

<p>HotSpot doesn’t poll unconditionally everywhere — poll placement is a
deliberate tradeoff, because “on top of the cost of the flag check itself,
maintaining a ‘known state’ adds significant complexity to the
implementation of certain optimizations,” so keeping safepoints further
apart widens the scope for optimization. The actual poll locations:</p>

<ul>
  <li>Effectively <strong>between any two bytecodes</strong> while running in the interpreter</li>
  <li>On <strong>non-counted loop back-edges</strong> in C1/C2-compiled code (a “counted”
loop — bounds known at compile time — can skip the poll, since the JIT
can prove it terminates)</li>
  <li>At <strong>method entry/exit</strong> in compiled code (entry on Zing, exit on
OpenJDK) — and the compiler removes these polls entirely when a method
gets inlined, since the caller’s own poll covers it</li>
</ul>

<p>You can find the actual poll instructions in <code class="language-plaintext highlighter-rouge">-XX:+PrintAssembly</code> output
by searching for <code class="language-plaintext highlighter-rouge">{poll}</code> or <code class="language-plaintext highlighter-rouge">{poll return}</code> in the instruction comments —
this is directly checkable on a running JVM, not just a claim from a blog.</p>

<p>A second correction worth stating precisely: <strong>“at a safepoint” does not
mean “blocked.”</strong> JNI code, for instance, runs <em>at</em> a safepoint (its Java
state representation is frozen and safe to inspect) without being
descheduled. “Being blocked always happens at a safepoint, but being at a
safepoint doesn’t require being blocked” — this is exactly analogous to
Go’s distinction between “blocked safe-points” and the state a goroutine
sits in during a syscall (Part 1.3, category 1): the state is
GC-inspectable without the thread being paused by anything external.</p>

<p>Two further points that generalize the model correctly:</p>

<ul>
  <li><strong>Global vs. per-thread safepoints.</strong> The page-protection trick (3.5.2)
is a <em>global</em> mechanism — OpenJDK brings all threads to a safepoint
together for STW work. Some JVMs (Azul’s Zing, via what it calls
“Checkpoints”) can bring an <em>individual</em> thread to a safepoint-like state
without a global pause, for short per-thread operations. This is the
same axis Go and BEAM sit on opposite ends of: Go’s STW GC phases are
global by necessity (Part 1.3), BEAM’s per-process GC is inherently
per-thread already (Part 3) — Zing’s Checkpoints are HotSpot borrowing
the BEAM-shaped idea onto a shared-heap runtime.</li>
  <li><strong>Unsafe code and safepoint density.</strong> Because a safepoint can occur
between any two bytecodes by default, JNI/Unsafe code that wants to run
long stretches without a safepoint (e.g. a large <code class="language-plaintext highlighter-rouge">Unsafe.copyMemory</code>)
must opt in to periodic safepoint opportunities itself — otherwise a
long native call can inflate the “time to safe-point” the VM thread
waits on before starting STW work, which is precisely the TTSP latency
the linked “Java Peak Safepoints” article measures in production.</li>
</ul>

<h3 id="352b-safepointcpps-begin-function">3.5.2b <code class="language-plaintext highlighter-rouge">safepoint.cpp</code>’s <code class="language-plaintext highlighter-rouge">begin()</code> function</h3>

<p>Read directly:
<a href="https://github.com/openjdk/jdk/blob/master/src/hotspot/share/runtime/safepoint.cpp"><code class="language-plaintext highlighter-rouge">src/hotspot/share/runtime/safepoint.cpp</code></a>,
<code class="language-plaintext highlighter-rouge">SafepointSynchronize::begin()</code>.</p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kt">void</span> <span class="n">SafepointSynchronize</span><span class="o">::</span><span class="n">begin</span><span class="p">()</span> <span class="p">{</span>
  <span class="n">assert</span><span class="p">(</span><span class="n">Thread</span><span class="o">::</span><span class="n">current</span><span class="p">()</span><span class="o">-&gt;</span><span class="n">is_VM_thread</span><span class="p">(),</span> <span class="s">"Only VM thread may execute a safepoint"</span><span class="p">);</span>
  <span class="p">...</span>
  <span class="n">log_trace</span><span class="p">(</span><span class="n">safepoint</span><span class="p">)(</span><span class="s">"Blocking threads from starting/exiting"</span><span class="p">);</span>
  <span class="n">Threads_lock</span><span class="o">-&gt;</span><span class="n">lock</span><span class="p">();</span>
  <span class="p">...</span>
  <span class="n">_waiting_to_block</span> <span class="o">=</span> <span class="n">nof_threads</span><span class="p">;</span>
  <span class="p">...</span>
  <span class="n">log_trace</span><span class="p">(</span><span class="n">safepoint</span><span class="p">)(</span><span class="s">"Arming safepoint using %s wait barrier"</span><span class="p">,</span> <span class="n">_wait_barrier</span><span class="o">-&gt;</span><span class="n">description</span><span class="p">());</span>
  <span class="n">arm_safepoint</span><span class="p">();</span>
  <span class="p">...</span>
  <span class="k">if</span> <span class="p">(</span><span class="n">SafepointTimeout</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">safepoint_limit_time</span> <span class="o">=</span> <span class="n">SafepointTracing</span><span class="o">::</span><span class="n">start_of_safepoint</span><span class="p">()</span>
        <span class="o">+</span> <span class="p">(</span><span class="n">jlong</span><span class="p">)(</span><span class="n">SafepointTimeoutDelay</span> <span class="o">*</span> <span class="n">NANOSECS_PER_MILLISEC</span><span class="p">);</span>
  <span class="p">}</span>
</code></pre></div></div>

<p>and further down, what happens if a thread doesn’t reach the poll in time:</p>

<div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="k">if</span> <span class="p">(</span><span class="n">AbortVMOnSafepointTimeout</span> <span class="o">&amp;&amp;</span> <span class="p">(</span><span class="n">os</span><span class="o">::</span><span class="n">elapsedTime</span><span class="p">()</span> <span class="o">*</span> <span class="n">MILLIUNITS</span> <span class="o">&gt;</span> <span class="n">AbortVMOnSafepointTimeoutDelay</span><span class="p">))</span> <span class="p">{</span>
    <span class="k">for</span> <span class="p">(</span><span class="n">JavaThreadIteratorWithHandle</span> <span class="n">jtiwh</span><span class="p">;</span> <span class="n">JavaThread</span> <span class="o">*</span><span class="n">cur_thread</span> <span class="o">=</span> <span class="n">jtiwh</span><span class="p">.</span><span class="n">next</span><span class="p">();</span> <span class="p">)</span> <span class="p">{</span>
      <span class="k">if</span> <span class="p">(</span><span class="n">cur_thread</span><span class="o">-&gt;</span><span class="n">safepoint_state</span><span class="p">()</span><span class="o">-&gt;</span><span class="n">is_running</span><span class="p">())</span> <span class="p">{</span>
        <span class="n">VMError</span><span class="o">::</span><span class="n">set_safepoint_timed_out_thread</span><span class="p">(</span><span class="n">cur_thread</span><span class="p">);</span>
        <span class="k">if</span> <span class="p">(</span><span class="o">!</span><span class="n">os</span><span class="o">::</span><span class="n">signal_thread</span><span class="p">(</span><span class="n">cur_thread</span><span class="p">,</span> <span class="n">SIGILL</span><span class="p">,</span> <span class="s">"blocking a safepoint"</span><span class="p">))</span> <span class="p">{</span>
          <span class="k">break</span><span class="p">;</span>
        <span class="p">}</span>
</code></pre></div></div>

<p><strong>Takeaway:</strong> <code class="language-plaintext highlighter-rouge">begin()</code> grabs <code class="language-plaintext highlighter-rouge">Threads_lock</code> so no thread can start or exit
mid-arming, then calls <code class="language-plaintext highlighter-rouge">arm_safepoint()</code> (the page-protect operation from
3.5.2), and waits, tracked via <code class="language-plaintext highlighter-rouge">_waiting_to_block</code>. HotSpot also ships a
built-in watchdog (<code class="language-plaintext highlighter-rouge">SafepointTimeout</code>/<code class="language-plaintext highlighter-rouge">AbortVMOnSafepointTimeoutDelay</code>)
that, past a configurable delay, forcibly sends <code class="language-plaintext highlighter-rouge">SIGILL</code> to whichever
thread hasn’t reached its poll — a thread can, in production, simply fail
to reach a safepoint in reasonable time, and the VM’s answer is to crash
that thread deliberately rather than hang forever. This is the closest
direct JVM analogue to the Go pre-1.14 failure mode (Part 1.3): HotSpot
has an explicit, source-visible failsafe for it, while Go’s answer was to
change the preemption mechanism so the stuck case couldn’t occur at all.</p>

<p><strong>Open question:</strong> what <code class="language-plaintext highlighter-rouge">arm_safepoint()</code>’s wait-barrier implementation
does at the OS level (mentioned as <code class="language-plaintext highlighter-rouge">_wait_barrier-&gt;description()</code> but not
read here) — the next step to fully trace the mechanism from “STW
requested” to “all threads confirmed parked.”</p>

<h3 id="353-why-go-didnt-reuse-this-approach">3.5.3 Why Go didn’t reuse this approach</h3>

<p>Go’s runtime doesn’t have a JIT emitting per-loop poll instructions the way
HotSpot’s C1/C2 compilers do — Go compiles ahead-of-time, and the
project’s own tried-and-rejected loop-back-edge check (Part 1.3) is
functionally the <em>interpreter-style</em> version of HotSpot’s mechanism minus
the page-fault trick: an explicit conditional branch at every loop
iteration, not a trap-backed memory read. That 7.8% regression is
consistent with paying interpreter-tier overhead in what should be
compiled-tier code — HotSpot avoids this cost by making the poll a load
the branch predictor and speculative execution can mostly hide, and by only
weaponizing it into an actual trap at the moment a safepoint is requested,
not on every iteration unconditionally. Go’s signal-based async safe-points
sidestep the problem differently: instead of a poll executed by the
goroutine itself, the OS interrupts the thread from outside, at zero
steady-state cost, and the runtime only pays for stack-map lookups on the
rare occasions a preemption is actually requested.</p>

<h3 id="354-checkpoint">3.5.4 Checkpoint</h3>

<p>Both HotSpot’s polling and Go’s pre-1.14 back-edge checks impose steady-state
cost on every loop iteration. What’s the qualitative difference in <em>how</em>
they impose it, and why does that explain why one shipped and the other
was measured at 7.8% and rejected? (Answer: HotSpot’s poll is a single
memory load against a page the CPU can usually predict/cache — cheap even
paid unconditionally, and JIT-compiled so it’s tuned per hot loop; Go’s
back-edge check was a comparison-and-branch against a stack guard variable,
inserted by a non-JIT ahead-of-time compiler across <em>all</em> loops uniformly,
without HotSpot’s runtime profiling to place it selectively.)</p>

<hr />

<h2 id="part-4--comparison-the-safe-point-problem-side-by-side">Part 4 — Comparison: the safe-point problem, side by side</h2>

<table>
  <thead>
    <tr>
      <th> </th>
      <th>Go</th>
      <th>Kotlin (JVM)</th>
      <th>BEAM</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Concurrency unit</td>
      <td>Goroutine (runtime-managed stack)</td>
      <td>Coroutine (compiler-transformed continuation)</td>
      <td>Process (VM-managed, isolated heap)</td>
    </tr>
    <tr>
      <td>Who controls switching</td>
      <td>Go runtime scheduler (GMP)</td>
      <td>Compiler-generated state machine + dispatcher</td>
      <td>BEAM scheduler (per-core)</td>
    </tr>
    <tr>
      <td>Preemption style</td>
      <td>Non-cooperative (signal, SIGURG) constrained by safe-points</td>
      <td>Cooperative (suspend points only)</td>
      <td>Non-cooperative (reduction counting)</td>
    </tr>
    <tr>
      <td>Memory model</td>
      <td>Shared heap across goroutines</td>
      <td>Shared heap across coroutines (same JVM heap)</td>
      <td>Isolated heap per process</td>
    </tr>
    <tr>
      <td>GC scope</td>
      <td>Whole-process; STW phases need all goroutines at a safe-point</td>
      <td>Whatever JVM GC is running; safe-points are a separate JIT-polling mechanism unrelated to coroutines</td>
      <td>Per-process; one process’s GC pause never affects others</td>
    </tr>
    <tr>
      <td>What can go wrong if preempted incorrectly</td>
      <td>GC sees stale/invalid pointer, collects live object</td>
      <td>N/A — nothing is force-preempted</td>
      <td>N/A — no shared state to corrupt</td>
    </tr>
    <tr>
      <td>Fault containment</td>
      <td>A panic in one goroutine can crash the process unless recovered</td>
      <td>An exception in one coroutine can propagate per structured-concurrency scope</td>
      <td>A crash is isolated to one process by construction</td>
    </tr>
  </tbody>
</table>

<p><strong>The one-sentence unification:</strong> the safe-point problem exists exactly
where preemption is non-cooperative <em>and</em> memory is shared — Go has both
and pays for it with stack maps and signal handling; BEAM has non-cooperative
preemption but no shared memory, so no safe-point problem exists; Kotlin has
shared memory but no non-cooperative preemption of coroutines, so it also
sidesteps the problem, at the cost of coroutines being unable to preempt
themselves out of a runaway loop.</p>

<hr />

<h2 id="part-5--graalvm-does-its-safepoint-mechanism-actually-differ-from-hotspots">Part 5 — GraalVM: does its safepoint mechanism actually differ from HotSpot’s?</h2>

<p>Source read directly:
<a href="https://github.com/oracle/graal/blob/master/substratevm/src/com.oracle.svm.core/src/com/oracle/svm/core/thread/Safepoint.java"><code class="language-plaintext highlighter-rouge">oracle/graal</code> → <code class="language-plaintext highlighter-rouge">substratevm/src/com.oracle.svm.core/.../thread/Safepoint.java</code></a>.</p>

<p>GraalVM Native Image (SubstrateVM) does ahead-of-time compilation of JVM
bytecode to a native binary, replacing HotSpot entirely. It changes startup
time and memory footprint substantially, and removes JIT-recompilation-
triggered safepoints (no JIT running concurrently). It does <strong>not</strong> change
the Kotlin coroutine suspension model — the CPS transform happens in the
Kotlin compiler before GraalVM ever sees bytecode (Part 2). But the
safepoint mechanism itself — the thing Part 3.5 covered for HotSpot — is
genuinely different in SubstrateVM, not just a reimplementation of the same
idea, which is worth being precise about since it’s easy to assume “still
the JVM, so still a poll page.”</p>

<p><strong><code class="language-plaintext highlighter-rouge">Safepoint.java</code>’s own class-level doc comment, quoted because the
mechanism it describes is the whole finding:</strong></p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cm">/**
 * Manages the initiation of safepoints. A safepoint is a global state where all Java threads,
 * except one, are paused so that invasive operations (such as a garbage collection) can execute
 * without interferences.
 *
 * When a safepoint is requested, one Java thread (the master) acquires the
 * {@link VMThreads#SAFEPOINT_MUTEX}. The master notifies all other threads about the pending
 * safepoint by modifying each thread's {@link SafepointCheckCounter} thread-local.
 *
 * Each Java threads periodically checks the value of its {@link SafepointCheckCounter}. If a
 * safepoint is pending, the thread enters the safepoint slowpath and blocks on the mutex that the
 * master is holding.
 */</span>
</code></pre></div></div>

<p>And the actual arming operation, which negates rather than page-protects:</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">value</span> <span class="o">=</span> <span class="nc">SafepointCheckCounter</span><span class="o">.</span><span class="na">getVolatile</span><span class="o">(</span><span class="n">thread</span><span class="o">);</span>
<span class="o">}</span> <span class="k">while</span> <span class="o">(!</span><span class="nc">SafepointCheckCounter</span><span class="o">.</span><span class="na">compareAndSet</span><span class="o">(</span><span class="n">thread</span><span class="o">,</span> <span class="n">value</span><span class="o">,</span> <span class="o">-</span><span class="n">value</span><span class="o">));</span>
</code></pre></div></div>

<p><strong>Takeaway:</strong> HotSpot arms a safepoint by protecting a shared
<em>memory page</em> (Part 3.5.2) — every thread’s poll is a load against the same
address, and the trap is a hardware page fault. SubstrateVM arms a
safepoint by <strong>negating each thread’s own counter value</strong> via CAS — there
is no shared page, no page fault, no signal involved in the common path at
all. Each compiled method decrements its thread-local
<code class="language-plaintext highlighter-rouge">SafepointCheckCounter</code> periodically (mirroring HotSpot’s loop-back-edge/
method-entry poll <em>placement</em> from Part 3.5.2a, but checking a counter
going negative instead of reading a protected page); when a thread notices
its own counter went negative, it takes the slowpath and blocks on
<code class="language-plaintext highlighter-rouge">SAFEPOINT_MUTEX</code>. This is architecturally closer to BEAM’s reduction
counting (Part 3.3) than to HotSpot’s own poll-page trap — a per-thread
software counter, not a shared trap-triggering memory region — even though
SubstrateVM is still fundamentally a JVM-semantics runtime, not a
process-isolated one. It’s a genuine mechanism swap under the same
“safepoint” name, not a cosmetic difference.</p>

<p><strong>Open question:</strong> why Oracle’s SubstrateVM engineers chose a per-thread
counter over reusing HotSpot’s poll-page approach — the file doesn’t
state the rationale. Plausible candidates (avoiding <code class="language-plaintext highlighter-rouge">mprotect</code> syscalls
per arm/disarm cycle, better behavior under isolates/multi-tenant native
images) are not confirmed by the source.</p>

<p><strong>Checkpoint:</strong> if your bottleneck is coroutine scheduling latency, does
GraalVM help? (Answer: no — that’s a compile-time property of the Kotlin
coroutine transform, orthogonal to which VM executes the resulting
bytecode.) Separately: does GraalVM’s safepoint mechanism reduce
“time to safepoint” compared to HotSpot’s for the same reason BEAM’s does?
(Answer: not for the same reason — BEAM’s advantage is structural, no
shared memory to protect at all; SubstrateVM still has a shared heap and
still needs a global stop, it’s only changed <em>how</em> each thread notices
the request, not the fact that all threads must eventually be stopped
together.)</p>

<hr />

<h2 id="part-6--the-gc-algorithms-behind-the-flags">Part 6 — The GC algorithms behind the flags</h2>

<p>The copy of <em>The Garbage Collection Handbook</em> at <code class="language-plaintext highlighter-rouge">~/Documents/Books_on_vm/</code>
is the <strong>2023 second edition</strong>, and it covers G1, ZGC, and Shenandoah by
name with dedicated sections. What follows is read directly from that PDF
(page numbers are the book’s own page numbers, printed in each page
header — the PDF file’s own page index has a fixed +37 offset from these,
front matter accounts for the difference).</p>

<h3 id="61-the-tricolor-abstraction-base-algorithm-before-the-formal-invariant">6.1 The tricolor abstraction: base algorithm before the formal invariant</h3>

<p>Source: Robert Nystrom, <a href="https://craftinginterpreters.com/garbage-collection.html"><em>Crafting Interpreters</em>, “Garbage Collection”</a>
(also read in this research thread — see <code class="language-plaintext highlighter-rouge">003_research.md</code> in this
directory). Nystrom’s chapter gives the base mark-sweep tricolor algorithm
in intuitive terms, before the GC Handbook’s formal correctness theorem
below:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1. Start off with all objects white.
2. Find all the roots and mark them gray.
3. Repeat as long as there are still gray objects:
    a. Pick a gray object. Turn any white objects that the object
       mentions to gray.
    b. Mark the original gray object black.
</code></pre></div></div>

<p>The “gray wavefront” image: roots turn gray, each gray object’s neighbors
turn gray while the object itself turns black, and the wavefront advances
through the graph leaving reached (black) objects behind it, sweeping
white-and-untouched objects up as garbage once no gray objects remain.</p>

<p><img src="/img/posts/concurrency-and-gc_tricolor-trace.png" alt="tricolor mark-sweep wavefront" style="width: 100%; object-fit: contain;" /></p>

<p><em>Image: Robert Nystrom, Crafting Interpreters, “Garbage Collection”
(craftinginterpreters.com/garbage-collection.html), used here per the
site’s stated terms for educational reference.</em></p>

<h3 id="61a-the-formal-version-of-the-same-invariant">6.1a The formal version of the same invariant</h3>

<p>The Go runtime’s own version of this invariant, from
<a href="https://github.com/golang/go/blob/master/src/runtime/mgc.go"><code class="language-plaintext highlighter-rouge">src/runtime/mgc.go</code></a>,
<code class="language-plaintext highlighter-rouge">gcStart</code>’s STW-to-mark-phase transition — the full sequence, not just the
one line:</p>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code>	<span class="c">// Enter concurrent mark phase and enable</span>
	<span class="c">// write barriers.</span>
	<span class="c">//</span>
	<span class="c">// Because the world is stopped, all Ps will</span>
	<span class="c">// observe that write barriers are enabled by</span>
	<span class="c">// the time we start the world and begin</span>
	<span class="c">// scanning.</span>
	<span class="c">//</span>
	<span class="c">// Write barriers must be enabled before assists are</span>
	<span class="c">// enabled because they must be enabled before</span>
	<span class="c">// any non-leaf heap objects are marked. Since</span>
	<span class="c">// allocations are blocked until assists can</span>
	<span class="c">// happen, we want to enable assists as early as</span>
	<span class="c">// possible.</span>
	<span class="n">setGCPhase</span><span class="p">(</span><span class="n">_GCmark</span><span class="p">)</span>

	<span class="n">gcBgMarkPrepare</span><span class="p">()</span> <span class="c">// Must happen before assists are enabled.</span>
	<span class="n">gcPrepareMarkRoots</span><span class="p">()</span>

	<span class="c">// Mark all active tinyalloc blocks. Since we're</span>
	<span class="c">// allocating from these, they need to be black like</span>
	<span class="c">// other allocations. The alternative is to blacken</span>
	<span class="c">// the tiny block on every allocation from it, which</span>
	<span class="c">// would slow down the tiny allocator.</span>
	<span class="n">gcMarkTinyAllocs</span><span class="p">()</span>

	<span class="c">// At this point all Ps have enabled the write</span>
	<span class="c">// barrier, thus maintaining the no white to</span>
	<span class="c">// black invariant. Enable mutator assists to</span>
	<span class="c">// put back-pressure on fast allocating</span>
	<span class="c">// mutators.</span>
	<span class="n">atomic</span><span class="o">.</span><span class="n">Store</span><span class="p">(</span><span class="o">&amp;</span><span class="n">gcBlackenEnabled</span><span class="p">,</span> <span class="m">1</span><span class="p">)</span>
</code></pre></div></div>

<p>Read top to bottom, this is the actual order of operations at the start
of a mark phase: (1) <code class="language-plaintext highlighter-rouge">setGCPhase(_GCmark)</code> flips the global phase while
the world is still stopped, so every P will see the new phase the instant
it resumes; (2) <code class="language-plaintext highlighter-rouge">gcBgMarkPrepare()</code> and <code class="language-plaintext highlighter-rouge">gcPrepareMarkRoots()</code> set up the
background mark workers and root-scanning job queue <em>before</em> anything is
allowed to mark, per the comment’s own stated ordering constraint (write
barriers before assists, assists before marking); (3) <code class="language-plaintext highlighter-rouge">gcMarkTinyAllocs()</code>
retroactively blackens small allocations already carved out of the tiny
allocator, so they don’t need individual barrier coverage; (4) only then
does <code class="language-plaintext highlighter-rouge">atomic.Store(&amp;gcBlackenEnabled, 1)</code> actually arm the write barrier
Part 1.3a’s <code class="language-plaintext highlighter-rouge">mbarrier.go</code> checks — the comment’s “no white to black
invariant” claim is true starting at this exact store, not before it.
Everything above this line is setup that must complete first because the
invariant would be violated if the barrier were armed before roots were
prepared.</p>

<p><strong>§15.1, “The tricolour abstraction, revisited”</strong> (p.331) restates this
exact comment as a general, two-condition correctness theorem, attributed
to Wilson [1994]:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Condition 1: the mutator stores a pointer to a white object into a black object, and
Condition 2: all paths from any grey objects to that white object are destroyed.
</code></pre></div></div>

<p><strong>Takeaway:</strong> an object only gets incorrectly collected if
<em>both</em> conditions hold simultaneously — a black object gains a hidden
white pointer, <em>and</em> every path the collector could have used to
rediscover that white object independently gets cut. Break either
condition and the collector stays correct. This immediately generalizes
into two named invariants, quoted directly:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>The weak tricolour invariant: All white objects pointed to by a black
object are grey protected (that is, reachable from some grey object,
either directly or through a chain of white objects).

The strong tricolour invariant: There are no pointers from black
objects to white objects.
</code></pre></div></div>

<p><strong>Takeaway:</strong> Go’s hybrid barrier (Yuasa deletion + Dijkstra insertion) is
an example of a barrier that enforces something between these two — Go’s
comment said “no white to black,” which is the strong invariant stated
for Go’s specific non-moving collector. Non-moving collectors like Go’s
can get away with only the weak invariant (“white pointers in black
objects are not a problem because their grey protected white targets are
eventually shaded”), while concurrent copying collectors must preserve
the strong invariant, because a moving collector discards the fromspace
white copy entirely at cycle end — a black tospace object still pointing
at a discarded fromspace address is a dangling pointer, not just a late
mark. This is the precise reason ZGC/Shenandoah (both moving) and Go
(non-moving) need structurally different barrier strength.</p>

<h3 id="62-116--the-vendor-agnostic-version-of-part-35">6.2 §11.6 — the vendor-agnostic version of Part 3.5</h3>

<p><strong>§11.6, “GC safe-points and mutator suspension”</strong> (p.198) makes a
distinction Part 3.5 didn’t have a name for: <strong>GC safe-points vs. GC
check-points.</strong></p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Many systems make the opposite choice and only allow garbage collection
at certain restricted safe-points, and only produce maps for those
points. The minimal set of safe-points needed for correctness includes
each allocation ... and each call of a routine in which there may be
allocation or which may cause the thread to suspend in a wait.
</code></pre></div></div>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Since these additional safe-points do not do anything that actually can
trigger a garbage collection, they need to have an added check for
whether garbage collection is needed/requested, so we call them GC
check-points.
</code></pre></div></div>

<p><strong>Takeaway:</strong> a safe-point is <em>any</em> point where the machine
state is GC-inspectable; a check-point is the (much smaller) subset where
the runtime has also inserted code to actually test “should I stop right
now.” HotSpot’s poll-page instructions (Part 3.5.2) are check-points in
this vocabulary — they’re placed at loop back-edges and method entries,
which the book independently identifies as the correct minimal placement
(“there needs to be a safe-point in each loop; a simple rule is to place a
safe-point at each backwards branch in a function… in addition there
needs to be a safe-point in each function entry or each return”). This is
the same placement rule Gil Tene’s account (Part 3.5.2a) gave for HotSpot
specifically — the book states it as the general principle HotSpot (and
Go’s old back-edge check, and SubstrateVM’s counter) are all independent
instances of.</p>

<p>The book also names the exact two mechanisms this document found in two
different runtimes and treats them as one general dichotomy, citing
Agesen [1998]:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Agesen [1998] compared two ways of causing a thread to suspend at a
GC-point. One is polling ... The other technique is patching, which
involves modifying the code at the next GC-point(s) of the thread.
</code></pre></div></div>

<p><strong>Takeaway:</strong> HotSpot’s poll-page (Part 3.5.2) is “polling.”
Go’s pre-1.14 stack-bound-check poisoning (Part 1.3) is closer to
“patching” in spirit — the runtime rewrites <code class="language-plaintext highlighter-rouge">stackguard0</code> so the <em>existing</em>
bound check becomes a trap, rather than adding a new check. Go’s post-1.14
<code class="language-plaintext highlighter-rouge">SIGURG</code> mechanism is neither in Agesen’s original two-way split — it’s a
third strategy (async external interrupt) the 1998 taxonomy predates,
which is a useful thing to know precisely: SIGURG isn’t a variant of
polling or patching, it’s a genuinely later addition to the design space.</p>

<h3 id="63-g1--the-actual-phase-list-not-just-the-region-idea">6.3 G1 — the actual phase list, not just the region idea</h3>

<p><strong>§16.5, “Garbage-First: collecting young and old regions”</strong> (p.354)
gives OpenJDK 20’s actual mixed-collection phase sequence, not just the
region-prioritization idea Part 6’s original version described. Quoting
the phase list directly (abridged):</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Start: This stop-the-world phase is piggy-backed on a 'young only'
collection that seeds the roots for concurrent marking. ... G1 sets a
Top At Mark Start (TAMS) variable for each region; any objects allocated
at addresses above the TAMS for a region will be considered implicitly
live/marked.

Concurrent Mark From Roots: ... using a snapshot-at-the-beginning
algorithm, with mutators using a deletion barrier ... G1 marking threads
... Each thread first claims a region to mark by atomically advancing a
global 'finger' to the right.
</code></pre></div></div>

<p><strong>Takeaway:</strong> TAMS is the mechanism that lets G1 avoid
marking newly-allocated objects at all — anything allocated after
marking starts in a region is <em>assumed</em> live by address comparison alone,
no barrier or scan needed for it. This is a genuinely different strategy
from Go’s approach (Part 1.3a), where new allocations are marked black
immediately via <code class="language-plaintext highlighter-rouge">gcBlackenEnabled</code>-gated behavior — G1’s TAMS threshold
achieves a similar goal (don’t waste work on new objects) through a
different mechanism (an address comparison per region, not a global
allocation-color flag). The book also names G1’s barrier as a <strong>deletion</strong>
barrier specifically (snapshot-at-the-beginning), which is the Yuasa half
only, not Go’s hybrid Yuasa+Dijkstra combination — a real algorithmic
difference between the two collectors’ barriers not visible from the JEP
alone.</p>

<h3 id="64-zgc--colored-pointers-confirmed-with-the-actual-bit-layout">6.4 ZGC — colored pointers, confirmed with the actual bit layout</h3>

<p><strong>§17.5</strong> describes ZGC [Lidén, 2018] and gives the actual pointer bit
layout (Figure 17.4) rather than just naming “colored pointers” as a
concept:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ZGC uses tagged pointers and self-healing load barriers. ... A group of
four higher-order bits determine a colour. The four bits are named F
(finalisable), R (relocated), M1 and M0 (marked). A given pointer will
have only one of R, M0 and M1 set ... At any given time, only one of R,
M0 or M1 is the good colour; loading pointers of other colours forces a
load barrier slow path.
</code></pre></div></div>

<p><strong>Takeaway:</strong> “colored pointer” isn’t metaphorical — it’s four specific
bits (F/R/M0/M1) living in the unused high bits of a 64-bit pointer, on a
system where the actual address only needs 47 bits (the address bits span
roughly bit 46 down to 0, leaving room above for tag bits). Self-healing:
when a thread loads a pointer with a stale colour, the load barrier fixes
the colour in place via the barrier’s slow path, so subsequent loads of
that same memory location by the same thread don’t re-trigger the slow
path. This is a materially different mechanism from both Go’s write
barrier (fires on writes, not reads) and G1’s deletion barrier (also
write-side) — ZGC’s barrier fires on reads.</p>

<h3 id="65-shenandoah">6.5 Shenandoah</h3>

<p><strong>§17.5, “Shenandoah”</strong> (p.401) gives both the algorithm and, notably, a
sentence where the cited authors question their own earlier design
argument:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>In 2016 Flood et al. argued that programs such as web caches hold onto
objects just long enough to defeat generational collectors so, instead,
Shenandoah focuses its effort on regions with fewer live objects...
They also argued that generational collection requires some kind of
remembered set... However, it is not clear that these concerns still
hold and implementation of a generational version of Shenandoah is in
progress.
</code></pre></div></div>

<p><strong>Takeaway:</strong> as of 2023, Shenandoah’s own foundational 2016 design
argument (why not to be generational) is under active reconsideration by
its own authors, with a generational variant in progress — a different
picture than a static “Shenandoah is non-generational, ZGC is
non-generational” fact would give; the field is mid-revision on this
point, not settled.</p>

<p>The self-healing load barrier itself, given as actual pseudocode
(Algorithm 17.12), is worth comparing line-by-line against ZGC’s:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Read(src, i):
    addr ← &amp;src[i]
    obj ← *addr
    if not isGCactive()             /* fast path: accesses thread-local flag */
        return obj
    if not isInCollectionSet(obj)
        return obj
    fwd = resolveForwardee(obj)     /* access forwarding pointer, if one */
    if obj ≠ fwd
        CAS(addr, fwd, obj)         /* self-heal */
        return fwd
</code></pre></div></div>

<p><strong>Takeaway:</strong> Shenandoah’s self-healing works by CAS-ing the
<em>forwarding pointer</em> back into the original slot once discovered — a
different self-healing mechanism than ZGC’s colored-pointer remap, but the
same goal (pay the barrier cost once per location, not once per access).
The <code class="language-plaintext highlighter-rouge">isGCactive()</code> fast-path check is functionally the same shape as G1’s
<code class="language-plaintext highlighter-rouge">gcBlackenEnabled</code> and Go’s <code class="language-plaintext highlighter-rouge">gcphase == _GCmark</code> gating (Part 1.3a) — all
three collectors pay for their respective barrier machinery only during an
active cycle, confirmed independently now in three different codebases and
this one textbook description.</p>

<h3 id="66-compressor-and-pauselessc4-page-protection-vs-tagged-pointers">6.6 Compressor and Pauseless/C4: page protection vs. tagged pointers</h3>

<p><strong>§17.5, “Compressor”</strong> (p.386) and <strong>“Pauseless and C4”</strong> (p.387–388).</p>

<p>Compressor’s mechanism, from the book’s own category list:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Live: pages containing (mostly) live objects
Condemned: pages containing some live objects, but mostly dead ones
Free: pages currently free but available for allocation
New Live: pages in which copied live objects have been allocated but not yet copied
Dead: unmapped pages that can be recycled once there are no pointers to them
</code></pre></div></div>

<p><strong>Takeaway:</strong> Compressor drives compaction entirely through page
protection — it <code class="language-plaintext highlighter-rouge">mprotect</code>s tospace pages so any mutator access traps,
and the trap handler performs forwarding/copying on demand. This is the
same style of mechanism as HotSpot’s safepoint poll page (Part 3.5.2) —
a hardware trap doing useful work, not just a stop signal — applied to
compaction correctness rather than safepoint arrival: the same trick
generalizes to enforcing GC invariants during copying, not just to
synchronizing mutator suspension.</p>

<p>Pauseless/C4’s mechanism, by contrast, avoids page protection almost
entirely and uses <strong>tagged pointers</strong> instead — confirmed by the actual
bit layout (Figure 17.2, p.388):</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Pauseless steals one address bit from the 64-bit address space to use as
a pointer tag. This Not-Marked-Through (NMT) bit is used by the LVB
during the concurrent marking phase of the collector to decide whether
the reference has previously been scanned by the collector.
</code></pre></div></div>

<p><strong>Takeaway:</strong> C4/Pauseless’s LVB (“Loaded Value Barrier”) is functionally
the same idea as ZGC’s self-healing colored-pointer load barrier
(Part 6.4) — both steal address bits, both self-heal on load. ZGC’s
design (2018) comes after and builds on Azul’s Pauseless/C4 (2005/2011),
not as an independent invention. C4’s generational extension steals an
additional tag bit per pointer to track which generation the referent
belongs to, letting young and old collections proceed independently
without cross-checking a single global NMT value.</p>

<p><strong>Open question:</strong> whether ZGC’s generational variant (Figure 17.4b) has
an analogous bit-budget tradeoff to C4’s — not verified against ZGC’s
generational tagged-pointer figure directly.</p>

<h3 id="67-staccato">6.7 Staccato</h3>

<p><strong>§19.7, “Staccato: best-effort compaction with mutator wait-freedom”</strong>
(p.455) — the McCloskey et al. paper also referenced in
<code class="language-plaintext highlighter-rouge">006_other_reading_materials.md</code> of this research directory.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Staccato [McCloskey et al., 2008] permits concurrent compaction without
requiring the mutators to use locks or atomic operations like
compare-and-swap in the common case, even on multiprocessors with weak
memory ordering.
</code></pre></div></div>

<p>The mechanism, confirmed via the book’s own pseudocode (Algorithm 19.8):</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>copyObjects(candidates):
    for each p in candidates
        CompareAndSet(&amp;forwardingAddress(p), p, p | COPYING)
        waitForRaggedSynch(writeFence; readFence)
        ...
</code></pre></div></div>

<p><strong>Takeaway:</strong> “ragged synchronisation” is the general name for a pattern
this document already touched via Collie’s “pre-compaction ragged
handshake” (Part 6.6) and §15.3’s “Ragged phase changes”: instead of a
single global stop-the-world rendezvous, each mutator independently
performs a memory fence “at regular intervals (such as GC safe-points),”
and the collector waits for all mutators to have crossed that fence
individually, at their own pace — a distributed handshake rather than a
synchronized barrier. This is a different STW-avoidance strategy than
HotSpot’s single global <code class="language-plaintext highlighter-rouge">Threads_lock</code>-based rendezvous (Part 3.5.2b).</p>

<h3 id="68-scope-note-and-further-reading">6.8 Scope note and further reading</h3>

<p>Not covered above: Ch.14 (parallel GC internals — marking, copying,
compaction algorithms), Tax-and-Spend (§19.6). Sapphire, Transactional
Sapphire, Platinum, Metronome, Stopless, Chicken, and Clover are covered
in the appendix below (§19.5–19.7), not in the numbered sections above.</p>

<p>Vendor/JEP-level pointers, for the production-flag view rather than the
algorithm view:</p>
<ul>
  <li><a href="https://openjdk.org/jeps/248">JEP 248 — Make G1 the Default Garbage Collector</a></li>
  <li><a href="https://openjdk.org/jeps/439">JEP 439 — Generational ZGC</a></li>
  <li><a href="https://openjdk.org/jeps/318">JEP 318 — Epsilon: A No-Op Garbage Collector</a></li>
  <li><a href="https://wiki.openjdk.org/display/zgc/Main">OpenJDK ZGC wiki</a></li>
  <li><a href="https://wiki.openjdk.org/display/shenandoah/Main">OpenJDK Shenandoah wiki</a></li>
  <li><a href="https://github.com/oracle/graal/blob/master/docs/reference-manual/native-image/MemoryManagement.md">GraalVM Native Image <code class="language-plaintext highlighter-rouge">MemoryManagement.md</code></a> — <code class="language-plaintext highlighter-rouge">--gc=serial|G1|epsilon</code> flags</li>
</ul>

<h3 id="69-sapphire-platinum-metronome-stopless-chicken-clover">6.9 Sapphire, Platinum, Metronome, Stopless, Chicken, Clover</h3>

<p><strong>Sapphire and Transactional Sapphire</strong> (§17.4, p.376–379) — a concurrent
copying algorithm for shared-memory multiprocessors that lets one mutator
thread at a time flip from reading fromspace to reading tospace, rather
than stopping all threads to flip together. Transactional Sapphire
extends this with parallel collector threads and hardware/software
transactions for object copying, moving through four phase groups (Mark,
Copy, Flip, Reclaim — Algorithm 17.4) with distinct write barriers per
phase.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Flip: In this group, the collector forwards pointers in global variables
and thread stacks and registers, flipping them one at a time into
tospace. Unflipped mutator threads may hold references to both fromspace
and tospace copies (even of the same object).
</code></pre></div></div>

<p><strong>Takeaway:</strong> incremental flipping (one thread at a time, rather than a
single stop-the-world flip) is the mechanism’s whole point — it trades a
more complex barrier (mutators may see both copies of the same object
simultaneously) for a shorter window where any thread needs to block.</p>

<p><strong>Platinum</strong> (§17.4, p.383–384) — mostly-concurrent, generational,
replicating, aimed at long tail latencies. Uses fewer collector threads
than cores and binds each to a specific core, and uses Intel’s memory
protection keys (not a syscall-heavy <code class="language-plaintext highlighter-rouge">mprotect</code>, but a fast per-thread
register) to give collector and mutator threads different access rights
to the same pages without a full page-table change.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Platinum sets up two protection keys. During collection, one is
associated with pages to which only the collector threads should have
write access, and Platinum arranges that the mutators set their
protection keys to disallow writes to these pages.
</code></pre></div></div>

<p><strong>Metronome</strong> (§19.5, p.439–441) — a time-based real-time collector for
Java: an incremental mark-sweep collector with partial on-demand
compaction, scheduled via fixed time quanta (500µs collector slices in a
10ms window) to guarantee a minimum mutator utilization (MMU) target,
commonly 70%.</p>

<p><strong>Stopless</strong> (§19.7, p.454–455) — a lock-free concurrent compactor. Rather
than requiring mutators to update both a fromspace and tospace copy of an
object (as Sapphire does), Stopless enforces that exactly one copy is
ever the definitive one, tracked via a double-word compare-and-swap on a
“wide” intermediate copy with a status word per field
(<code class="language-plaintext highlighter-rouge">inOriginal</code>/<code class="language-plaintext highlighter-rouge">inWide</code>/<code class="language-plaintext highlighter-rouge">inCopy</code>).</p>

<p><strong>Chicken</strong> (§19.7, p.458) — architecturally close to Staccato (Part 6.7),
developed independently, targeting x86/x86-64’s stronger memory model
specifically. Because that architecture orders reads relative to atomics,
only writes need to abort an in-progress copy, and the ragged
synchronisation Staccato relies on doesn’t need the read-fence half.</p>

<p><strong>Clover</strong> (§19.7, p.458–459) — guarantees compaction with lock-free
mutator access in the common case by having the collector mark
just-copied fields with a reserved sentinel value α (chosen to make
collision with a real program value astronomically unlikely, using a
128-bit compare-and-swap on modern x86-64). A mutator that reads α knows
to reload the field through the forwarding pointer instead.</p>

<hr />

<h2 id="part-7--reading-this-material-in-passes-not-linearly">Part 7 — Reading this material in passes, not linearly</h2>

<p>The framework below is applied selectively, not as a checklist run against
every source uniformly — some of these materials don’t have all five
properties (a rejected-alternatives section, an admitted weak spot), and
forcing the framework onto a source that doesn’t have that structure would
produce a false pattern rather than an honest one. Where a technique
doesn’t fit a given source, it’s left out for that source rather than
padded in.</p>

<p><strong>The one dominant constraint</strong> applies cleanly to Go (GC needs pointer
maps → constrains preemption, Part 1.3), to BEAM (shared-nothing →
no safe-point problem, Part 3.2–3.3a), and to Kotlin (can’t modify the
JVM → CPS transform is the only lever, Part 2.1). It applies less cleanly
to the GC algorithms in Part 6 — G1, ZGC, and Shenandoah are all solving
the <em>same</em> constraint (bound pause time independent of heap size) with
different mechanisms, so for that cluster the useful move is comparing
mechanisms against a shared constraint, not finding a different
constraint for each.</p>

<p><strong>An admitted weak spot / rejected path</strong> is present, concretely, in three
places already surfaced in this document, not hypothetically: Go’s
proposal rejecting loop-back-edge checks over a measured 7.8% regression
(Part 1.3), HotSpot’s own <code class="language-plaintext highlighter-rouge">AbortVMOnSafepointTimeout</code> watchdog admitting a
thread can simply fail to reach a safepoint in production (Part 3.5.2b),
and Shenandoah’s authors’ 2023-dated uncertainty about their own 2016
non-generational argument (Part 6.5). These three are the highest-value
re-reads in the whole document if you only have time to revisit three
things.</p>

<h3 id="71-concept-mindmap--the-tension-map-not-a-fact-list">7.1 Concept mindmap — the tension map, not a fact list</h3>

<pre><code class="language-mermaid">mindmap
  root((Preempt a running&lt;br/&gt;task safely))
    Who initiates preemption
      OS timer interrupt
        No cooperation needed
        Thread has no say
      Go: SIGURG signal
        Async, external
        Still gated by safe-point check
      HotSpot: poll page
        Thread checks itself
        Trap turns it involuntary
      SubstrateVM: counter CAS
        Per-thread, no shared page
      BEAM: reduction counter
        Every instruction dispatch
        No separate check needed
    What makes a point unsafe
      Shared GC-managed heap
        Pointer vs raw int ambiguity
        Go stack maps
        HotSpot stack maps
      No shared heap at all
        BEAM sidesteps the question
        Every point is already safe
    GC correctness invariant
      Weak tricolour
        Non-moving OK
        Go hybrid barrier
      Strong tricolour
        Required for moving GC
        ZGC load barrier
        Shenandoah load barrier
        G1 deletion barrier
    Concurrency unit
      Go: goroutine, CSP channels
      Kotlin: continuation, CPS
        compiler layer
        kotlinx.coroutines layer
      BEAM: process, shared nothing
</code></pre>

<p><strong>What this diagram is arguing, in one sentence per branch:</strong> preemption
mechanisms differ in <em>who</em> decides to stop a task; safety mechanisms
differ in <em>whether</em> shared GC-managed memory makes most points unsafe to
stop at; GC correctness reduces to which tricolour invariant a collector
can afford to only weakly enforce, which is itself downstream of whether
the collector moves objects. The three top-level branches are not
independent — the “who initiates” answer each language chose was
constrained by the “what makes a point unsafe” answer their memory model
already committed them to.</p>

<h3 id="72-reading-roadmap--four-passes-across-this-documents-actual-source-list">7.2 Reading roadmap — four passes across this document’s actual source list</h3>

<pre><code class="language-mermaid">flowchart TD
    subgraph P1["Pass 1 — Orient: what problem is being solved"]
        direction LR
        A1["Go proposal 24543&lt;br/&gt;abstract + problem statement"]
        A2["mgc.go header comment&lt;br/&gt;(algorithm summary, lines 1-20)"]
        A3["Safepoint.java class doc&lt;br/&gt;(GraalVM)"]
        A4["GC Handbook §1.3&lt;br/&gt;Comparing GC algorithms"]
    end

    subgraph P2["Pass 2 — Find the one constraint"]
        direction LR
        B1["preempt.go safe-point&lt;br/&gt;categories comment"]
        B2["signal_unix.go SIGURG&lt;br/&gt;4-criteria rationale"]
        B3["Continuation.kt&lt;br/&gt;+ why suspend is compiler-level"]
        B4["erl_process.h fcalls comment&lt;br/&gt;+ Part 3.2 shared-nothing"]
        B5["GC Handbook §11.6&lt;br/&gt;safe-point vs check-point"]
    end

    subgraph P3["Pass 3 — Map rejected paths and tensions"]
        direction LR
        C1["Go: loop back-edge check,&lt;br/&gt;7.8% regression, rejected"]
        C2["HotSpot: SafepointTimeout&lt;br/&gt;watchdog admits failure mode"]
        C3["Shenandoah §17.5: authors&lt;br/&gt;question own 2016 argument"]
        C4["Agesen 1998: polling vs&lt;br/&gt;patching vs SIGURG (3rd path)"]
    end

    subgraph P4["Pass 4 — Compress to one sentence per system"]
        direction LR
        D1["Go: non-cooperative,&lt;br/&gt;gated by pointer safety"]
        D2["Kotlin: cooperative by&lt;br/&gt;construction, no interrupt exists"]
        D3["BEAM: no unsafe points&lt;br/&gt;exist, only a budget"]
        D4["JVM family: same constraint&lt;br/&gt;(bounded pause), different&lt;br/&gt;mechanism per collector"]
    end

    P1 --&gt; P2 --&gt; P3 --&gt; P4

    A1 -.reread after.-&gt; C1
    B2 -.reread after.-&gt; C4
    B5 -.reread after.-&gt; C2
</code></pre>

<p><strong>How to actually use this roadmap:</strong> don’t read every box once in order —
Pass 1 across all four sources first (just enough to know what problem
each is solving), then Pass 2 across all four (find the load-bearing
constraint in each), then Pass 3 only for the sources that have a rejected
path or admitted gap (not all of them do — Continuation.kt, for instance,
doesn’t have a “we tried X and rejected it” moment the way Go’s proposal
or Shenandoah’s retrospective do), then Pass 4 forces you to write the
one-sentence compression for each system, which is the actual test of
whether the first three passes worked. If you can’t write Pass 4’s
sentence for a system, that’s the signal to go back to Pass 2 for that
system specifically, not to reread everything.</p>

<hr />

<h2 id="source-references-for-going-deeper-only-if-a-checkpoint-above-stumps-you">Source references for going deeper (only if a checkpoint above stumps you)</h2>

<h3 id="primary-source-files-fetched-and-read-directly-for-this-document">Primary source files fetched and read directly for this document</h3>

<p><strong>Go</strong> (golang/go, master):</p>
<ul>
  <li><a href="https://github.com/golang/go/blob/master/src/runtime/preempt.go"><code class="language-plaintext highlighter-rouge">src/runtime/preempt.go</code></a></li>
  <li><a href="https://github.com/golang/go/blob/master/src/runtime/signal_unix.go"><code class="language-plaintext highlighter-rouge">src/runtime/signal_unix.go</code></a></li>
  <li><a href="https://github.com/golang/go/blob/master/src/runtime/mgc.go"><code class="language-plaintext highlighter-rouge">src/runtime/mgc.go</code></a></li>
  <li><a href="https://github.com/golang/go/blob/master/src/runtime/proc.go"><code class="language-plaintext highlighter-rouge">src/runtime/proc.go</code></a></li>
  <li><a href="https://github.com/golang/go/blob/master/src/runtime/mbarrier.go"><code class="language-plaintext highlighter-rouge">src/runtime/mbarrier.go</code></a></li>
</ul>

<p><strong>Kotlin/kotlinx.coroutines</strong>:</p>
<ul>
  <li>JetBrains/kotlin → <a href="https://github.com/JetBrains/kotlin/blob/master/libraries/stdlib/src/kotlin/coroutines/Continuation.kt"><code class="language-plaintext highlighter-rouge">libraries/stdlib/src/kotlin/coroutines/Continuation.kt</code></a></li>
  <li>JetBrains/kotlin → <a href="https://github.com/JetBrains/kotlin/blob/master/libraries/stdlib/src/kotlin/coroutines/intrinsics/Intrinsics.kt"><code class="language-plaintext highlighter-rouge">libraries/stdlib/src/kotlin/coroutines/intrinsics/Intrinsics.kt</code></a></li>
  <li>Kotlin/kotlinx.coroutines → <a href="https://github.com/Kotlin/kotlinx.coroutines/blob/master/kotlinx-coroutines-core/common/src/CoroutineDispatcher.kt"><code class="language-plaintext highlighter-rouge">kotlinx-coroutines-core/common/src/CoroutineDispatcher.kt</code></a></li>
  <li>Kotlin/kotlinx.coroutines → <a href="https://github.com/Kotlin/kotlinx.coroutines/blob/master/kotlinx-coroutines-core/jvm/src/scheduling/CoroutineScheduler.kt"><code class="language-plaintext highlighter-rouge">kotlinx-coroutines-core/jvm/src/scheduling/CoroutineScheduler.kt</code></a></li>
  <li>Kotlin/kotlinx.coroutines → <a href="https://github.com/Kotlin/kotlinx.coroutines/blob/master/kotlinx-coroutines-core/common/src/internal/DispatchedContinuation.kt"><code class="language-plaintext highlighter-rouge">kotlinx-coroutines-core/common/src/internal/DispatchedContinuation.kt</code></a></li>
  <li>(attempted, 404) <code class="language-plaintext highlighter-rouge">.../intrinsics/CoroutineIntrinsics.kt</code> — doesn’t exist at that path, discarded</li>
</ul>

<p><strong>OpenJDK/HotSpot</strong> (openjdk/jdk, master):</p>
<ul>
  <li><a href="https://github.com/openjdk/jdk/blob/master/src/hotspot/share/runtime/safepoint.hpp"><code class="language-plaintext highlighter-rouge">src/hotspot/share/runtime/safepoint.hpp</code></a></li>
  <li><a href="https://github.com/openjdk/jdk/blob/master/src/hotspot/share/runtime/safepointMechanism.hpp"><code class="language-plaintext highlighter-rouge">src/hotspot/share/runtime/safepointMechanism.hpp</code></a></li>
  <li><a href="https://github.com/openjdk/jdk/blob/master/src/hotspot/share/runtime/safepoint.cpp"><code class="language-plaintext highlighter-rouge">src/hotspot/share/runtime/safepoint.cpp</code></a></li>
</ul>

<p><strong>GraalVM/SubstrateVM</strong> (oracle/graal, master):</p>
<ul>
  <li><a href="https://github.com/oracle/graal/blob/master/substratevm/src/com.oracle.svm.core/src/com/oracle/svm/core/thread/Safepoint.java"><code class="language-plaintext highlighter-rouge">substratevm/src/com.oracle.svm.core/src/com/oracle/svm/core/thread/Safepoint.java</code></a></li>
  <li><a href="https://github.com/oracle/graal/blob/master/docs/reference-manual/native-image/MemoryManagement.md"><code class="language-plaintext highlighter-rouge">docs/reference-manual/native-image/MemoryManagement.md</code></a> — official docs, <code class="language-plaintext highlighter-rouge">--gc=serial|G1|epsilon</code> flags and defaults</li>
</ul>

<h3 id="the-garbage-collection-handbook-2nd-ed-2023--read-directly-local-pdf">The Garbage Collection Handbook, 2nd ed. (2023) — read directly, local PDF</h3>

<p><code class="language-plaintext highlighter-rouge">~/Documents/Books_on_vm/Jones, Richard - The Garbage Collection Handbook...pdf</code>
(Jones, Hosking, Moss; Chapman and Hall/CRC, 2023). Sections read directly
for Part 6: §11.6 “GC safe-points and mutator suspension” (p.198), §15.1
“Correctness of concurrent collection” / tricolour invariants (p.331–334),
§16.5 “Garbage-First: collecting young and old regions” (p.354), §17.4
“Replication copying” — Sapphire, Transactional Sapphire, Platinum
(p.376–384), §17.5 “Concurrent compaction” — Compressor (p.386),
Pauseless/C4 (p.387–388), Collie (p.395), ZGC (p.396), Shenandoah (p.401),
§19.5 “Metronome” (p.439–441), §19.7 “Controlling fragmentation” —
Staccato, Stopless, Chicken, Clover (p.453–459). Not yet read directly:
Ch.14 (parallel GC internals), §19.6 “Tax-and-Spend” (p.448).</p>

<h3 id="gc-algorithms-part-6--not-read-as-source-cited-as-pointers-for-later-reading">GC algorithms (Part 6) — not read as source, cited as pointers for later reading</h3>

<ul>
  <li><a href="https://openjdk.org/jeps/248">JEP 248 — Make G1 the Default Garbage Collector</a></li>
  <li><a href="https://openjdk.org/jeps/439">JEP 439 — Generational ZGC</a></li>
  <li><a href="https://openjdk.org/jeps/318">JEP 318 — Epsilon: A No-Op Garbage Collector</a></li>
  <li><a href="https://wiki.openjdk.org/display/zgc/Main">OpenJDK ZGC wiki</a></li>
  <li><a href="https://wiki.openjdk.org/display/shenandoah/Main">OpenJDK Shenandoah wiki</a></li>
  <li>Detlefs, Flood, Heller, Printezis, “Garbage-First Garbage Collection” (ISMM 2004) — search title, not linked directly</li>
  <li>Flood, Ross, Shipilev et al., “Shenandoah: An Open-Source Concurrent Compacting Garbage Collector for OpenJDK” (2016) — search title, not linked directly</li>
</ul>

<p><strong>Erlang/OTP</strong> (erlang/otp, master):</p>
<ul>
  <li><a href="https://github.com/erlang/otp/blob/master/erts/emulator/beam/erl_process.c"><code class="language-plaintext highlighter-rouge">erts/emulator/beam/erl_process.c</code></a></li>
  <li><a href="https://github.com/erlang/otp/blob/master/erts/emulator/beam/erl_process.h"><code class="language-plaintext highlighter-rouge">erts/emulator/beam/erl_process.h</code></a></li>
  <li><a href="https://github.com/erlang/otp/blob/master/erts/emulator/beam/erl_vm.h"><code class="language-plaintext highlighter-rouge">erts/emulator/beam/erl_vm.h</code></a></li>
  <li><a href="https://github.com/erlang/otp/blob/master/erts/emulator/beam/emu/beam_emu.c"><code class="language-plaintext highlighter-rouge">erts/emulator/beam/emu/beam_emu.c</code></a></li>
  <li><a href="https://github.com/erlang/otp/blob/master/erts/emulator/beam/erl_gc.c"><code class="language-plaintext highlighter-rouge">erts/emulator/beam/erl_gc.c</code></a></li>
  <li><a href="https://github.com/erlang/otp/blob/master/erts/emulator/beam/erl_bif_info.c"><code class="language-plaintext highlighter-rouge">erts/emulator/beam/erl_bif_info.c</code></a> (grep only, minimal use)</li>
</ul>

<h3 id="crafting-interpreters--read-directly-via-003_researchmd-this-directory">Crafting Interpreters — read directly (via <code class="language-plaintext highlighter-rouge">003_research.md</code>, this directory)</h3>

<p>Robert Nystrom, <a href="https://craftinginterpreters.com/garbage-collection.html"><em>Crafting Interpreters</em>, “Garbage Collection”</a>
— base mark-sweep and tricolor abstraction (Part 6.1); the tricolor
wavefront image used in Part 6.1 is sourced from this chapter.</p>

<h3 id="secondary-sources-pastedcited-not-independently-fetched-for-this-doc">Secondary sources (pasted/cited, not independently fetched for this doc)</h3>

<ul>
  <li>Sunny Srinidhi, <a href="https://medium.com/software-under-the-hood/under-the-hood-java-peak-safepoints-dd45af07d766">“Under the Hood: Java Peak Safepoints”</a></li>
  <li>Nitsan Wakart / Gil Tene (mechanical-sympathy list), <a href="http://psy-lob-saw.blogspot.com/2015/12/safepoints.html">“Safepoints: Meaning, Side Effects and Overheads”</a></li>
  <li><a href="http://chriskirk.blogspot.com/2013/09/what-is-java-safepoint.html">“What is a Java Safepoint?”</a></li>
  <li><a href="http://jpbempel.blogspot.com/2013/03/safety-first-safepoints.html">“Safety First: Safepoints”</a></li>
</ul>]]></content><author><name>ikouchiha47</name></author><summary type="html"><![CDATA[How Go, Kotlin coroutines, and BEAM handle preemption safely, why GC safe-points exist, and a source-grounded tour of tricolor GC, G1, ZGC, Shenandoah, C4, and more.]]></summary></entry><entry><title type="html">Optimux: from a worker-pool tutorial to a DynamicScaler</title><link href="https://ikouchiha47.github.io/2026/08/10/optimux-dynamic-scaler.html" rel="alternate" type="text/html" title="Optimux: from a worker-pool tutorial to a DynamicScaler" /><published>2026-08-10T00:00:00+00:00</published><updated>2026-08-10T00:00:00+00:00</updated><id>https://ikouchiha47.github.io/2026/08/10/optimux-dynamic-scaler</id><content type="html" xml:base="https://ikouchiha47.github.io/2026/08/10/optimux-dynamic-scaler.html"><![CDATA[<p><a href="https://github.com/go-batteries/optimux">optimux</a> is a Go service that resizes, re-encodes, and streams images and video on the fly — <code class="language-plaintext highlighter-rouge">/resize?image_url=...&amp;sizes=300x0&amp;format=webp&amp;quality=60</code>, done. It’s been running as an internal service for a while. I’m open-sourcing it now, AGPL-3.0, because the worker pool underneath it — the part that decides how many goroutines are doing image processing at any given moment — ended up being the most interesting thing in the codebase, and it deserves to exist somewhere other than a private repo.</p>

<p>This is the story of how that worker pool got there, including the part where the pattern everyone starts with turned out to be actively wrong for this workload.</p>

<h2 id="starting-point-the-jobdispatcher-pattern">Starting point: the Job/Dispatcher pattern</h2>

<p>Like a lot of people who’ve had to build a Go job queue, I started from Marcio Castilho’s <a href="https://medium.com/smsjunk/handling-1-million-requests-per-minute-with-golang-f70ac505fcaa">Handling 1 Million Requests per Minute with Golang</a>. Worth being precise about what that article actually builds, because the mismatch with image processing turned out to matter a lot:</p>

<ul>
  <li>A <code class="language-plaintext highlighter-rouge">Job</code> struct wraps one unit of work (there, a POST payload headed to S3).</li>
  <li>A buffered <code class="language-plaintext highlighter-rouge">JobQueue chan Job</code> receives incoming jobs.</li>
  <li>A <code class="language-plaintext highlighter-rouge">Dispatcher</code> owns a <code class="language-plaintext highlighter-rouge">WorkerPool chan chan Job</code> — a <strong>channel of channels</strong>. Each <code class="language-plaintext highlighter-rouge">Worker</code> has its own <code class="language-plaintext highlighter-rouge">chan Job</code>, and the moment a worker finishes a job, it pushes its <em>own</em> channel back into the shared <code class="language-plaintext highlighter-rouge">WorkerPool</code>. The dispatcher’s loop just pulls one worker-channel off <code class="language-plaintext highlighter-rouge">WorkerPool</code> and hands it the next job.</li>
  <li>No worker ever polls for work or reports “I’m free” via a flag — availability <em>is</em> “my channel is currently sitting in the pool.” <code class="language-plaintext highlighter-rouge">MAX_WORKERS</code> and <code class="language-plaintext highlighter-rouge">MAX_QUEUE</code> are env-configured, fixed for the process lifetime.</li>
</ul>

<p>The article’s numbers are genuinely good: they took a system that needed ~100 EC2 instances down to 4 <code class="language-plaintext highlighter-rouge">c4.Large</code> instances handling close to a million requests a minute, by replacing unbounded goroutine spawning with this bounded pool. But the workload is uploading JSON payloads to S3 — almost entirely I/O wait, barely any CPU. I built the same pattern, pointed it at image resizing instead, and it did not translate.</p>

<h2 id="the-pattern-didnt-fit-and-pprof-proved-it">The pattern didn’t fit, and pprof proved it</h2>

<p>I wired up the Job/Dispatcher pattern for image jobs and it was slow in a way that didn’t make sense from the throughput numbers alone. So I pulled a blocking profile, and it was unambiguous:</p>

<blockquote>
  <p>pprof blocking profile shows <strong>67.99%</strong> of total time spent waiting on <code class="language-plaintext highlighter-rouge">chanrecv1</code>.</p>
</blockquote>

<p>That’s workers blocked waiting for jobs, and the dispatcher blocked waiting for workers to hand their channel back — a pattern built for a workload where a worker is <em>cheap and fast</em> per unit (fire an S3 PUT, wait on the network, done) turns into mostly idle channel choreography when a unit of work is <em>hundreds of milliseconds of CPU-bound image processing</em> instead. The channel-of-channels handoff, the dispatcher loop, the per-job registration — all of that overhead is invisible when a worker’s actual job takes microseconds of your attention and the rest is network wait. It stops being invisible when the job itself is the bottleneck. Target throughput was <code class="language-plaintext highlighter-rouge">8.6rps</code>; no matter what I tuned in this shape, it sat at <code class="language-plaintext highlighter-rouge">2.0xrps</code>.</p>

<p>So I threw it out. Not tuned, not patched — removed, and rebuilt from a synchronous baseline up, one variable at a time, so I’d actually know what each change bought me instead of tuning inside a pattern that was wrong from the start.</p>

<h2 id="rebuilding-from-zero-one-variable-at-a-time">Rebuilding from zero, one variable at a time</h2>

<p><strong>Synchronous baseline, no workers at all</strong> — inline resize, respond directly. <code class="language-plaintext highlighter-rouge">2.9rps</code>. Already ahead of the dispatcher pattern’s <code class="language-plaintext highlighter-rouge">2.0x</code>, which was the first sign the problem wasn’t “not enough workers,” it was the shape of the concurrency itself.</p>

<p><strong>Single channel, single worker.</strong> The smallest thing that still queues — one goroutine pulling off one channel, closer to a single core churning a work list than to a “pool” in any real sense. <code class="language-plaintext highlighter-rouge">2.1rps</code> — <em>worse</em> than doing nothing concurrent at all. Concurrency has a floor cost, and this workload was paying it without buying anything back yet.</p>

<p><strong>Single queue, 4 workers.</strong> <code class="language-plaintext highlighter-rouge">3.15rps</code>, average latency <code class="language-plaintext highlighter-rouge">1.37s</code>. First real win, and a small one.</p>

<p><strong>Two queues — fetch and process, split apart — 4 workers each.</strong> <code class="language-plaintext highlighter-rouge">3.07rps</code>, marginally <em>lower</em> throughput than the single queue, but the latency distribution across percentiles visibly smoothed out. Tracing explained why: fetching a source image from tmpfs cost <code class="language-plaintext highlighter-rouge">40-100ms</code>; processing it (libvips, resizing a 3.1MB source down to a 120×240 webp) cost <code class="language-plaintext highlighter-rouge">~700ms</code>. Those are two stages with wildly different service times sharing a queue and worker pool — an impedance mismatch, where a burst of cheap fetches can queue up behind a slow processor, or the reverse.</p>

<p><strong>Two queues, but weighted 2 fetchers / 4 processors</strong>, leaning into the imbalance on purpose. Performed <em>worse</em> than the even split, still around <code class="language-plaintext highlighter-rouge">3+rps</code>. My read at the time: this isn’t a batch pipeline where you can freely over-provision the expensive stage and let a queue absorb the mismatch — it’s a real-time request path where the caller is still on the other end of the HTTP connection waiting, so over-provisioning one stage just moves the queueing, it doesn’t remove it.</p>

<p>I also tried a demand-driven producer/consumer setup along the way — closer to Elixir’s GenStage, where the consumer explicitly asks the producer for N items instead of the producer pushing whenever it has something. It didn’t pan out for this workload, and honestly the specifics didn’t survive in my notes — only the conclusion did: the demand/ack round-trip was adding coordination cost that a plain buffered channel already got for free, without a corresponding improvement in how work actually got scheduled.</p>

<p>Separately, before any dynamic scaler existed, I added something orthogonal to worker count entirely: instead of buffering the whole encoded image and writing it in one response, the handler started flushing early and streaming bytes out as the encoder produced them. That alone moved the needle independent of worker shape — <code class="language-plaintext highlighter-rouge">3.28rps</code>, latency down to <code class="language-plaintext highlighter-rouge">1.31s</code>.</p>

<h2 id="what-actually-generalizes-and-what-doesnt">What actually generalizes, and what doesn’t</h2>

<p>Here’s the actual lesson, not just the numbers: the Job/Dispatcher pattern isn’t wrong, it’s scoped to a specific kind of workload — one where the <em>external</em> resource (S3, a network call, a database) is the thing you’re rationing, and your own CPU is basically idle waiting on it. In that world, a large fixed worker count is nearly free — you’re just capping how many outstanding waits you allow. libvips-backed image processing inverts that: the constrained resource is <em>your own CPU and memory</em>, fetch and process have genuinely different cost profiles, and a fixed <code class="language-plaintext highlighter-rouge">MAX_WORKERS</code> picked once has no way to track a queue that swings between bursty and empty. <code class="language-plaintext highlighter-rouge">iostat</code> on the EC2 box backed this up directly — CPU usage swinging from 80%+ down to 40% inside the same short window, <code class="language-plaintext highlighter-rouge">%steal</code> spiking as high as 8.74% (hypervisor-stolen cycles, which no amount of worker retuning fixes). A workload like that needs the worker count itself to be a live variable, not a constant — which is what actually motivated moving off any fixed-size pool entirely, GenStage-shaped or otherwise, toward something that watches queue depth and scales.</p>

<p>It’s worth being precise about <em>where</em> the two patterns actually diverge structurally, because it’s not just “one has a scaler and one doesn’t.”</p>

<p>The dispatcher pattern’s availability signal is a second layer of indirection: each worker owns a private <code class="language-plaintext highlighter-rouge">chan Job</code>, and “I’m free” is expressed by pushing that channel into a shared <code class="language-plaintext highlighter-rouge">WorkerPool chan chan Job</code>. That’s <code class="language-plaintext highlighter-rouge">N+1</code> channels for <code class="language-plaintext highlighter-rouge">N</code> workers — one pool channel plus one per-worker channel — and a dispatcher goroutine whose whole job is shuttling a channel out of the pool, handing it a job, and waiting for it to come back. It’s a clean pattern, but it exists to solve a problem optimux’s worker pool doesn’t have: every worker in <code class="language-plaintext highlighter-rouge">DynamicScaler</code> pulls directly off one shared <code class="language-plaintext highlighter-rouge">Queue chan T</code>. There’s no dispatcher goroutine, no per-worker channel, no explicit “I’m free” message at all — a worker’s availability <em>is</em> nothing more than “currently blocked on a receive from <code class="language-plaintext highlighter-rouge">Queue</code>,” which Go’s own runtime already arbitrates correctly among however many goroutines are competing to receive. The single-shared-queue shape showed up as early as the “single queue, 4 workers” experiment above, well before any scaler existed, and every version since kept it — the channel-of-channels indirection never came back, because a single channel with competing receivers gave equal or better throughput without the extra bookkeeping.</p>

<p>The other divergence is what each pattern treats as fixed. The article’s <code class="language-plaintext highlighter-rouge">MAX_WORKERS</code> is an env var read once at startup — there’s no concept of a worker ever leaving the pool short of the whole process dying, because the workload it targets doesn’t need one: S3 upload capacity doesn’t really shrink at 2am. <code class="language-plaintext highlighter-rouge">DynamicScaler</code> has an explicit opposite belief baked into its registry — every <code class="language-plaintext highlighter-rouge">WorkerSlot</code> can be marked <code class="language-plaintext highlighter-rouge">Retiring</code> and needs a defined, race-free way to leave the pool without dropping in-flight work, because for this workload the <em>right</em> worker count at any moment is itself the thing under contention (CPU cores, memory for concurrently-decoded images), not a constant you get to pick once.</p>

<h2 id="what-dynamicscaler-does">What DynamicScaler does</h2>

<p><code class="language-plaintext highlighter-rouge">DynamicScaler[T]</code> (<code class="language-plaintext highlighter-rouge">src/mediahose/schedulers.go</code>) is a generic worker pool that grows and shrinks based on queue pressure instead of running a fixed goroutine count. The type parameter is the job type, so the same scaler independently drives image jobs, video jobs, and batch jobs off different queues with different thresholds.</p>

<p>The registry is a single ordered slice, append-only in creation order:</p>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">type</span> <span class="n">WorkerSlot</span><span class="p">[</span><span class="n">T</span> <span class="n">any</span><span class="p">]</span> <span class="k">struct</span> <span class="p">{</span>
    <span class="n">Idx</span>      <span class="kt">int64</span>
    <span class="n">Retiring</span> <span class="kt">bool</span> <span class="c">// marked for retirement; reclaimed only on exit notify</span>
    <span class="n">worker</span>   <span class="n">Worker</span><span class="p">[</span><span class="n">T</span><span class="p">]</span>
    <span class="n">cancel</span>   <span class="n">context</span><span class="o">.</span><span class="n">CancelFunc</span>
    <span class="n">retire</span>   <span class="k">chan</span> <span class="k">chan</span> <span class="kt">bool</span>
<span class="p">}</span>

<span class="k">type</span> <span class="n">DynamicScaler</span><span class="p">[</span><span class="n">T</span> <span class="n">any</span><span class="p">]</span> <span class="k">struct</span> <span class="p">{</span>
    <span class="n">WorkerFactory</span>      <span class="k">func</span><span class="p">(</span><span class="n">idx</span> <span class="kt">int64</span><span class="p">,</span> <span class="n">done</span> <span class="k">chan</span> <span class="kt">int64</span><span class="p">)</span> <span class="n">Worker</span><span class="p">[</span><span class="n">T</span><span class="p">]</span>
    <span class="n">Queue</span>              <span class="k">chan</span> <span class="n">T</span>
    <span class="n">MinWorkers</span><span class="p">,</span> <span class="n">MaxWorkers</span> <span class="kt">int</span>
    <span class="n">ScaleUpThreshold</span><span class="p">,</span> <span class="n">ScaleDownThreshold</span> <span class="kt">int</span>
    <span class="n">CheckInterval</span><span class="p">,</span> <span class="n">ScaleCooldown</span><span class="p">,</span> <span class="n">RetireGrace</span> <span class="n">time</span><span class="o">.</span><span class="n">Duration</span>
    <span class="n">workers</span> <span class="p">[]</span><span class="o">*</span><span class="n">WorkerSlot</span><span class="p">[</span><span class="n">T</span><span class="p">]</span> <span class="c">// tail = newest</span>
<span class="p">}</span>
</code></pre></div></div>

<p>Because the slice is append-only, the tail is always the newest worker — which makes “retire the newest worker first” (LIFO) a deterministic scan instead of a random pick. There’s no separate counter tracking how many workers are active; the live count is derived on read:</p>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">func</span> <span class="n">countLive</span><span class="p">[</span><span class="n">T</span> <span class="n">any</span><span class="p">](</span><span class="n">workers</span> <span class="p">[]</span><span class="o">*</span><span class="n">WorkerSlot</span><span class="p">[</span><span class="n">T</span><span class="p">])</span> <span class="kt">int</span> <span class="p">{</span>
    <span class="n">live</span> <span class="o">:=</span> <span class="m">0</span>
    <span class="k">for</span> <span class="n">_</span><span class="p">,</span> <span class="n">wsl</span> <span class="o">:=</span> <span class="k">range</span> <span class="n">workers</span> <span class="p">{</span>
        <span class="k">if</span> <span class="o">!</span><span class="n">wsl</span><span class="o">.</span><span class="n">Retiring</span> <span class="p">{</span>
            <span class="n">live</span><span class="o">++</span>
        <span class="p">}</span>
    <span class="p">}</span>
    <span class="k">return</span> <span class="n">live</span>
<span class="p">}</span>
</code></pre></div></div>

<p>Nothing else exists to fall out of sync with the registry, because there’s nothing else — the count is a computation over the one piece of state, not a second piece of state someone has to remember to update alongside it.</p>

<p><strong>Retirement happens at an idle boundary, not on demand.</strong> A worker that opts in (<code class="language-plaintext highlighter-rouge">FetchWorker</code>, the image/video processing worker, does) runs a single blocking <code class="language-plaintext highlighter-rouge">select</code> over three cases — context cancellation, a retire request, and the job queue — with no <code class="language-plaintext highlighter-rouge">default</code> branch:</p>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">select</span> <span class="p">{</span>
<span class="k">case</span> <span class="o">&lt;-</span><span class="n">ctx</span><span class="o">.</span><span class="n">Done</span><span class="p">()</span><span class="o">:</span>
    <span class="k">return</span>
<span class="k">case</span> <span class="n">retireReq</span> <span class="o">:=</span> <span class="o">&lt;-</span><span class="n">fw</span><span class="o">.</span><span class="n">RetireCh</span><span class="o">:</span>
    <span class="k">select</span> <span class="p">{</span>
    <span class="k">case</span> <span class="n">retireReq</span> <span class="o">&lt;-</span> <span class="no">true</span><span class="o">:</span>
    <span class="k">default</span><span class="o">:</span>
    <span class="p">}</span>
    <span class="k">return</span>
<span class="k">case</span> <span class="n">job</span> <span class="o">:=</span> <span class="o">&lt;-</span><span class="n">jobQueueChan</span><span class="o">:</span>
    <span class="c">// process it</span>
<span class="p">}</span>
</code></pre></div></div>

<p>No <code class="language-plaintext highlighter-rouge">default</code> matters: the worker is genuinely parked, doing nothing, only while blocked in that select. A retire request lands the instant the worker goes idle, not after it happens to finish whatever job comes next. When the scaler decides to shrink the pool, it marks the newest slot <code class="language-plaintext highlighter-rouge">Retiring: true</code> and sends a one-shot ack request; the slot stays in the registry, still counted toward the hard <code class="language-plaintext highlighter-rouge">MaxWorkers</code> cap, until the worker actually exits and notifies the scaler on a close channel. There’s a bounded grace period (default 2s) — if the worker hasn’t idled out by then, the scaler cancels its context as a fallback nudge, but removal from the registry only ever happens on the real exit notification, never on the timeout itself.</p>

<p><strong>Scale decisions are cooled down, with an escape hatch.</strong> The signal driving <code class="language-plaintext highlighter-rouge">scale()</code> is <code class="language-plaintext highlighter-rouge">len(ds.Queue)</code> — instantaneous backlog, sampled on a ticker. That’s not the same thing as, say, SQS handing you <code class="language-plaintext highlighter-rouge">ApproximateNumberOfMessagesVisible</code> alongside an in-flight count, smoothed over a window before an alarm fires. A raw instantaneous queue length is noisy under bursty traffic: it can cross <code class="language-plaintext highlighter-rouge">ScaleUpThreshold</code> and <code class="language-plaintext highlighter-rouge">ScaleDownThreshold</code> within a couple of ticks without the underlying load actually having changed, and a scaler with no way to tell a real trend from a blip would happily add a worker and retire it right back. The cooldown (default 30s between scale actions) exists because that better signal doesn’t exist yet — it’s a blunt fix for a noisy input, not a fundamental property of dynamic pools. The one case that bypasses it entirely: if live workers ever drop below <code class="language-plaintext highlighter-rouge">MinWorkers</code>, the scaler refills immediately regardless of cooldown, so a crash storm can’t leave the pool sitting empty for a full cooldown window waiting on a timer that doesn’t care capacity is gone.</p>

<p>That’s the whole mechanism: one ordered registry as the single source of truth for both “who’s alive” and “who retires next,” idle-boundary handshakes instead of blind cancellation, and a cooldown standing in for a smarter scaling signal that isn’t built yet.</p>

<h2 id="what-i-didnt-test-prefork">What I didn’t test: prefork</h2>

<p>One pattern from a different corner of the same problem space I never actually tried: <a href="https://docs.gofiber.io/">Fiber’s prefork mode</a> — instead of one Go process handling everything under a single <code class="language-plaintext highlighter-rouge">GOMAXPROCS</code>, use <code class="language-plaintext highlighter-rouge">SO_REUSEPORT</code> to run N forked OS processes, each getting its own slice of cores, with the kernel load-balancing connections across them.</p>

<p>My honest guess is it wouldn’t have moved the throughput numbers above. Every pool-shape experiment — single worker, 4 workers, split queues, weighted split — barely moved throughput (<code class="language-plaintext highlighter-rouge">2.1</code> to <code class="language-plaintext highlighter-rouge">3.15rps</code>), because the real cost is <code class="language-plaintext highlighter-rouge">~700ms</code> of libvips CPU work per image, not goroutine-scheduling or channel overhead. Prefork changes how many OS processes share a machine’s cores, not how many cores exist; the same physical CPU is doing the same total amount of resize work whether it’s one process running N goroutines or K processes each running fewer, and Go’s scheduler already multiplexes goroutines across cores efficiently for CPU-bound work like this.</p>

<p>Where I’d actually expect it to help is somewhere none of these benchmarks were looking: tail latency and fault isolation. This worker loop allocates large image byte slices per job (there’s a <code class="language-plaintext highlighter-rouge">byts = nil</code> in it as a GC hint) — a GC pause or heavy mark-assist period in one process adds jitter to <em>every</em> concurrent request in that process. With prefork, GC pressure in one fork doesn’t stall requests being served by a sibling fork. Same story for a pathological input (corrupt file, huge dimensions) wedging or OOMing one process — the kernel keeps routing new connections to the survivors instead of the whole fleet going down. Neither of those is a throughput property, and neither is something <code class="language-plaintext highlighter-rouge">Progression.md</code>’s numbers (throughput, average latency) would have caught even if true. Genuinely untested — not a claim, a gap.</p>

<h2 id="whats-next">What’s next</h2>

<p>Three things this codebase is deliberately waiting on or missing rather than half-building now, documented in the repo for anyone who wants to pick them up.</p>

<p><strong>Streaming instead of buffering, once govips ships it.</strong> Every image today gets fully buffered into a <code class="language-plaintext highlighter-rouge">[]byte</code> at each stage — load, encode, respond — before the next stage starts. govips just merged native streaming support (<code class="language-plaintext highlighter-rouge">LoadImageFromReader</code>, <code class="language-plaintext highlighter-rouge">SaveToWriter*</code>, PR <a href="https://github.com/davidbyttow/govips/pull/539">davidbyttow/govips#539</a>), which would let a load and an encode work directly against <code class="language-plaintext highlighter-rouge">io.Reader</code>/<code class="language-plaintext highlighter-rouge">io.Writer</code> instead of a fully materialized buffer — real memory savings for a service whose job is exactly this: dynamic compression of non-standard sizes at request time. It’s not in a tagged release yet (latest tag predates the merge by months), so this is parked as <code class="language-plaintext highlighter-rouge">docs/ADR-003</code> in the repo until that lands, rather than pinned to an untagged commit in something serving production traffic.</p>

<p><strong>A real HTTP/2 cross-stream priority scheduler.</strong> This one came out of a completely different rabbit hole: <a href="https://blog.cloudflare.com/parallel-streaming-of-progressive-images/">Cloudflare’s parallel-streaming-of-progressive-images</a> work, where a server multiplexing many concurrently-loading images over one HTTP/2 connection sends every image’s size header first, then every preview, then remaining refinement data — instead of blasting one image’s full body before starting the next. Optimux already does the single-image version of this (<code class="language-plaintext highlighter-rouge">StreamEncoder</code>/<code class="language-plaintext highlighter-rouge">ProgressiveStreamEncoder</code> chunk header→preview→remainder), but the cross-stream scheduling piece — deciding, across every concurrently open image request, whose bytes go out next — turned out to be something almost nobody exposes as a plugin point. Go’s <code class="language-plaintext highlighter-rouge">x/net/http2.WriteScheduler</code> is deprecated by its own maintainers (“provides too much visibility into implementation internals, is difficult to use”); nginx has an internal RFC 7540 priority tree that’s private to its core module, not reachable from third-party modules; <code class="language-plaintext highlighter-rouge">fasthttp/http2</code> parses the <code class="language-plaintext highlighter-rouge">PRIORITY</code> frame’s weight and never reads it again anywhere. The real prior art turned out to be H2O (a genuine O(1) scheduler, but measurably unfair — Tempesta Tech’s own simulation found only 2 of 256 streams got ideal scheduling against it), nghttp2 (correct WFQ, but O(n)), and <a href="https://github.com/tempesta-tech/tempesta/pull/1973">Tempesta FW</a>, who landed on WFQ backed by HAProxy’s <code class="language-plaintext highlighter-rouge">ebtree</code> specifically for the 100-1000-stream range this kind of service would actually see. The plan, written up in <code class="language-plaintext highlighter-rouge">docs/EXPLORATION-HTTP2-Priority-Scheduler.md</code>, is a from-scratch server on <code class="language-plaintext highlighter-rouge">golang.org/x/net/http2.Framer</code> directly — bypassing the deprecated hook entirely — staged from a toy proving the interleaving works, up to real flow control, up to wiring in the actual image pipeline.</p>

<p><strong>A scale-up signal that knows when adding a worker won’t help.</strong> <code class="language-plaintext highlighter-rouge">scale()</code> only ever asks “is the queue long” — it has no way to tell a genuine burst apart from a backlog that’s building because libvips’s own thread pool is already saturated, in which case another Go worker just queues behind the same C-level pool without adding real capacity, while still paying for a decoded image buffer’s worth of memory. I went looking for a way to read that saturation directly from govips and it isn’t there: <code class="language-plaintext highlighter-rouge">ConcurrencyLevel</code> is write-only in practice (set once via <code class="language-plaintext highlighter-rouge">vips_concurrency_set</code>, read back only for a startup log line), and <code class="language-plaintext highlighter-rouge">RuntimeStats</code> tracks cumulative operation counts, not current thread-pool occupancy. libvips’s underlying <code class="language-plaintext highlighter-rouge">GThreadPool</code> (GLib) does expose real introspection — <code class="language-plaintext highlighter-rouge">g_thread_pool_unprocessed()</code>, <code class="language-plaintext highlighter-rouge">g_thread_pool_get_num_threads()</code> — govips just never wraps it. The more practical fix doesn’t need any of that: an atomic in-flight counter around each worker’s <code class="language-plaintext highlighter-rouge">processor.Process()</code> call, gating <code class="language-plaintext highlighter-rouge">addWorkerLocked</code> when in-flight already matches <code class="language-plaintext highlighter-rouge">VipsConcurrency</code>. Documented as a finding, not built yet.</p>

<h2 id="why-open-source-it-now">Why open-source it now</h2>

<p>The scaler is the part of this codebase I’d actually want other people to read, argue with, or reuse — a fairly small, self-contained piece of generic Go with real constraints (goroutine lifecycle, backpressure, graceful shutdown) that most worker-pool tutorials, mine included at the start, wave away. The rest of optimux — image/video transforms, S3 wiring, the HTTP surface — is useful but unremarkable by comparison.</p>

<p>Repo: <strong><a href="https://github.com/go-batteries/optimux">github.com/go-batteries/optimux</a></strong>, AGPL-3.0.</p>]]></content><author><name>ikouchiha47</name></author><summary type="html"><![CDATA[The Job/Dispatcher worker pool from the famous '1 Million Requests' article is built for I/O-bound fan-out. Image processing is a mixed I/O+CPU pipeline, and pprof proved the difference. Here's the road from that pattern to a self-scaling worker pool.]]></summary></entry><entry><title type="html">CodeKeyboard Rebuild Notes</title><link href="https://ikouchiha47.github.io/keyboard/" rel="alternate" type="text/html" title="CodeKeyboard Rebuild Notes" /><published>2026-08-08T08:00:00+00:00</published><updated>2026-08-08T08:00:00+00:00</updated><id>https://ikouchiha47.github.io/codekeyboard-notes</id><content type="html" xml:base="https://ikouchiha47.github.io/keyboard/"><![CDATA[<p><img src="/assets/keyboard/keyboard.png" alt="CodeKeyboard — split Sofle-inspired layout" style="max-width:100%;height:auto;display:block;margin:0 auto 1.5rem;" /></p>

<p>Written so a human or an LLM can rebuild the keyboard without inventing APIs.</p>

<p>CodeKeyboard is a system Android keyboard (IME), not a text box demo — split Sofle-inspired layout, multiple layers, home-row modifiers, and on-device prediction that learns personal and mixed-script typing (Banglish included) without a server. These notes walk through the canvas rendering, composing/selection handling, the suggestion strategy stack, bigram prediction, fuzzy correction, and the build/CI/F-Droid pipeline — in the order the system actually depends on itself, not the order features got added.</p>

<p>Source: <a href="https://github.com/ikouchiha47/codekeyboard">github.com/ikouchiha47/codekeyboard</a></p>]]></content><author><name>ikouchiha47</name></author><summary type="html"><![CDATA[]]></summary></entry><entry><title type="html">Shrinking a Materials Database from 11GB to 266MB Without Losing the Answer</title><link href="https://ikouchiha47.github.io/2026/08/05/materialsdb-11gb-to-266mb.html" rel="alternate" type="text/html" title="Shrinking a Materials Database from 11GB to 266MB Without Losing the Answer" /><published>2026-08-05T00:00:00+00:00</published><updated>2026-08-05T00:00:00+00:00</updated><id>https://ikouchiha47.github.io/2026/08/05/materialsdb-11gb-to-266mb</id><content type="html" xml:base="https://ikouchiha47.github.io/2026/08/05/materialsdb-11gb-to-266mb.html"><![CDATA[<p><a href="https://github.com/ikouchiha47/materialsDB">materialsDB</a> is a small Go API serving 145,923 materials from the Materials Project, backed by a single SQLite file. Live at <a href="https://materials-db.fly.dev/">materials-db.fly.dev</a>.</p>

<p>The source data is <strong>11.35GB</strong>. The database it actually runs on is <strong>266MB</strong>. This is the story of what happens in between, and why most of that 11GB was never something the API needed in the first place.</p>

<hr />

<h2 id="whats-actually-in-the-dataset">What’s Actually in the Dataset</h2>

<p>The source is <a href="https://figshare.com/articles/dataset/Materials_Project_Trjectory_MPtrj_Dataset/23713842">MPtrj</a> — the Materials Project Trajectory dataset. One file, <code class="language-plaintext highlighter-rouge">MPtrj_2022.9_full.json</code>, 11.35GB. It’s the training corpus behind <strong>CHGNet</strong>, a machine-learned interatomic potential that predicts energy, force, and stress for arbitrary atomic arrangements.</p>

<p>That’s the detail that explains everything else in this post: CHGNet’s whole job is predicting energy, force, and stress for <em>any</em> atomic arrangement, not just the relaxed ground state — that’s what makes it useful for running new relaxations on structures nobody’s computed yet. To train a model that generalizes across the energy landscape, you need labeled examples away from the minimum too: high-pressure configurations, partially-relaxed intermediate steps, structures that are “wrong” by various amounts. That’s exactly what a material’s other relaxation tasks are — points along a trajectory at different pressures and energies, each one a training example teaching the model “here’s what the forces look like when the structure is bent this way.”</p>

<p>So the dataset isn’t 1.58M rows because someone was sloppy about deduplication. It’s 1.58M rows because each material’s relaxation run gets sampled at several points, and every point is a valid (structure, energy, force, stress) training tuple for the model this data was built for.</p>

<h2 id="the-mp-149-example">The mp-149 Example</h2>

<p>Concretely — material <code class="language-plaintext highlighter-rouge">mp-149</code> (silicon) has 7 tasks in the dataset:</p>

<table>
  <thead>
    <tr>
      <th>task_id</th>
      <th>energy/atom (eV)</th>
      <th>pressure (GPa)</th>
      <th>structure kept?</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">mp-655585-0-0</code></td>
      <td>-5.4253</td>
      <td>-0.13</td>
      <td><strong>yes</strong></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">mp-149-1-1</code></td>
      <td>-5.4243</td>
      <td>-11.87</td>
      <td>no</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">mp-149-1-0</code></td>
      <td>-5.4228</td>
      <td>19.25</td>
      <td>no</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">mp-11721-1-1</code></td>
      <td>-5.4205</td>
      <td>-24.46</td>
      <td>no</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">mp-11721-1-0</code></td>
      <td>-5.4149</td>
      <td>40.82</td>
      <td>no</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">mp-1057366-1-1</code></td>
      <td>-5.2118</td>
      <td>-116.43</td>
      <td>no</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">mp-1057366-1-0</code></td>
      <td>-5.0935</td>
      <td>311.51</td>
      <td>no</td>
    </tr>
  </tbody>
</table>

<p>Seven snapshots of the same material under different amounts of strain, all valid CHGNet training data. materialsDB keeps the structure for exactly one of them — the lowest-energy task — because its job is “what does silicon’s ground state actually look like,” not “teach a neural network the shape of silicon’s energy surface.” Six structures get discarded per material, on average, for a job that was never asking for them.</p>

<h2 id="what-gets-thrown-away-and-why-thats-a-real-cost">What Gets Thrown Away, and Why That’s a Real Cost</h2>

<p>Worth being honest about this rather than calling it free cleanup: those six structures are a real loss, not redundant data.</p>

<p>The schema keeps <code class="language-plaintext highlighter-rouge">max_force</code> — a single scalar, the largest force magnitude across all atoms in that task. It does not keep the full per-atom force vectors, and it doesn’t keep the structure those forces were computed against. That’s the actual damage: a force value only means something in reference to which atom sat where. <code class="language-plaintext highlighter-rouge">max_force</code> with no structure attached is a diagnostic number. It can’t be replayed into a training loop, can’t be used to fine-tune a potential, can’t reconstruct the energy surface’s curvature around the minimum, can’t be reused the next time someone wants to benchmark a newer model against MPtrj’s reference structures.</p>

<p>So this isn’t “we pruned redundant data.” It’s “we discarded scientifically real training value that MPtrj was built to provide, because this API’s job — look up a material’s ground-state properties — never needed it.” A legitimate scope decision. Not a free lunch.</p>

<h2 id="dedup-the-first-20x">Dedup: The First ~20x</h2>

<p>The first and biggest reduction happens before any compression, in the ingest step:</p>

<div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">WITH</span> <span class="n">ranked</span> <span class="k">AS</span> <span class="p">(</span>
    <span class="k">SELECT</span> <span class="o">*</span><span class="p">,</span>
        <span class="n">ROW_NUMBER</span><span class="p">()</span> <span class="n">OVER</span> <span class="p">(</span>
            <span class="k">PARTITION</span> <span class="k">BY</span> <span class="n">material_id</span>
            <span class="k">ORDER</span> <span class="k">BY</span> <span class="n">energy_per_atom</span> <span class="k">ASC</span> <span class="n">NULLS</span> <span class="k">LAST</span>
        <span class="p">)</span> <span class="k">AS</span> <span class="n">rn</span>
    <span class="k">FROM</span> <span class="n">raw</span>
<span class="p">)</span>
<span class="k">SELECT</span> <span class="p">...</span> <span class="k">FROM</span> <span class="n">ranked</span> <span class="k">WHERE</span> <span class="n">rn</span> <span class="o">=</span> <span class="mi">1</span>
</code></pre></div></div>

<p>One structure per material, picked by lowest energy. Every other task keeps its five scalar properties in a separate <code class="language-plaintext highlighter-rouge">material_tasks</code> table — cheap, no JSON, no BLOB — but loses its structure. That single decision takes the dataset from 11.35GB down to roughly <strong>592MB</strong>: a ~20x reduction, and it happens purely from deciding what the API needs to answer, before a single byte gets compressed.</p>

<h2 id="compression-592mb--266mb">Compression: 592MB → 266MB</h2>

<p>The remaining 592MB is ~96% <code class="language-plaintext highlighter-rouge">structure_json</code> — one pymatgen <code class="language-plaintext highlighter-rouge">Structure</code> blob per material. Two more things happen to it.</p>

<p><strong>Strip before compressing.</strong> Raw pymatgen JSON is verbose — <code class="language-plaintext highlighter-rouge">{"species":[{"element":"Si","occu":1}],"abc":[0,0,0]}</code> per site. A benchmark script (<code class="language-plaintext highlighter-rouge">scripts/bench_encoding.py</code>) compared four encodings head to head — raw JSON, zlib-only, stripped-JSON-only, stripped+zlib — by sampling 1,000 random structures and measuring both size and decode latency. Stripped-then-compressed won: verbose JSON key names don’t compress as well relative to their size as a compact positional array does. The on-disk shape is:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>{"l":[[...]],"s":[["Si",0,0,0,1],["Si",0.25,0.25,0.25,1]]}
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">l</code> = lattice matrix, <code class="language-plaintext highlighter-rouge">s</code> = sites as <code class="language-plaintext highlighter-rouge">[element, a, b, c, occupancy]</code> arrays. Then zlib on top of that.</p>

<p><strong>Version the wire format, don’t do a hard cutover.</strong> Each blob gets a 4-byte header — magic byte, version, codec id, reserved — before the payload:</p>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">header</span> <span class="o">:=</span> <span class="p">[</span><span class="m">4</span><span class="p">]</span><span class="kt">byte</span><span class="p">{</span><span class="m">0x53</span><span class="p">,</span> <span class="n">version</span><span class="p">,</span> <span class="n">codecByte</span><span class="p">,</span> <span class="m">0x00</span><span class="p">}</span>
</code></pre></div></div>

<p>A <code class="language-plaintext highlighter-rouge">Registry</code> decodes by dispatching on the version byte, and legacy uncompressed rows (no magic byte at the front) get returned as-is instead of erroring. That’s what let the actual production database get migrated in place with a one-time script, rather than requiring a full re-ingest or a moment where old and new rows couldn’t coexist.</p>

<p>End result: 592MB → <strong>266MB</strong>. Combined with the dedup step, that’s roughly a <strong>97.7% reduction</strong> from the original 11.35GB source file.</p>

<pre><code class="language-mermaid">graph LR
    A["MPtrj source&lt;br/&gt;11.35GB"] --&gt; B["Dedup: one structure&lt;br/&gt;per material — ~592MB"]
    B --&gt; C["Strip + zlib,&lt;br/&gt;versioned codec — 266MB"]
    C --&gt; D["Served: WAL + mmap,&lt;br/&gt;sub-500ms warm"]
    style A fill:#f3f4f6,stroke:#6366f1,color:#111827
    style D fill:#eef2ff,stroke:#6366f1,color:#111827
</code></pre>

<h2 id="serving-it-fast">Serving It Fast</h2>

<p>None of the size work matters if queries are slow, so the serving side gets its own set of decisions:</p>

<ul>
  <li><strong>WAL mode + 1GB mmap</strong> — at 266MB, the entire database fits comfortably inside a single mmap region, so reads hit page cache instead of disk after the first touch.</li>
  <li><strong>A channel-based pool of read-only connections</strong>, sized to <code class="language-plaintext highlighter-rouge">GOMAXPROCS</code>, each one running warmup queries against the key indexes (primary key, formula, bandgap, the material→task foreign key) on boot — so the <em>first</em> real request isn’t also the <em>first</em> time an index gets touched.</li>
  <li><strong>Singleflight</strong> collapses concurrent identical requests into one query — 100 requests for the same material in flight becomes one SQLite call, replayed to all 100 callers.</li>
  <li><strong>An LRU cache</strong> in front of both material lookups and search results, since the data is static — no invalidation logic needed.</li>
</ul>

<p>Worth stating the honest caveat rather than only the good number: this is all <em>warm-path</em> performance. On a 512MB Fly.io VM, the very first request after a cold start pays the cost of mmap page-fault warmup against a 266MB file — 15 to 24 seconds. Every request after that is sub-500ms. That tradeoff is a property of running on a small VM with a memory-mapped file, not something the caching layers paper over — it’s worth knowing about if you’re the one hitting the API first after a deploy.</p>

<h2 id="what-it-adds-up-to">What It Adds Up To</h2>

<p>Most of the size reduction here didn’t come from a clever encoding trick — it came from asking what the API actually needed to answer, and being honest that the answer to “do we need the other six structures” was no, even though those structures were genuinely valuable for the job the dataset was originally built for. The compression work (266MB from 592MB) is the smaller, more mechanical half of the story. The bigger number (592MB from 11.35GB) came from a single <code class="language-plaintext highlighter-rouge">ROW_NUMBER()</code> and a decision about scope.</p>]]></content><author><name>ikouchiha47</name></author><summary type="html"><![CDATA[materialsDB is a small Go API serving 145,923 materials from the Materials Project, backed by a single SQLite file. Live at materials-db.fly.dev.]]></summary></entry><entry><title type="html">Chasing 1 Million RPS</title><link href="https://ikouchiha47.github.io/millionrps/" rel="alternate" type="text/html" title="Chasing 1 Million RPS" /><published>2026-08-03T00:00:00+00:00</published><updated>2026-08-03T00:00:00+00:00</updated><id>https://ikouchiha47.github.io/millionrps</id><content type="html" xml:base="https://ikouchiha47.github.io/millionrps/"><![CDATA[<p><em>yes, this is clickbait. kind of.</em></p>

<h2 id="what-1-million-rps-actually-means">What “1 Million RPS” Actually Means</h2>

<p>“1 million RPS” is not a single number. It depends entirely on what the server is doing per request.</p>

<p>A handler that returns <code class="language-plaintext highlighter-rouge">{"status":"ok"}</code> with no I/O can hit 10-20M RPS on a single machine. A handler that does a primary-key SELECT on a warm Postgres index might do 50k-200k RPS before the database becomes the ceiling. A write with fsync — maybe 5k-20k. An aggregation query with a table scan: 500. These are not the same problem and should not share a headline.</p>

<p>The breakdown matters:</p>

<p><strong>Read-heavy (cache or memory):</strong> latency is microseconds, bottleneck is the network stack and CPU cycles in the request path — TCP, kernel syscalls, HTTP parsing, serialization. This is where IRQ pinning, prefork, and buffer tuning show up. This is the regime we’re in for <code class="language-plaintext highlighter-rouge">/simple</code>.</p>

<p><strong>Compute-intensive (CPU-bound, no I/O):</strong> bottleneck is CPU cores. Throughput scales linearly with cores until you hit scheduling overhead. <code class="language-plaintext highlighter-rouge">/compute</code> in this journal is this case — 128 cores saturated, 1.7M RPS.</p>

<p><strong>Read with DB (SELECT):</strong> you now have a latency floor set by the database round-trip (0.2ms local, 1-5ms over network). At 1ms average, Little’s Law says you need 1000 concurrent connections to drive 1M RPS from a single DB connection pool. Connection limits, query planning, index hits, buffer pool size — all of these cap you before the HTTP layer does.</p>

<p><strong>Write with DB (INSERT/UPDATE):</strong> add write amplification, WAL flushes, lock contention, replication lag. fsync at 10ms latency means 100 RPS per write path, not 1M. Batching and async writes push this up but introduce consistency trade-offs.</p>

<p><strong>Failure handling:</strong> a server doing retries, circuit breaking, or fallback logic on each request burns CPU per failure. At high RPS, even 0.1% error rate with a retry doubles load on a degraded downstream. Failure handling changes the per-request cost model entirely.</p>

<p><strong>Consistency:</strong> a linearizable key-value write (single-node, synchronous) is fast. A distributed write requiring quorum adds network round-trips per request. Eventual consistency lets you write to a single replica and replicate async — throughput goes up, guarantees go down.</p>

<p>The 1M RPS target is a lens, not a finish line. It forces you to be precise about what work the server is actually doing, because vague claims collapse immediately when you try to reproduce them. What workload? What latency? What hardware? What was the client doing?</p>

<p>This journal documents those questions, not the headline number.</p>

<h2 id="what-were-testing">What We’re Testing</h2>

<ul>
  <li><strong>HTTP servers in Go</strong> — net/http, fasthttp, fiber — with different workloads (tiny JSON, large payloads, CPU-bound computation)</li>
  <li><strong>Kernel network stack tuning</strong> — IRQ affinity, RPS, RFS, SO_REUSEPORT, socket options</li>
  <li><strong>Infrastructure</strong> — single machine vs cross-machine, AWS instance families, NIC limitations</li>
  <li><strong>Load generation</strong> — vegeta, autocannon, wrk — and why the tool matters as much as the server</li>
  <li><strong>Tradeoffs</strong> — prefork vs single process, pipelining vs independent requests, throughput vs latency</li>
</ul>

<h2 id="the-setup">The Setup</h2>

<p>All benchmarks run on AWS (ap-south-2, Hyderabad) unless otherwise noted. Server code is Go + Fiber v3. Load generation uses autocannon (for pipelining tests) or vegeta (for rate-controlled tests).</p>

<p>Source code: <a href="https://github.com/ikouchiha47/millionrps">ikouchiha47/millionrps</a></p>

<h2 id="chapters">Chapters</h2>

<p>Each chapter is a different class of high-throughput problem. Same hardware, same Go, different bottlenecks.</p>

<p><strong>Chapter 1 — Simple HTTP:</strong> a handler returning a large JSON payload. No I/O, no computation. Ceiling is the network stack: IRQ affinity, prefork, NIC bandwidth, compression.</p>

<p><strong>Chapter 2 — Fan-out (SSE likes):</strong> one write → N subscribers. The metric shifts from RPS to events delivered per second. Bottlenecks are goroutine contention, IPC overhead, and the cost of fanning a single event to 64K open connections.</p>

<p><strong>Chapter 3 — Metrics ingestion</strong> <em>(planned)</em>: write-heavy, time-series, aggregation at ingestion. Different shape again.</p>

<h2 id="journal">Journal</h2>

<p><em>Entries below are in chronological order. Green = done, orange = in progress, grey = planned.</em></p>]]></content><author><name>ikouchiha47</name></author><summary type="html"><![CDATA[yes, this is clickbait. kind of.]]></summary></entry><entry><title type="html">Materials Science Engineering Notes</title><link href="https://ikouchiha47.github.io/p/b8e2c4/materials-science-notes/" rel="alternate" type="text/html" title="Materials Science Engineering Notes" /><published>2026-08-02T00:00:00+00:00</published><updated>2026-08-02T00:00:00+00:00</updated><id>https://ikouchiha47.github.io/p/b8e2c4/materials-science-notes</id><content type="html" xml:base="https://ikouchiha47.github.io/p/b8e2c4/materials-science-notes/"><![CDATA[<p>My brief stint at a materials science company. These are technical notes on the engineering behind a research platform — agent architectures, hypothesis-driven workflows, and document processing pipelines for scientific papers.</p>]]></content><author><name>ikouchiha47</name></author><summary type="html"><![CDATA[My brief stint at a materials science company. These are technical notes on the engineering behind a research platform — agent architectures, hypothesis-driven workflows, and document processing pipelines for scientific papers.]]></summary></entry><entry><title type="html">Learnings from Building LLM Systems</title><link href="https://ikouchiha47.github.io/2026/08/01/learnings-materials-science.html" rel="alternate" type="text/html" title="Learnings from Building LLM Systems" /><published>2026-08-01T00:00:00+00:00</published><updated>2026-08-01T00:00:00+00:00</updated><id>https://ikouchiha47.github.io/2026/08/01/learnings-materials-science</id><content type="html" xml:base="https://ikouchiha47.github.io/2026/08/01/learnings-materials-science.html"><![CDATA[<p>My previous employment was at a company in the <code class="language-plaintext highlighter-rouge">Digital asset management</code> space, working on making short form commercial videos, towards the end.</p>

<p>One of the USP was, consistent character generation, which was until <a href="https://blog.google/technology/ai/nano-banana-pro/">Nano Banana</a>, and soon other mainstream models caught up. So the switch was to
generate short form commercial videos.
Once the shop closed, I spent time reflecting on how things went, and how the overlap landscape changes with AI in picture.
One of those thoughts gave birth to <a href="https://github.com/ikouchiha47/krearts">krearts</a></p>

<p>During the time I was also:</p>

<ul>
  <li>Helping bootstrap an LLM-powered research platform for materials scientists.
The kind where researchers upload papers, ask questions, and get structured answers with citations. Not a chatbot. A system.</li>
  <li>Was trying to make hot-chocolate, because 90% of hot chocolate served in Bangalore is either a middle-class bourvita drink with extra hot water,
and the rest 10% adds a bunch of non-sense to hide their pathetic hot-chocolate.</li>
</ul>

<p>This blog documents some of the observations, in building a research oriented framework, and my understanding of this space, apart from code.</p>

<p>For the engineering deep-dive on the research platform itself, see <a href="/p/b8e2c4/materials-science-notes/">Materials Science Engineering Notes</a>:</p>
<ul>
  <li><a href="/materials-science/progressive-indexing.html">Progressive Indexing: Making Papers Searchable in Seconds</a></li>
  <li><a href="/materials-science/anatomy-of-a-research-agent.html">Anatomy of a Research Agent</a></li>
  <li><a href="/materials-science/hypothesis-engineering-loop.html">From Paper Chaos to Testable Hypotheses</a></li>
</ul>

<hr />

<h2 id="the-gpt-wrapper-fallacy">The GPT wrapper fallacy</h2>

<p>When people see ChatGPT, Claude, Grok — the assumption is that building an LLM product is just API calls with a nice UI. Upload a PDF, call the model, return the answer. Ship it.</p>

<p>The gap between <em>using</em> these tools and <em>building with</em> them is massive.</p>

<p>Attachment parsing alone is a project. Scientific PDFs have multi-column layouts, inline equations, tables that span pages, figures with captions that reference other figures. Getting clean text out of that isn’t <code class="language-plaintext highlighter-rouge">pdf2text</code>. It’s a pipeline — layout detection, table extraction, figure-caption association, section boundary identification. And every format is different. A Nature paper looks nothing like an arXiv preprint.</p>

<p>Then there’s retrieval. Without preprocessing and indexing:</p>
<ul>
  <li>Every question re-parses everything from scratch.</li>
  <li>Every query pays the full cost of understanding the document.</li>
</ul>

<p>The first prototype worked exactly this way - no tagging of sections, no embeddings, only enriched extractions.</p>

<p><strong>It was slow, and it had to be tweaked continuously to support new types of queries</strong></p>

<p>Corrective RAG, structured extraction, citation grounding — each turned out to be its own subsystem with its own failure modes.
The “wrapper” ended up being the smallest part of the system.</p>

<p><a href="https://docs.crewai.com/">Crewai</a> should get more credit, it is like the <code class="language-plaintext highlighter-rouge">rails</code> of <code class="language-plaintext highlighter-rouge">ruby</code>.
Especially the memory management, extensibility, tooling and integrations. It also has a inbuilt support for varieties of doc format.
For a smaller startup with captial, one could throw <code class="language-plaintext highlighter-rouge">GPUs</code> and <code class="language-plaintext highlighter-rouge">docling</code> at the problem, 
Otherwise choose to build the intermediate stages.</p>

<p>Similar could be said for langchain and langgraph. But I would relate it more to <code class="language-plaintext highlighter-rouge">sinatra</code>.</p>

<p><strong>N.B: Watchout for the event management system of crewai, its a app level global situation, but one can use async context to map events to connections</strong></p>

<hr />

<h2 id="what-actually-happens-to-a-query">What actually happens to a query</h2>

<p>A user types: <em>“What compositions showed the lowest formation energy across these three papers?”</em></p>

<p>The naive assumption is “send to LLM, get answer.” In practice, there’s a whole pipeline before the model ever sees the question:</p>

<p><strong>Query expansion.</strong> The raw question gets rewritten. “Formation energy” might need to expand to “enthalpy of formation,” “DFT-computed stability,” or specific notation like ΔHf. A single user question becomes multiple retrieval queries.</p>

<p><strong>Intent classification.</strong> Is this a comparison across papers? A lookup in a single table? A synthesis question that needs reasoning? The retrieval strategy changes depending on the answer.</p>

<p><strong>Hybrid retrieval.</strong> Full-text search (FTS), n-gram matching, and semantic embeddings each catch different things. FTS finds exact terms. N-grams handle partial matches and chemical formulas that embedding models mangle. Embeddings capture semantic similarity — “thermal stability” matching “resistance to decomposition.”</p>

<p><strong>Reranking.</strong> The initial retrieval casts a wide net. A reranker (cross-encoder or LLM-based) scores each chunk against the original question for fine-grained relevance. This is where you go from “related passages” to “the actual answer is in these three paragraphs.”</p>

<blockquote>
  <p>Reciprocal Rank Fusion (RRF) merges results from different retrieval methods into a single ranked list. The LLM then reasons over the top results instead of just returning links.</p>
</blockquote>

<p>None of this retrieval composition is new. Google has done this for decades. The difference is composing these with an LLM reasoning loop instead of hand-tuned ranking signals.</p>

<p><strong>What this replaces:</strong>
Traditionally, doing this well meant deploying <strong>Elasticsearch or Solr</strong> — heavy infrastructure with its own operational cost, query DSLs, analyzers, synonym dictionaries, spell-check configs, and tokenizer tuning.</p>

<p>With an LLM and vector search, a lot of that goes away:</p>

<ul>
  <li><strong>Spell correction, synonyms, query reformulation</strong> — handled natively by the LLM. “therml stability” still matches “thermal stability” because the embedding is close enough, and the LLM rewrites the query anyway.</li>
  <li><strong>The search cluster itself</strong> — a vector database (or even just <code class="language-plaintext highlighter-rouge">pgvector</code>) plus FTS on Postgres replaces what used to require a dedicated search deployment.</li>
</ul>

<blockquote>
  <p>The complexity of maintainance cost and migration costs, associated with elasticsearch, and its java baked ecosystem reduces to something much simpler - <code class="language-plaintext highlighter-rouge">GPUs</code>, <code class="language-plaintext highlighter-rouge">Database</code>, and <code class="language-plaintext highlighter-rouge">API calls</code></p>
</blockquote>

<hr />

<h2 id="the-preprocessing-tax">The preprocessing tax</h2>

<p>Without an embedding and indexing pipeline, every query pays the full cost. Parse the PDF. Chunk it. Embed the chunks. Search. Answer. For a 40-page paper, that’s 30+ seconds before the user sees anything.</p>

<p>The alternative is progressive indexing: make the paper useful immediately and build deeper indexes in the background.</p>

<p>This works in tiers:</p>

<ol>
  <li><strong>Instant</strong> — Raw text extraction + section splitting. The paper is searchable within seconds.</li>
  <li><strong>Fast</strong> — Table detection, figure extraction, metadata parsing. Available within a minute.</li>
  <li><strong>Deep</strong> — Full embedding generation, entity extraction, cross-reference resolution. Runs in the background.</li>
</ol>

<p>A query that arrives at tier 1 gets FTS-only retrieval. Not perfect, but fast and useful. By the time the researcher has read the first answer and typed a follow-up, tier 2 or 3 is ready.</p>

<p>The key insight: researchers don’t upload a paper and immediately ask their hardest question. They start with “what’s this about?” and work their way to specifics. Progressive indexing matches the system’s readiness to the user’s actual behavior.</p>

<p>This is the same pattern behind <a href="/2025/10/02/media-search.html">Cinestar’s</a> five-phase video indexing pipeline — make a video searchable the moment it’s uploaded (phase 0, basic metadata), then progressively refine with multi-modal enrichment, coarse segmentation, fine segmentation, and cross-reference passes.</p>

<p>The domain is different but the architecture is identical: immediate utility, background refinement, each tier unlocking better search quality.</p>

<hr />

<h2 id="graph-rag--where-it-fits">Graph RAG — where it fits</h2>

<p>Graph RAG has real value. But the costs are real too.</p>

<h3 id="single-document-usually-not-worth-it">Single document: usually not worth it</h3>

<p>Building a knowledge graph over <strong>one document</strong> requires:</p>
<ul>
  <li>Full entity extraction — identifying entities, properties, conditions, methods</li>
  <li>Relationship extraction between them</li>
  <li>Co-reference resolution</li>
  <li>Building a traversable graph</li>
</ul>

<p>For a single document, this is expensive relative to the payoff. A well-chunked document with good metadata gets roughly 80% of the way there.</p>

<p><strong>The better alternative</strong> — <code class="language-plaintext highlighter-rouge">section_covers</code>.</p>

<p>No matter how unstructured a PDF layout looks, the domain and the humans in it have a structure. Every scientific paper has an implicit hierarchy — title, abstract, hypothesis, methods, results, conclusion. The sections might be named differently, merged together, or split across pages, but the structure is always there. Researchers read papers this way instinctively.</p>

<p>The idea: teach the LLM this structure through the prompt, and have it classify each chunk during ingestion. The classification is an array — <code class="language-plaintext highlighter-rouge">["methods", "results", "datasets"]</code> — not a single label, because sections overflow. A “methods” section often contains datasets and preliminary results too.</p>

<p><strong>How the LLM knows the structure.</strong> The agent prompt is hierarchically organized with custom tags — identity, capabilities, workflows, security, output rules — each scoped and nested. Within this, the paper-reading workflows define phased strategies:</p>

<ul>
  <li><strong>Summarization:</strong> extract six elements from any paper — hypothesis, claim, evidence, assumption, experiment, result — mapped to section types</li>
  <li><strong>Comparison:</strong> compare across papers element-by-element, using section types as the navigation axis</li>
  <li><strong>Hypothesis generation:</strong> scan → deep-read → synthesize, with section-aware retrieval at each phase</li>
</ul>

<p>The <code class="language-plaintext highlighter-rouge">Researcher</code> agent, is given detailed instructions on how a “researcher” reads — which sections to check first for which kind of question, when to fall back to broader reading, how to cross-reference across documents.</p>

<p>At query time, filtering by section type is a simple indexed array lookup.
“Show me just the methods across these three papers” — no graph traversal needed. The LLM can then drive a ReAct loop to compare across sections, navigate the hierarchy, and synthesize — all without an entity graph.</p>

<h3 id="across-a-workspace-over-time-where-it-shines">Across a workspace over time: where it shines</h3>

<p>Researchers don’t work with one paper. They work with a dozens of papers, experimental notes, simulation results, reviewer feedback. Over weeks and months, connections accumulate:</p>
<ul>
  <li>This paper’s synthesis conditions produced the same phase as that paper’s computational prediction</li>
  <li>This reviewer’s objection was addressed by that experiment</li>
  <li>This failed hypothesis ruled out a class of compositions</li>
</ul>

<p>That’s where a knowledge graph becomes valuable. <strong>Not blind LLM extraction</strong> — asking a model to “extract all entities” from a paper produces confident garbage.</p>

<p>What works is:</p>
<ul>
  <li><strong>Curated, validated connections</strong> built incrementally</li>
  <li><strong>Human-in-the-loop validation</strong> for each new paper and result</li>
  <li>The graph grows as the research grows</li>
</ul>

<blockquote>
  <p>Graph RAG for single-document retrieval is usually overkill. Graph RAG as an epistemic knowledge web built over months of research — that’s where it becomes worth the investment.</p>
</blockquote>

<hr />

<h2 id="the-cage-and-the-wind">The cage and the wind</h2>

<p>Models get switched. Pricing changes, a new model drops with better structured output, an open-source option gets good enough for a subtask. Each time, it means rewriting prompts and fixing output parsing — unless the system is built for it.</p>

<p>The prompt is the cage geometry — it shapes the output. But real portability comes from treating LLM output as untrusted input.</p>

<ul>
  <li>Expected a JSON object with specific fields? Parse it, validate the schema, check the types.</li>
  <li>Expected citations? Verify they reference real passages in the retrieved chunks.</li>
  <li>Expected a numerical comparison? Check that the numbers actually appear in the source material.</li>
</ul>

<p>When validation fails, the system retries with a corrective prompt that includes the validation error. Self-correcting loops. In practice, most queries resolve in one pass. Some need two. When it takes three, the problem is almost always in the prompt design or the retrieval, not the model.</p>

<p>This leads to a useful model-agnostic metric: not “which model is best” but “how many correction cycles does this model need for this task.” GPT-4 might need one cycle where Claude needs two, or vice versa, depending on the task class. The system handles both.</p>

<p>One observation: most open-source models share failure modes — they’re fine-tuned from the same bases. A correction loop that handles Llama’s JSON formatting quirks tends to handle Mistral’s too. Build the cage right and the wind can change direction.</p>

<hr />

<h2 id="the-context-window-problem">The Context Window Problem</h2>

<p>I wished I could write very passionately about these things, but I don’t really find much challange or learnings
in terms of high level abstractions.</p>

<p>Some of the instruction following of LLM has gotten better over time, so the <code class="language-plaintext highlighter-rouge">ReACT</code> is a good place to start at.
So the only two things that remain:</p>

<ol>
  <li>Handle Compression of Context</li>
  <li>Use memory agent with a storage backed, and expose the tools of the agent.</li>
</ol>

<p>The interesting parts are in the:</p>
<ul>
  <li>details of how to better store or structure the data for storage.</li>
  <li>combining sparse and dense embeddings</li>
  <li>how year or month long conversations can be retrieved.</li>
</ul>

<p>Rest of everything even <code class="language-plaintext highlighter-rouge">RLM</code>s sounds like a fancy FAD to me.</p>

<blockquote>
  <p>Using python env to run code, to get information from context. And if it gets the code wrong does it retry? how many tokens per roundtrip? how do you run it securely? how about the llm lieeeeessssss in the code?.</p>
</blockquote>

<p><strong>ReACT</strong> should be evolved into, <code class="language-plaintext highlighter-rouge">ParallelReACT</code>, <code class="language-plaintext highlighter-rouge">Goal Planning</code>, and other mechanisms that are already well established in gaming systems. Instead of <em>Why don’t we replace a simple predictible agent and retrival system into a token vaccum and a security knightmare, rather than have a proper data structure and ways to query them!! SQL is for insane people</em></p>

<hr />

<h2 id="video-generation-spatial-awareness-and-hot-chocolate">Video Generation, Spatial Awareness and Hot Chocolate</h2>

<p>Foundational models are trained on a lot of data. The assumption is that because of how embedding spaces work, the LLM would be the one that sees all patterns — but more powerful.</p>

<p>Maybe it does. But what it does and doesn’t depends on <strong>how it was trained</strong>. Combining different domains means mapping them to the same embedding space. General LLMs are not an answer to this.</p>

<h3 id="domain-knowledge-has-layers">Domain knowledge has layers</h3>

<p>In video generation, the underlying models have to be tweaked enough to guard how the LLM generates media. The nuances are real:</p>

<table>
  <thead>
    <tr>
      <th>Level</th>
      <th>What it determines</th>
      <th>Example</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>Macro</strong></td>
      <td>Physics, environment, scene composition</td>
      <td>Bags are not displayed the same way as watches</td>
    </tr>
    <tr>
      <td><strong>Micro</strong></td>
      <td>Audience, messaging, tone</td>
      <td>A wrist watch is not advertised the same way as a wall clock. A mechanical watch is not advertised the same way as a quartz.</td>
    </tr>
    <tr>
      <td><strong>Cultural</strong></td>
      <td>Aesthetics, conventions, expectations</td>
      <td>A Japanese website looks nothing like a US-built website. Japanese fashion magazines are structured for data extraction — what to wear, how to pair it, what it’s for.</td>
    </tr>
  </tbody>
</table>

<p>LLMs tend to take the path of least work. Hence the need for detailed planning, guardrails, and extensive instruction following. If these foundational models had all this world knowledge baked in, why would anyone need custom embedding models?</p>

<blockquote>
  <p>This sounds more like a Mixture of Experts, but constrained to a domain.</p>
</blockquote>

<h3 id="llms-lack-spatial-awareness">LLMs lack spatial awareness</h3>

<p>When working with non-textual data, it’s fairly impossible for an LLM to predict things in real life. Back in very late 2025, none of the LLM models were consistently good at:</p>
<ul>
  <li>Dynamic camera angles mid-scene</li>
  <li>Heavy action sequences — even something simple as parkour would fall apart</li>
  <li>Predicting anything that needs <strong>visual cues or real-world feedback loops</strong></li>
</ul>

<p>The failure has multiple layers, and it’s well-documented:</p>

<ul>
  <li><strong>Text encoders lose spatial information before generation even starts.</strong> CLIP-based encoders (used in most diffusion models) establish representations in early layers and don’t compose spatially. T5-based encoders (Imagen, DeepFloyd) do ~10% better because they process sequentially — but still fall short. (<a href="https://arxiv.org/abs/2311.17937">Unlocking Spatial Comprehension in T2I Diffusion Models</a>)</li>
  <li><strong>Training data rarely contains explicit spatial language.</strong> Captions say “a dog in a park” not “a dog positioned 2 meters left of a bench.” The models have minimal exposure to spatial relations like “inside”, “below”, “smaller than.” (<a href="https://arxiv.org/html/2403.00587">Improving Explicit Spatial Relationships in T2I Generation</a>)</li>
  <li><strong>No internal 3D or physics model.</strong> The generator learns statistical co-occurrence — “pocket” appears with “phone”, “boombox” appears with “music player” — but never learns that a pocket has a fixed volume, or that an object must be smaller than its container.</li>
  <li>Ask an image model for “a big music player bulging in someone’s pocket” — you’ll get a person holding a boombox. It retrieved the strongest visual pattern for “big music player”, never computed whether it fits inside the pocket.</li>
  <li><a href="https://arxiv.org/abs/2307.06350">T2I-CompBench</a> (NeurIPS 2023) confirms <strong>spatial relationships are the weakest category</strong> across all tested models. OpenAI’s own <a href="https://arxiv.org/html/2402.17177v2">Sora technical report</a> acknowledges failures in physics, causality, and left/right differentiation.</li>
</ul>

<p>The precise term from the literature: <strong>statistical co-occurrence without compositional constraint satisfaction</strong>. The models know what things look like together, but can’t enforce constraints between them.</p>

<blockquote>
  <p>LLMs are probabilistic systems. <code class="language-plaintext highlighter-rouge">If you expect a probabilistic system to reliably (do X), it will probably, reliably (do X)</code></p>
</blockquote>

<h3 id="the-hot-chocolate-test">The hot chocolate test</h3>

<p>GPT has a lot of information about how good hot chocolate is made. It can tell you how different countries and regions like theirs, <strong>if you ask for it</strong>. It has access to all kinds of recipes, ratings, discussions — enough for a fair idea.</p>

<p>But an LLM has never <strong>seen or tasted</strong> hot chocolate. It relies on what I call <strong>“collective truth”</strong>. So it can’t accurately predict how changes in quantities lead to different taste.</p>

<p>This is the same with materials research — CHGNet and other tools provide contextual models with <strong>computation baked in</strong>, rather than an LLM trying to predict outcomes.</p>

<p>When I asked the LLM to adjust quantities for 4 people, the amount of water and sugar was way off from reality. <strong>What actually worked:</strong></p>
<ul>
  <li>The LLM knew the <strong>desired result</strong> of each step — “a creamy paste, not pudding-thick, but not watery”</li>
  <li>The LLM knew what <strong>good taste means</strong> (not going into the details of one-shot or ReAct)</li>
  <li>But it needed <strong>real-world feedback</strong> to get the quantities right</li>
</ul>

<h3 id="the-sensory-gap">The sensory gap</h3>

<p>For automation, this means: as long as the LLM has access to eyes, ears and other senses into the real world, foundational models can actively guide towards real-life usable outcomes.</p>

<p>The same principles apply to sequential or batched image and video generation — with a corrective feedback loop. But costs shoot up.</p>

<p><em>With enough effort — YOLO for vision, RPi Zero or ESP32 to capture images in batches, actual instruments and sensors providing continuous feedback — most real-life applications of AI will come when we add sensory elements to it. The LLM continuously validates against the desired outcome at each stage.
The easier version would be just to let an app do the compute</em></p>

<hr />

<h2 id="ui-in-the-era-of-llms">UI in the era of LLMs</h2>

<p>This is still a very interesting field, which is realted to one of my projects, <code class="language-plaintext highlighter-rouge">adaptui</code>. I think, one day, <code class="language-plaintext highlighter-rouge">UI</code> would be built on fly.
The cost is an inherent problem - not just token cost, but time taken. It would obviously take less time to just generate data
for a static UI, than letting an llm decide an UI. Like any other system design, we can divide the UI into two parts, static and dynamic, as well.</p>

<p>Many of the recent day llms also understand <code class="language-plaintext highlighter-rouge">react components</code> (from reactjs), tailwind, icons etc. So it would be intereseting to see
where that goes. Maybe most apps will be replaced by a <code class="language-plaintext highlighter-rouge">SuperApp</code>, with some UI protocol allowing different brands to have consistency and control over some parts of the app.</p>

<p>A good middleground right now is somewhere in between:</p>
<ul>
  <li>Either the LLM can generate the data and the html component, which has to be rendered safetly on FE</li>
  <li>Or, the LLM is given the data and the expected response structure, that is associated with a response schema.
The <code class="language-plaintext highlighter-rouge">response schema</code> in this case can be used to populate the data in the corresponding <code class="language-plaintext highlighter-rouge">react</code> component.</li>
</ul>

<p>The second one, requires some upfront thinking, like a <code class="language-plaintext highlighter-rouge">react component</code> builder, on what parts to allow to change and what is the
expected data structure. Again, the problem here is the same:</p>

<blockquote>
  <p>More things we leave it to the LLM, more the latency and chances of error increases, and hence more error recovery mechanism.</p>
</blockquote>

<p>On a bad day, you will really have a bad day.</p>

<hr />
<h2 id="testing-from-both-directions">Testing from both directions</h2>

<p>Not being a materials science expert meant there was no way to eyeball whether the system was producing good hypotheses. So the build happened in two directions simultaneously.</p>

<h3 id="direction-1-build-the-subsystems">Direction 1: Build the subsystems</h3>

<p>Same approach as building a compiler — lexer, parser, codegen, each independently testable:</p>

<table>
  <thead>
    <tr>
      <th>Subsystem</th>
      <th>What it does</th>
      <th>Testable in isolation?</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Document pipeline</td>
      <td>Parse, chunk, index scientific PDFs</td>
      <td>Yes — output is structured text, verifiable</td>
    </tr>
    <tr>
      <td>Retrieval</td>
      <td>Hybrid search across indexed papers</td>
      <td>Yes — relevance scoring against known queries</td>
    </tr>
    <tr>
      <td>Domain tools</td>
      <td>CHGNet, GPAW, materials databases</td>
      <td>Yes — known inputs, known outputs</td>
    </tr>
    <tr>
      <td>Agents</td>
      <td>Orchestrate tools, maintain context</td>
      <td>Yes — given fixed retrieval, does the plan make sense</td>
    </tr>
    <tr>
      <td>Experiment runner</td>
      <td>Execute computational experiments</td>
      <td>Yes — scripts produce reproducible results</td>
    </tr>
  </tbody>
</table>

<p>Each piece could be validated without domain expertise. The document pipeline either extracts tables correctly or it doesn’t. CHGNet either returns a valid energy prediction or it doesn’t.</p>

<h3 id="direction-2-build-the-evaluation">Direction 2: Build the evaluation</h3>

<p>The harder question: <strong>does the whole system, end-to-end, produce hypotheses that are actually good?</strong></p>

<p>The approach:</p>
<ul>
  <li>Pick a <strong>resolved scientific controversy</strong> with a known outcome (LK-99 superconductivity)</li>
  <li>Build the paper chain <strong>without</strong> the final resolution paper</li>
  <li>Feed the incomplete chain to the system <strong>in a conversational pattern</strong> — not “tell me the answer”, but the way a researcher would actually explore: “what are the competing claims?”, “what experiments would resolve this?”, “which hypothesis has the strongest evidence?”</li>
  <li>Score the generated hypotheses against the withheld paper</li>
</ul>

<p>The conversational pattern matters. A direct request — “what caused the LK-99 results?” — would test whether the model memorized the answer. A conversational exploration tests whether the <em>system architecture</em> can guide reasoning through literature, contradictions, and evidence toward a defensible hypothesis.</p>

<p>The hypothesis engine was essentially reverse-engineered from this evaluation. The question “how do you know if it’s any good?” shaped every architectural decision — what agents exist, how they communicate, what tools they call, how hypotheses get ranked.</p>

<hr />

<h2 id="shipping-llm-powered-apps">Shipping LLM Powered Apps</h2>

<p>The obvious first move is a web app. Upload papers, ask questions, get answers. Fastest to ship, easiest to demo. For a lot of teams, it’s the right choice.
But once the architecture is model-agnostic, it doesn’t actually <em>need</em> to be centralized.</p>

<p>The workspace, the agents, the retrieval layer — all of it can run on a researcher’s machine or a lab’s own infrastructure. That opens up delivery options worth considering.</p>

<h3 id="the-options-as-i-see-them">The options as I see them</h3>

<table>
  <thead>
    <tr>
      <th> </th>
      <th><strong>Web / SaaS</strong></th>
      <th><strong>Editor (JetBrains / Cursor-style)</strong></th>
      <th><strong>VSCode / Codium plugin</strong></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>Where the workspace lives</strong></td>
      <td>Cloud</td>
      <td>Researcher’s machine or lab infra</td>
      <td>Researcher’s machine</td>
    </tr>
    <tr>
      <td><strong>Model inference</strong></td>
      <td>Hosted endpoints</td>
      <td>Local, lab cluster, or hosted — their choice</td>
      <td>Same</td>
    </tr>
    <tr>
      <td><strong>Data residency</strong></td>
      <td>Provider-managed</td>
      <td>User-managed</td>
      <td>User-managed</td>
    </tr>
    <tr>
      <td><strong>Git-backed tracking</strong></td>
      <td>Possible but uncommon</td>
      <td>Natural fit — queries, experiments, hypotheses all versioned</td>
      <td>Same</td>
    </tr>
    <tr>
      <td><strong>Lab equipment access</strong></td>
      <td>Via API tunnels</td>
      <td>Direct — instruments register as tools</td>
      <td>Direct</td>
    </tr>
    <tr>
      <td><strong>Distribution</strong></td>
      <td>URL</td>
      <td>Installer / package manager</td>
      <td>Marketplace (shared across Codium-based editors)</td>
    </tr>
    <tr>
      <td><strong>Trade-off</strong></td>
      <td>Fastest to ship. Data residency is a conversation with every enterprise customer.</td>
      <td>Most integrated, most opinionated. Bigger upfront investment.</td>
      <td>Lowest barrier to adoption. Less integrated long-term.</td>
    </tr>
  </tbody>
</table>

<p>No single right answer:</p>

<ul>
  <li>SaaS is simpler to operate</li>
  <li>An editor gives researchers a workspace that feels like <em>theirs</em></li>
  <li>A plugin meets them where they already are</li>
  <li>Different labs, different constraints, different choices</li>
</ul>

<p>The interesting observation is that a model-agnostic design (the cage and the wind) is what makes these options possible at all.</p>

<h3 id="what-local-delivery-unlocks">What local delivery unlocks</h3>

<p><strong>Git-based tracking.</strong> When the workspace is local, versioning research artifacts in git becomes natural. Every query, experiment config, hypothesis — versioned. Branch a research direction. Diff two experimental setups. Revert a dead end. Researchers already think in version control for code; extending it to research is a small step.</p>

<p><strong>The device-driver pattern.</strong> With local or lab-hosted infrastructure, physical equipment can connect directly. The framework defines the tool interface; labs implement the connector for their instruments — a synthesis furnace, an XRD machine, a spectrophotometer. Same interface as any other tool in the registry. This is harder to pull off through a cloud intermediary, though not impossible.</p>

<blockquote>
  <p>Once inference is swappable — hosted, self-hosted, open-source, Model Garden, whatever — the delivery question becomes about where the <em>data</em> lives, not where the <em>model</em> lives.</p>
</blockquote>

<p>In terms of business models, at the time of writing this, I see three prevalent ones.</p>

<ol>
  <li>That bets on the foundational model being better, using simple adapters, all forms of prompt and context engineering, to build a product.</li>
  <li>The ones that are building their own foundational models, embedding models, ocr and other llm-ification of existing tools.</li>
  <li>They have a or a bunch of products, and LLM, VLM, Vector Embeddings are a driver of the product. These could be services industry as well, providing custom implementations on top of different models.</li>
</ol>

<p>The 1st one however, would probably not survive, if it’s not a niche the big players don’t want to focus on, and public data is unavailable. Without a proper moat, the fear of being invalidated or competitor proliferation is much higher.</p>

<p>The DocuSign incident is a glaring example — OpenAI launched DocuGPT, and DocuSign’s stock <a href="https://finance.yahoo.com/news/docusign-docu-falls-12-openai-044456722.html">dropped 17%</a> overnight. Open-source alternatives like <a href="https://www.opensignlabs.com/">OpenSign</a> and <a href="https://documenso.com/">Documenso</a> were already circling.</p>

<p>When everyone has a gun, you need a bigger gun (leverage).</p>

<hr />

<h2 id="thoughts">Thoughts</h2>

<p>Overall, as I understand, llm’s use on textual data, is pretty limited use of this technology. Yes one can convert a natural language to an sql query.
The system you might have built, using agents and tools, to produce a valid grounded query, might as well be one shotable with a long enough context window.</p>

<p>So there isn’t much inherent value in building such systems. Looking back, I realize, for sure, if an MVP or bootstrapped product, is headon with
existing features available on the available SaaS platform, and is presented to the users, the <strong>in-evitable, un-avoidable and un-answereable question, how is that different from GPT</strong>,
will be presented.</p>

<p>In all honesty, its probably not, because building a production grade ChatGPT like interface, with all the functionalities and edge cases take time.
It would be similar to asking <code class="language-plaintext highlighter-rouge">Build me Twitter</code>. <em>Sure, but why?</em></p>

<blockquote>
  <p>The users do not and should not care about such engineering challenges.</p>
</blockquote>

<ul>
  <li>Businesses should identify an actual gap or pain-point, which is much narrow, and yet generic enough for 10 people.</li>
  <li>Evaluate where in a LLM and its Senses are needed, if at all.</li>
  <li>Build on the narrow domain, and choose the pain points early on. Especially for bootstrap teams or startups, choosing the right battle is necessary</li>
</ul>

<p>Chat becomes another interface to communicate with the system, much like REST. And hence the comparison against GPT or other models never come along.</p>

<p>Overall I dont feel like much has affected in terms of how one does business.
When the internet came along, there were loads of websites just built with no actual substance, and as time went by,
the web just became a medium or enabler for actual labour.</p>

<p>I think the same will happen for AI, most companies, whose identities are based around LLM will not survive in the end.
Anything and most things, that was generated or is one shotable by an LLM, will eventually get replicated and saturated.</p>

<p>So one way I guess could be to build wrappers very fast, on multiple domains. I mean, there are 100s and 1000s of n8n workflows,
skills, subagents, entire repositories of “leaked” prompts for agentic coding tools.</p>

<p>The only leverage these tools have is funding. If funding, networking and marketing were readily available, everyone could sell their own spinoff of <code class="language-plaintext highlighter-rouge">opencode</code>.
Which again is business as usual. There are a lot of twitter clones, and reddit alternatives, but the rate of success doesn’t depend on just code execution.</p>

<blockquote>
  <p>Code execution at base level has always been cheap. Especially in India, where culture is mostly managerial driven. CxOs all over the world, earn disproportionately more, not because they can write code.</p>
</blockquote>

<p>And it’s getting cheaper. Cross-provider deployment can be automated pretty easily now — Terraform, Pulumi, SST, whatever your flavor. Code generation is commoditized. Porting concepts from one language’s ecosystem to another is a weekend project with a coding agent. Elixir’s supervision trees in Go, Python’s Ray actors in Rust, Ruby’s convention-over-configuration patterns anywhere — the implementation barrier between “I know this pattern exists” and “it’s running in my stack” has collapsed. Which means the moat is never in the code.</p>

<h3 id="org-changes">Org changes</h3>

<p>I don’t think this is new practice now, but still to acknowledge, AI has caused some cultural shifts in org.
It also has somehow set the wrong expectations from a lot of people. In a way, I feel the bigger question is missed.</p>

<p>It begins with educating everyone alike, juniors, seniors, management, stakeholders.</p>

<p>This is both in terms of work volume and work culture. Both of them can be solved to a good extent using code and conventions.
Maybe in the near future, instead of <code class="language-plaintext highlighter-rouge">full-stack engineers</code>, we have actual <code class="language-plaintext highlighter-rouge">product engineers</code>, who can understand product and the build it as well.</p>

<p>Until then, platforms in the org needs to bridge this gap. In such a style of development, a developer would need to be able to identify such parts of a system in the architecture planning stage. Building abstractions, configurations, and the right interfaces and pipelines which allows the developers to be able to handoff some or most of the work to the specific dept.</p>

<p>A backend dev, building a service layer, doesn’t need to what prompts are being used, what ml models are running. He/She would instead build a system, where:</p>
<ul>
  <li>Prompts are versioned and the source is decoupled</li>
  <li>Loads of configurations for models or family of models, the location of model, finetuned or not is not his headache.</li>
  <li><strong>Eval framework</strong>, no matter how lightweight, to compare against golden data</li>
  <li>Monitoring and logging</li>
  <li>Keeping a data migration plan between versions ready.</li>
</ul>

<hr />

<h2 id="the-bigger-question">The Bigger Question</h2>

<p>If you have a look at <a href="https://github.com/ikouchiha47/claude-scientific-skills">claude-skills-repo</a>, I can alwaya
go and create a few <code class="language-plaintext highlighter-rouge">sub-agents</code>, prompt so financial-analysis to life, looking at geographical data, charts etc.,
and one could write a few scripts, which can be directly used to run backtesting and quant-analysis.</p>

<p>There is this other thing - if something can be built with ai, it can be replicated with ai.</p>

<p>So the question still is, if its possible to let things happen automatically:</p>
<ul>
  <li>then how does business look?</li>
  <li>how does the concept of fairness evolve, is it going to be first mover?, only deep tech? or deep connections and pockets?</li>
  <li>software engineers are not essential? a doctor could vibe code some app.</li>
</ul>

<h3 id="the-hidden-cost-of-development">The hidden cost of development</h3>

<p>I look back at my experience with building cinestar, and I had no choice but to keep vibe coding, like a druggie.
What started off as maybe at max a week like hackathon, turned into a 2-3 month tussle with the LLM:</p>
<ul>
  <li>constantly lying/skipping</li>
  <li>writing tests to satisfy its own understanding</li>
  <li>missing the data gathering phases</li>
  <li>fixing one thing, breaking other things</li>
  <li>refactor nightmare</li>
</ul>

<p>In hindsight, if I had done an <code class="language-plaintext highlighter-rouge">assist</code> mode, instead of <code class="language-plaintext highlighter-rouge">vibe</code> mode, it would be been a much better experience.</p>

<p><strong>So, even if you know what needs to be done, seeing the entire codebase playout from inception to delivery and refactor, cycles still need human suffering behind it</strong></p>

<blockquote>
  <p>You are/should be objectively faster at writing a binary search without mistakes, than an LLM, and can do it multiple times without losing money.</p>
</blockquote>

<p>And I am not entirely sure, the people higher up in the org is necessarily using AI to write and deliver code, as soon as he gets back from a client meeting. Nor do I think they should do it at a bigger org, where more complex things are at play.</p>

<p>I am also not entirely sure, why would a doctor be trusted with a vibe coded application, to use for anything substantial in his work.</p>

<p>As we move more closer to the real world, we see that apart from LLMs there are more focussed models and maths which do-not hallucinate. <strong>Here, probably, LLM becomes a driver</strong> . Eventually the outcomes predicted has to be tested in the real world,
which was quite evident from our <code class="language-plaintext highlighter-rouge">hot chochlate</code> stint.</p>

<h3 id="replication-and-ux-problem">Replication and UX problem</h3>

<p>I could not only argue that, Cinestar can be replicated, and be replicated faster, but also say, this whole project can be a collection of <code class="language-plaintext highlighter-rouge">skills</code> and <code class="language-plaintext highlighter-rouge">sub-agents</code>. For the <code class="language-plaintext highlighter-rouge">terminal-ly</code> challanged, we could expose a website or an app or a plugin.</p>

<p>The replication problem has exsisted, but the time to replicate has come down by a lot (arguably). Lets assume that it improves over time. So what’s left?</p>

<p>What actually holds value:</p>

<ol>
  <li><strong>Data moats</strong>, not code moats. The software is replicable. Your proprietary dataset isn’t. The memory agent recording execution pathways, curated knowledge, internal algorithms/workflows/secret sauces — that’s probably the moat. The code is commodity. The accumulated intelligence isn’t.</li>
  <li><strong>Distribution &gt; creation</strong>. Always was true, now brutally so. Two identical products — the one with users wins. It’s not just speed, it’s access to the audience, and also the type of audience.</li>
  <li><strong>Domain depth</strong>: Kknowing that NMDC drops after earthquakes near Bellary, that INR depreciation eats nominal returns, that specific credit card reward structures can be arbitraged — an AI can discover these things, but <strong>Someone has to know to ask the right questions</strong>. The person who’s lived in Indian markets for years asks better questions than someone who just has the same tools.</li>
</ol>

<p>The UX is also quite scattered:</p>
<ul>
  <li>Devs use it in a certain set of ways. Editors, TUIs, Plugins</li>
  <li>General dumb public, who would let products have complete autonomy of devices</li>
  <li>Non-devs who are entering into the dev territory. These are people who just want to build.</li>
</ul>

<p>Overall, even if some systems are built, maintaining them would require engineers, so the task or way of execution
in the lower layer changes. As long as there is a core product/problem which requires more work than just prompting
it out, finer integrations, are the ones that do end up surviving and keeping the influx of engineers.</p>

<ul>
  <li>Platform/infra engineers — Core services, Devops + Data Engineers</li>
  <li>Security engineers — attack surface grows with AI-generated code</li>
  <li>Domain specialists who code and audit — the quant trader, the biotech researcher, the fintech person</li>
  <li>“Taste” roles — product sense, design judgment, editorial quality</li>
</ul>

<p>** I wonder is it always going to be biased towards people who have better networking, capital and influence?**</p>

<p>I guess, Yes. But that was always true. The industry genuienly hates software engineers. There is quite some science to this,
but it rarely reflects in the way the IT system functions.</p>

<p>If I consider, that people always saw <code class="language-plaintext highlighter-rouge">Software engineering</code> as a temporary anomaly, a means to an end — a 30-year window where a person with a laptop and skill could create outsized value without capital or connections, and</p>

<p>What’s replacing it is closer to how every other industry works:</p>

<ul>
  <li>Capital lets you run more experiments faster (more API calls, more compute, more data)</li>
  <li>Network gives you distribution and domain access</li>
  <li>Speed matters because the first mover accumulates the data/users that become the moat</li>
</ul>

<blockquote>
  <p>A person building AI-driven quant tools in their bedroom can absolutely beat a bank’s trading desk on idea generation. But the bank has the capital to act on those ideas at scale, the regulatory access to trade, and the data feeds and a lot more compute and brains and speed(lol, so much more speed its diabolical)</p>
</blockquote>

<p>So the bedroom builder either joins the bank, sells to the bank, or finds a niche the bank doesn’t care about.</p>

<p>Not to mention, this shit is not for consumers, at all. All windows users, using powershell to run claude code, yeah sure. So whatever consumer adoption comes, that is going through the business/products they consume.</p>

<p>So <strong>computer science as a discipline will still be valid</strong>, the stuff in between might eventually become production-line, like amazon factory workers, where someone just monitors metrics, and the llm crashes systems, causes a crowrdstrike like incident and then blames it on the CTO’s family.</p>

<hr />

<h2 id="what-id-tell-someone-starting-this">What I’d tell someone starting this</h2>

<p>Some non-negotiables:</p>
<ul>
  <li>Configurability: as much as possible, especially with prompts, model names/bundles, iteration counts, embeeders, stores, anything that is experiment land (which is anything that the language models and embeddings touches)</li>
  <li>VersionSystem: for schema changes and prompt changes</li>
  <li>Conventions for collaboration: allow for people to participate. <strong>Platform engineers now need to consider non-tech people as junior developers, so that other teams can make the changes in-dependently</strong></li>
  <li>Abstractions: This is not the what design pattern to use.
If I were a famous person, I would add an <code class="language-plaintext highlighter-rouge">C</code> (changability) at the end of <code class="language-plaintext highlighter-rouge">CAP</code>.</li>
</ul>

<p><strong>Systems now not only need to worry about Read and Write patterns, but also which parts of the whole system can be replaced. This is Strategy Pattern applied at a more org and infra level</strong></p>

<p>Once you think, you have a problem, you should explore with existing system. A path could look like so:</p>

<ul>
  <li>Start with finding what is your delivery mechanism, and pin point on 2 maybe. This also forces you to write code for future expansion</li>
  <li>Try the whole workflow manually with your favourite tools, <code class="language-plaintext highlighter-rouge">chatgpt.com</code>, <code class="language-plaintext highlighter-rouge">codex</code>, <code class="language-plaintext highlighter-rouge">claude</code>.</li>
  <li>In case you have no idea about the field, you can use a similar approach of <code class="language-plaintext highlighter-rouge">Simulation</code>, let the llm figure out test and train and target datasets.</li>
  <li>Try to build the system using exsisting opensource tools, without much code, just bare-bones.</li>
  <li>If the above turns out to be not so diffucult, that you can wrap the tui in a API and SSE, find something else or other business model. Otherise,</li>
  <li>If the above turns out can not be completely done with a claude/opencode skills and subagents or n8n, it means you have leverage and a solution.</li>
  <li>So even if the workflow can be built by someone, neither the provider, nor the competitor would have access to the propeitory stuff.</li>
  <li>Once you have a system, which works with these already established tools like above, rebuilt them in code.</li>
  <li>Run against a bunch of queries, and multi turn conversations, and track the tools used, total iterations taken. Try ot against different models and embeddings, So, <strong>making things configurable should come from day one</strong></li>
  <li>This is where you start to see failures, and correction loops of the agent.</li>
  <li>Start tweaking the prompts and tool descriptions, add the memory/storage layer, think about the <strong>intermediate pre-processing steps</strong>, as we saw with the <code class="language-plaintext highlighter-rouge">section_covers</code>.</li>
</ul>

<p><strong>Remember, LLMs and neural nets end of the day are high on patterns, and how humans have interracted with the system. This realisation
should help you organise the data access patterns and tool prompts for them</strong></p>

<ul>
  <li>Build correction loops from day one. If the system can’t handle a malformed LLM response gracefully, it can’t handle production.</li>
  <li>Measure retrieval quality separately from generation quality. When the answer is wrong, it’s almost always because retrieval surfaced the wrong context, not because the model can’t reason.</li>
  <li>Treat every shortcut as debt. Skipping the indexing pipeline feels fast until every query takes 30 seconds. Hardcoding prompts for one model feels easy until you need to switch. Building for one document format feels simple until researchers upload the weird one.</li>
</ul>

<p>This post is the map. The territory is in the implementation.</p>]]></content><author><name>ikouchiha47</name></author><summary type="html"><![CDATA[My previous employment was at a company in the Digital asset management space, working on making short form commercial videos, towards the end.]]></summary></entry><entry><title type="html">Domain is Moat</title><link href="https://ikouchiha47.github.io/2026/07/31/domain-is-moat.html" rel="alternate" type="text/html" title="Domain is Moat" /><published>2026-07-31T00:00:00+00:00</published><updated>2026-07-31T00:00:00+00:00</updated><id>https://ikouchiha47.github.io/2026/07/31/domain-is-moat</id><content type="html" xml:base="https://ikouchiha47.github.io/2026/07/31/domain-is-moat.html"><![CDATA[<p>I thought I’d be building AI systems</p>

<p>When I joined a startup building AI generated images and videos, I expected to spend my time solving difficult generation problems.</p>

<ul>
  <li>Model routing.</li>
  <li>Evaluation.</li>
  <li>Fine tuning. (Although not my forte)</li>
  <li>Specialized pipelines.</li>
</ul>

<p>Instead, I found myself building folders full of prompt templates.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>experts/
    ecommerce/
      prompts.txt
    jewellery/
      prompts.txt
    watches/
      prompts.txt
    fashion/
      prompts.txt
</code></pre></div></div>

<p>To give credit where due, the <code class="language-plaintext highlighter-rouge">prompts.txt</code> is not a single file, but a structured set of files, which allows sharing of prompts, templating, and even versioning.</p>

<ul>
  <li>Every feature meant adding another prompt.</li>
  <li>Every failure meant tweaking another prompt.</li>
</ul>

<p>It worked well enough to ship products, but something felt fundamentally wrong.</p>

<p>Whenever I suggested evolving the architecture, the response was usually the same.</p>

<blockquote>
  <p>“We’re in a red ocean. We have to keep sailing.”</p>
</blockquote>

<p>I understood the business pressure. Speed matters.
But every week I became more convinced we were optimizing the wrong layer.</p>

<p><em>Different products fail for different reasons.</em></p>

<hr />

<p>The first thing that bothered me was how differently models behaved across categories.</p>

<p>To humans, these prompts are obviously different.</p>

<ul>
  <li>A pocket watch.</li>
  <li>A wrist watch.</li>
  <li>A wall clock.</li>
  <li>A handbag.</li>
  <li>A necklace.</li>
</ul>

<p>To a diffusion model, they are simply distributions of pixels and language.</p>

<p>Sometimes that difference matters a lot.</p>

<p>I remember asking a model to generate a watch dangling from a pocket.</p>

<p>Instead of a pocket watch hanging from a chain, it generated something closer to a wall clock attached to clothing.</p>

<ul>
  <li>The physics made no sense.</li>
  <li>The object relationship was wrong.</li>
  <li>Changing the wording to “pocket watch” suddenly produced much better results.</li>
</ul>

<p>Interestingly, another model interpreted the original prompt correctly.</p>

<p>That was the moment something clicked.</p>

<p><strong>The problem wasn’t just prompt engineering.</strong></p>

<p>Different models understood different concepts with different levels of accuracy.</p>

<p>A prompt isn’t a strategy, if one is serious about making a business. Given the advent of agentic cli tools and cursor like editors, there is nothing preventing another person to replicate and refine prompts.</p>

<p>The more categories I looked at, the more obvious it became.</p>

<ul>
  <li>A watch fails differently from jewellery.</li>
  <li>Jewellery fails differently from clothing.</li>
  <li>Clothing fails differently from furniture.</li>
</ul>

<p>Even within watches, a luxury wrist watch and an antique pocket watch have different visual expectations.</p>

<p>Yet our pipeline treated every request as the same problem.</p>

<ul>
  <li>Take a prompt.</li>
  <li>Insert it into a template.</li>
  <li>Call the model.</li>
  <li>Hope for the best.</li>
</ul>

<p><strong>That isn’t really a generation strategy. It’s a wrapper.</strong></p>

<p>The interesting work happens before generation. At some point I stopped asking,</p>

<p>“How do we write a better prompt?”</p>

<p>Instead I started asking,</p>

<p>“How do we decide what should happen before generation even begins?”</p>

<p>That completely changed how I thought about the system.</p>

<p>A user asking for an advertisement isn’t really asking for pixels. They’re describing intent.</p>
<ul>
  <li>What are they selling?</li>
  <li>Who is the audience?</li>
  <li>Is this luxury?</li>
  <li>Is it fashion?</li>
  <li>Is it food?</li>
  <li>Does this become an image?</li>
  <li>A short video?</li>
  <li>A cinematic product showcase?</li>
</ul>

<p>Those decisions shouldn’t be hidden inside prompt text.
They should exist as first class components of the system.</p>

<p>I started imagining a team of specialists. Instead of one giant generation pipeline, I started thinking about specialists.</p>

<p><strong>Imagine a creative director instead of a prompt template.</strong></p>

<p>The director receives a request. The first decision isn’t which prompt to use.</p>

<p>The first decision is which expert should handle the request.</p>

<ul>
  <li>A jewellery expert.</li>
  <li>A fashion expert.</li>
  <li>A luxury goods expert.</li>
  <li>A food photography expert.</li>
</ul>

<p>Each expert:</p>
<ul>
  <li>Understands different failure modes.</li>
  <li>Choose a different model.</li>
  <li>Choose different generation parameters.</li>
  <li>Choose an entirely different pipeline.</li>
</ul>

<p>Some categories might use a LoRA. Others might use a custom workflow. Others might rely on a different base model entirely.</p>

<p>The important part is that specialization becomes intentional instead of accidental.</p>

<p><strong>The router becomes the product</strong></p>

<hr />

<h3 id="the-missing-creator-loop">The Missing Creator Loop</h3>

<p>There was one fatal flaw that no amount of clever routing or LoRA adapters could fix.</p>

<p><strong>We had no creators.</strong></p>

<p>Not a single person on the team regularly made marketing content for a living. We were a group of technical people trying to build a creative tool for a domain we didn’t deeply understand.</p>

<p>We assumed that if we built a good enough prompt-to-video system, creators would naturally come and use it. That was a serious mistake.</p>

<p>Professional short-form video is not a “write one good prompt” problem. It is a craft that involves:</p>

<ul>
  <li>Understanding hook structures that work in the first 1-3 seconds</li>
  <li>Color grading and visual rhythm specific to the platform</li>
  <li>Pacing, cut timing, and visual storytelling</li>
  <li>Rapid iteration across dozens of variations</li>
  <li>Tight feedback loops between concept, generation, and editing</li>
</ul>

<p>None of this was researched. There was no internal studio, no embedded creator, no regular user testing with actual marketing professionals. Even the one video specialist on the team refused to use the product.</p>

<p>We spent months building prose-mirror features and debating internal architecture while completely ignoring the fundamental truth: <strong>if real creators won’t use your tool to make their living, nothing else matters.</strong></p>

<p>The best generative media companies treat creators as the primary feedback mechanism from day one. We treated them as the last-mile problem.</p>

<p>This wasn’t a technical failure.<br />
It was a <strong>creator bankruptcy</strong>.</p>

<hr />

<p>People often say AI startups are just wrappers around foundation models.</p>

<p>They’re usually right.</p>

<p>But I don’t think the wrapper is the interesting part.</p>

<p>The interesting part is everything between user intent and the model call.</p>

<p>A good system might</p>
<ul>
  <li>classify the request.</li>
  <li>Select the appropriate model.</li>
  <li>Choose a category specific adapter.</li>
  <li>Inject constraints, Run validation.</li>
  <li>Evaluate the output, and</li>
  <li>Record what succeeded.</li>
</ul>

<blockquote>
  <p>Over time, that decision layer becomes smarter. Not because the frontier models changed, because your system learned which decisions produce better outcomes.</p>
</blockquote>

<p><em>That is much harder to copy than a collection of prompts.</em></p>

<hr />

<p>This is where LoRA adapters become useful. LoRA is one piece, not the destination</p>

<ul>
  <li>Not because every category needs fine tuning.</li>
  <li>Not because every model should have an adapter.</li>
</ul>

<p>But because some domains genuinely benefit from specialization.</p>

<p>Imagine training adapters only on high quality examples for a specific category.</p>

<ul>
  <li>One for watches.</li>
  <li>One for jewellery.</li>
  <li>One for cosmetics. etc.</li>
</ul>

<p>Now the router isn’t just choosing prompts. It’s choosing expertise.</p>

<p>That expertise can evolve independently as new data arrives.</p>

<p><strong>Data becomes your advantage</strong></p>

<hr />

<p>Every generation tells you something.</p>

<ul>
  <li>Which model was used?</li>
  <li>
    <p>Which adapter? and parameters?</p>
  </li>
  <li>Did the user regenerate?</li>
  <li>Did they download it?</li>
  <li>Did they publish it or reject it?</li>
</ul>

<p>Over time you stop collecting prompts. You start collecting decisions and outcomes.</p>

<p>That feedback loop becomes increasingly difficult for competitors to replicate because it reflects how your users actually create content.</p>

<hr />

<h2 id="looking-back">Looking back</h2>

<p>When I first joined, I thought prompt engineering would be the interesting part of the job.</p>

<p>It wasn’t.</p>

<p>The interesting part was realizing that prompts were only the visible surface of a much larger system.</p>

<p>The companies that survive won’t win because they discovered the perfect prompt.</p>

<p>They’ll win because they build better decision systems.</p>

<p>Foundation models will continue improving.</p>

<p>The real question is no longer which model you call.</p>

<p>It’s how intelligently you decide to call it.</p>]]></content><author><name>ikouchiha47</name></author><summary type="html"><![CDATA[Most teams treat prompts and models as the product. The real moat is understanding the domain deeply enough to build the right decision systems and feedback loops.]]></summary></entry><entry><title type="html">RUNE</title><link href="https://ikouchiha47.github.io/rune/" rel="alternate" type="text/html" title="RUNE" /><published>2026-04-25T00:00:00+00:00</published><updated>2026-04-25T00:00:00+00:00</updated><id>https://ikouchiha47.github.io/rune-index</id><content type="html" xml:base="https://ikouchiha47.github.io/rune/"><![CDATA[<p><img src="/assets/rune/img/IMG_20260502_device_box.jpg" alt="RUNE — plastic box prototype, blue BLE indicator, desk setup" style="max-width:100%;height:auto;display:block;margin:0 auto 1.5rem;" /></p>

<p>RUNE is a wrist-worn gesture controller. Flick right — D-pad right. Pitch down — back. Hold still — nothing happens, which is the correct default. It talks to smart TVs, bulbs, plugs, and desktop inputs over BLE. No voice. No phone in hand. No accent required.</p>

<p>You don’t need an accent to control a smart device. (Alexa 😉😉)</p>

<hr />

<h2 id="what-its-not">What it’s not</h2>

<p>It’s not a fitness tracker. This is a deliberate product decision, not an oversight.</p>

<p>Knowing your HRV every 20 minutes doesn’t improve your health unless you’re doing something with that number. Most people aren’t. If you’re outside having a good time wearing a Rolex, and you glance at your wrist, you can tell — you’re having a good time.</p>

<p>RUNE is a tool you put on when you want to control something, then take off and wear a real watch. Different category entirely.</p>

<hr />

<h2 id="how-it-works">How it works</h2>

<p>Firmware runs on an nRF52840 with a BNO085 — a 9-axis IMU that does sensor fusion on its own ARM Cortex-M0+ at 400Hz. Gesture segmentation happens on the chip: it classifies when motion starts, what shape it makes, and sends a label over BLE.</p>

<p>The phone app is the bridge. It receives the gesture, maps it to a device action, and fires the command — Android TV D-pad injection, smart plug HTTP, whatever. The execution logic lives in TypeScript, not C++.</p>

<p>That split exists for a reason. Iterating gesture matching at phone-flash speed is 10× faster than Arduino-reflash speed. The firmware stays minimal; the app handles the policy layer. Also: I ain’t trusting an LLM with firmware I don’t understand yet. The C++ side is written and reviewed line by line.</p>

<hr />

<h2 id="the-roadmap">The roadmap</h2>

<table>
  <thead>
    <tr>
      <th>Phase</th>
      <th>What ships</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>RUNE-I</strong></td>
      <td>Ship to real users, phone-paired, OTA updates</td>
    </tr>
    <tr>
      <td><strong>RUNE-II</strong></td>
      <td>Push compute on-device, add memory module</td>
    </tr>
    <tr>
      <td><strong>RUNE-III</strong></td>
      <td>Complex gestures — DTW or on-device ML (might split 3A/3B)</td>
    </tr>
    <tr>
      <td><strong>RUNE-IV</strong></td>
      <td>Custom PCB, lower profile, IR transmitter/receiver</td>
    </tr>
    <tr>
      <td><strong>RUNE-V</strong></td>
      <td>EMG pads for intent detection, extendable via pogo pins</td>
    </tr>
  </tbody>
</table>

<p>Everything before RUNE-IV is software. That’s intentional — validate the interaction model before spinning custom silicon. RUNE-V is the version that turns it into something you might actually want on your wrist: clip on the EMG module when you need it, leave it off when you don’t.</p>

<hr />

<h2 id="the-series">The series</h2>

<p><strong><a href="/rune/01-hardware">Part 1: The hardware</a></strong>
What’s in it, what it cost (not cheap), and how it evolved from a glowing breadboard to a plastic box on velcro to a Three.js case model. Thanks to SM Electronics Kodhalli and Robocraze.</p>

<p><strong><a href="/rune/02-sensor-wars">Part 2: The sensor wars</a></strong>
Why 6-DOF failed, what the Kalman gain has to do with it, and why a magnetometer doesn’t fix the problem indoors.</p>

<p><strong><a href="/rune/03-wakeup">Part 3: The chip wouldn’t wake up</a></strong>
Two layered bugs in the sleep/wake path, including a concurrency issue in the SparkFun library that took two days to find.</p>

<p><strong><a href="/rune/04-gestures">Part 4: Eight problems called gesture detection</a></strong>
Segmentation, drift, axis isolation, combo sequences — each one looks simple until you’ve shipped it wrong once.</p>

<p><strong><a href="/rune/05-calibration">Part 5: Calibration, from 125 seconds to 3</a></strong>
MASR throttling, stable windows, and why the timer has to fire in <code class="language-plaintext highlighter-rouge">loop()</code> regardless of IMU events.</p>

<p><strong><a href="/rune/06-app">Part 6: The app</a></strong>
Claude built most of it. Humans tested the joints. The androidtvremote2 protocol, DTW, and what required a real TV.</p>

<p><strong><a href="/rune/07-power">Part 7: Power on 300mAh</a></strong>
Adaptive sample rates, staged sleep tiers, and the nine hours we spent watching INT blink at nothing.</p>

<p><strong><a href="/rune/08-gesture-symbols">Research on RUNE-III: Recognising Symbols in the Air</a></strong>
DTW, feature vectors, the $1 recognizer, shapelets, HMMs — five approaches to 1-shot gesture matching, ranked by how much they ask of you.</p>

<hr />

<p>The hardware exists. The gestures work. The parts that were broken are documented in the posts above, not papered over.</p>

<p><strong><a href="/rune/09-epilogue">Epilogue: Read the Datasheet</a></strong>
Every meaningful fix came from reading something first. Not Googling “BNO085 not working” and hoping for a hit. Reading the SH-2 Reference Manual. Reading the SparkFun library source. Reading the datasheet section on MASR. Every time something broke in a non-obvious way, the answer was in a document that existed before the problem did.</p>]]></content><author><name>ikouchiha47</name></author><summary type="html"><![CDATA[Building a wrist gesture controller for smart devices — TV, bulbs, plugs, desktop. Not a fitness tracker.]]></summary></entry></feed>